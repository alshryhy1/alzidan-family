
# عائلة الزيدان — Engineering Roadmap (إدارة مشروع + مرجع هندسي)

**الحالة:** معتمد رسميًا — وثيقة إدارة مشروع (ليست قائمة مزايا فقط)  
**التاريخ:** 2026-08-07  
**آخر تحديث تخطيط:** 2026-08-09 — مراجعة رحلة الطلب [`REQUEST-JOURNEY-REVIEW-v1.md`](./REQUEST-JOURNEY-REVIEW-v1.md) · بوابة 1 🟡 · بوابة 2 🟡 · لا كود DW  
**القرارات المعمارية المقفلَة:** [`docs/ADR.md`](./ADR.md)

### دستور المنصة مقابل هذه الخارطة

| | تجيب عن |
|--|---------|
| **هذه الخارطة (Roadmap)** | **ماذا نبني؟** ومتى — المراحل · الحالة · الدين · الانحدار |
| **دستور المنصة (Constitution)** | **كيف نفكر؟ وكيف نبني؟ وما الذي يُمنع؟** — **دستور حي** لا «مرحلة وثائق» |

**الوثائق الأربع (دستور حي):**

1. [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md) — لغة المنتج · Human First · ثلاث لغات · محظورات  
2. [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) — حالات · انتقالات · تعيين · Audit · إشعارات *(≡ `WORKFLOW-SPECIFICATION.md`)*  
3. [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) — فهرس النوايا · المرجع الوحيد لإضافة عملية  
4. [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) — المبادئ · قواعد الطبقة · لغة المستخدم · Truth Before Speed · SSOT · Single Write Rule  

مرجع مرحلة مستقلة: [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md).  
مراجعة رحلة الطلب كاملة (as-is → to-be · قبل أي RX/DW UI): [`REQUEST-JOURNEY-REVIEW-v1.md`](./REQUEST-JOURNEY-REVIEW-v1.md) — **لا تطوير لواجهة المندوب الآن**.

وثيقة التنفيذ الهندسية الرسمية لمشروع عائلة الزيدان. المنصة = **معرفة عائلية + سير اعتماد** — ليست شجرة فقط. تُدار كـ**برنامج مشاريع** بثلاثة مسارات + طبقات معمارية ثابتة + بنية تحتية مشتركة.

**سياسة عامة للمشروع:** لا «مقبول / تم» بلا تطبيق متحقَّق — على الطلبات، الصور، الزوجات، الدمج، وأي اعتماد لاحق.  
**بعد القواعد الدستورية:** لا تطوير خارج المسار — أي فكرة تُسأل أولًا: «في أي مرحلة/طبقة؟»

### بوابتان قبل بدء مراحل المنتج (Request Experience فما بعد)

الحالة صادقة: **البوابتان ما زالتا 🟡** — لا تُعلَنان 🟢 إلا بعد القبول / التجميد.  
**لا يُشترط** Workflow Engine 🟢 قبل Request Experience. إغلاق المحرك (سلامة + إزالة مسار اعتماد قديم + إعلان استقرار) يحدث **عند الوصول لمرحلته في السلّم**.

| البوابة | الاسم | الحالة | المعنى |
|---------|--------|--------|--------|
| **1** | **Delegates v2** | 🟡 | فشل قبول حي (دخول بعد القبول) — ثبّت منفّذ Workspace v2 (`maint.sql_workspace_run_v2` / CREATE OR REPLACE) ثم `maint.fix_delegate_portal_path_v1` + إعادة اختبار |
| **2** | **Product Foundation** = **دستور المنصة / دستور حي** | 🟡 | تجميد الدستور + **Truth Before Speed** — ليست «مرحلة وثائق» |

**بوابة 1 — Delegates v2 🟡→🟢** (قبول فقط):

- اختبار الصلاحيات  
- اختبار الأدوار  
- اختبار التفعيل والتعطيل  
- اختبار سجل التدقيق  

**بوابة 2 — Product Foundation 🟡→🟢** (**دستور المنصة / دستور حي** — ليس «مرحلة docs»):

- تجميد الوثائق الأربع كدستور حي  
- اعتماد Product Language · Workflow Specification · Request Catalog · Platform Principles  
- اعتماد **Truth Before Speed** — الصحة قبل سرعة الاعتماد؛ كل الفحوصات قبل طفرة الشجرة  
- يمنع أي تطوير يخالفها إلا بقرار معماري  

### السلّم الرسمي (SSOT ladder)

```
Delegates v2
        ↓
Product Foundation
        ↓
Request Experience
        ↓
Validation Engine          ← مرحلة مستقلة جديدة
        ↓
Workflow Engine            ← إغلاق/توسيع عند هذه المرحلة (لا بوابة قبل RX)
        ↓
Delegate Workspace
        ↓
Admin UX
        ↓
Family Engine Alignment
        ↓
iOS Experience Rebuild
```

**ملاحظة Workflow على main:** أساس Workflow Engine v1 الموجود = **مؤقت / provisional** — لا يُوسَّع كإنتاج حتى تُنشأ مرحلة Validation Engine وتتم المواءمة.

**الهدف التنفيذي الحالي:** إغلاق البوابتين 1→2 (🟡 الآن) → Request Experience → Validation Engine → ثم إغلاق Workflow على السلّم. iOS **بعد** المراحل الكانونية فقط.

---

## قواعد تشغيل

### قاعدة تسليم SQL (Standing · 2026-08-09)

بعد إضافة SQL Workspace: **مسار المشغّل = الإدارة → أدوات الصيانة → أوامر الصيانة الجاهزة → تشغيل → مُنفذ**.  
لا تُرسل تعليمات «افتح Supabase والصق COPY-ME» كمسار افتراضي. الملفات في `supabase/sql/` تبقى مصدر الحقيقة. الاستثناء الوحيد الموثّق: فشل ترقية المنفّذ بسبب قيد تقني قديم (`SQL-WS-MULTI` على أجسام الدوال) — مرة واحدة ثم Workspace لكل ما بعده. التفاصيل: [`SQL-WORKSPACE-REPORT.md`](./SQL-WORKSPACE-REPORT.md).


## 0) نموذج الحالات (Status Model)

لا يكفي ✅. لكل بند مادّي يُستخدم أحد الحالات التالية:

| الرمز | الحالة | المعنى التشغيلي |
|--------|--------|------------------|
| 🟢 | **منشور ومختبر** | كود + SQL (إن لزم) على البيئة المعنية + دخان/قبول ناجح |
| 🟡 | **كود مكتمل — بانتظار** | المنطق جاهز في المستودع؛ ينقص SQL و/أو Hard Refresh و/أو اختبار قبول |
| 🔵 | **قيد التنفيذ** | شريحة نشطة قيد الكتابة/المراجعة |
| ⚪ | **مخطّط** | معتمد في الترتيب؛ لم يبدأ التنفيذ |
| ⬛ | **مؤجّل** | مؤجّل عمدًا (بوابة أو قرار منتج) |

**عمود «بانتظار» الشائع:** `SQL` · `Hard Refresh` · `اختبار قبول` — يُذكر صراحة بجانب 🟡.

### تقييم صادق — بنود حديثة (2026-08-08)

| البند | الحالة | ملاحظة |
|--------|--------|--------|
| **Admin Hub / Phase 1 شِلّ** | 🟢 | شِلّ + Hub + `#module=` — [`ADMIN-REDESIGN-PHASE1.md`](./ADMIN-REDESIGN-PHASE1.md)؛ **SQL Workspace** داخل `#module=tools` — [`SQL-WORKSPACE-REPORT.md`](./SQL-WORKSPACE-REPORT.md) (منفّذ v2 عبر CREATE OR REPLACE فقط — مسار `pg_proc` مُلغى؛ RPC 🟡 حتى تثبيت v2 مرة) |
| **Delegates v2 — Foundation + Enforce** | 🟡 | قبول حي فشل على تفعيل الدخول بعد «قبول»؛ إصلاح مسار التفعيل في المستودع — [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md). Multi-stage = ⚪ |
| **tree-import reuse** | 🟡 | عميل + `verify:tree-import-reuse`؛ **بانتظار COPY-ME على الإنتاج** — [`PATCH-TREE-IMPORT-REUSE.md`](./PATCH-TREE-IMPORT-REUSE.md) · Data Version `5` معلّق النشر |
| **Integrity Engine v2 (RPC/views)** | 🟡 | ملف SQL جاهز؛ **بانتظار نشر SQL** — [`PATCH-INTEGRITY-DEPLOY-SQL.md`](./PATCH-INTEGRITY-DEPLOY-SQL.md) · قراءة فقط |
| **Health Center** | 🟢 | أساس قراءة فقط في الإدارة |
| **Repair يدوي** | 🟢 | مغلق للتخطيط — لا مسار apply تلقائي من الواجهة |
| **DateEngine (أساس + حارس ends_at)** | 🟢 | مكتبة + ADR-009 + مستهلك أوّل |
| **DateEngine على كل النماذج** | ⚪ | ميلاد/وفاة/مناسبات — دين تقني (§ دين تقني) |
| **Admin-SC / قائمة البطاقات** | 🟡 | حفظ ✅؛ قائمة **بانتظار SQL** `admin_special_cards_list_v1` إن لم يُطبَّق |
| **TREE-004** | 🟢 | عزل أبناء + unlink يدوي مكتمل؛ **لا يُعاد** |
| **Product Foundation (بوابة 2)** | 🟡 | **دستور حي** + Truth Before Speed — **بانتظار التجميد** بعد 🟢 Delegates — ليست مرحلة docs |
| **Request Experience** | ⚪ | بعد البوابتين 1+2 🟢 فقط — **لا يُشترط** Workflow 🟢 |
| **Validation Engine** | ⚪ | مرحلة مستقلة بعد RX — [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) |
| **Workflow Engine v1** | 🟡 | أساس **provisional** على main — إغلاق (سلامة + إزالة مسار قديم) **عند مرحلته** بعد Validation — لا توسيع قبلها — [`WORKFLOW-ENGINE-V1-REPORT.md`](./WORKFLOW-ENGINE-V1-REPORT.md) |
| **Delegate Workspace v1** | ⚪ | UX ✅ معتمد («ابدا») — [`DELEGATE-WORKSPACE-UX-v1.md`](./DELEGATE-WORKSPACE-UX-v1.md) · **كود ❄️** حتى RX→VE→WF · رحلة الطلب: [`REQUEST-JOURNEY-REVIEW-v1.md`](./REQUEST-JOURNEY-REVIEW-v1.md) |
| **Admin UX (مراقبة)** | ⚪ | بعد Delegate Workspace |
| **Family Engine Alignment** | ⚪ | مواءمة + **Single Write Rule** — لا إعادة بناء |
| **iOS Experience Rebuild** | ⬛ | بعد إغلاق المراحل الكانونية فقط |

---

## 1) المعمارية الأساسية (Core Architecture) — ثابتة

**غير قابلة للتغيير إلا بقرار معماري صريح (ADR).** الطبقات من الأعلى للأسفل:

```
                المستخدم
                   │
                   ▼
      Request Experience     ← ماذا يريد أن يفعل؟ (نية / حقائق)
                   │
                   ▼
      Validation Engine      ← يتحقق — لا ينفّذ (تعارض · تكرار · اكتمال · سلامة)
                   │
                   ▼
        Workflow Engine      ← إنشاء · حالات · تعيين · سجل · إشعارات (مستقل عن الواجهة)
                   │
          ┌────────┴────────┐
          ▼                 ▼
 Delegate Workspace      Admin UX
      (التنفيذ)           (مراقبة التدفق)
                   │
                   ▼
        Family Engine / Business Services
     (شجرة · مناسبات · ذكريات · بطاقات · …)  ← Single Write Rule
                   │
                   ▼
              Data Layer
```

**Presentation (تفصيل الأدوار):**

```
Presentation
  ├── Request Experience / Visitor   ← نية + متابعة المقدّم
  ├── Delegate Workspace             ← إنجاز العمل على الطلب
  └── Admin Workspace                ← مراقبة التدفق والاختناقات
```

### فصل المسؤوليات (لا تُخلط)

| الطبقة | المسؤولية | تعرف عن الواجهة؟ |
|--------|-----------|------------------|
| **Delegates v2** | من لديه صلاحية (دور · فرع · عمليات مسموحة · تفعيل) | لا — كتالوج وصلاحيات |
| **Request Experience** | كيف يعبّر الزائر عن النية ويتابع العملية | واجهة نية فقط |
| **Validation Engine** | فحص الحقائق قبل المندوب/الشجرة — يتحقق ولا ينفّذ | لا — طبقة محرك |
| **Workflow Engine** | ماذا يحدث للطلب (إنشاء · تعيين · انتقال حالة · إشعار · Audit · deep link) | **لا شيء** عن شكل الشاشة |
| **Delegate Workspace** | كيف يعمل المندوب (استلام · مراجعة · أدوات في سياق الطلب · قرار · التالي) | واجهة تنفيذ فقط |
| **Admin Workspace** | كيف يراقب المدير التدفق (أين عالق · الحمل · التأخير · الاختناقات) | واجهة مراقبة فقط |
| **Family Engine (خدمات)** | أثر البيانات بعد الاعتماد — عقود واضحة؛ لا كتابة مباشرة من UI | لا تعرف الواجهة |

- Validation Engine وWorkflow Engine **ليسا** جزءًا من Delegate Workspace ولا Admin UI ولا Request Experience.
- كل أنواع الطلبات تشترك في **نفس عُقد المحرك** — الفرق = **Request Type** من [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md).
- المواصفة: [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) · التحقق: [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) · القرار: **ADR-010** · المبادئ: [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md).

---

## 1ب) مبادئ المنتج — ثابتة (ملخص)

**المصدر الكامل:** [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) (دستور). ملخص سريع:

| # | المبدأ |
|---|--------|
| 1 | **Human First** + **No Database Thinking** + **لغة المستخدم أولًا** |
| 2 | **الطلب محور العمل** · الطلب = **جلسة عمل** |
| 3 | كل طلب له **مالك واحد واضح في كل لحظة** |
| 4 | كل انتقال حالة **يُسجَّل** (Audit) |
| 5 | **Truth Before Speed** — الصحة قبل سرعة الاعتماد |
| 6 | **Validation Engine** يتحقق ولا ينفّذ — قبل المندوب/الشجرة |
| 7 | **Workflow = مصدر الحقيقة** لحالة الطلب |
| 8 | **Single Write Rule** — لا كتابة شجرة مباشرة من UI؛ عبر Workflow + Validation |
| 9 | الواجهات **بلا منطق أعمال** — تستخدم المحركات |
| 10 | **واجهة واحدة لكل دور** · الحقائق تُقدَّم — النظام يتحقق ويربط |
| 11 | أي إشعار من **انتقال حالة** — لا من زر/شاشة |
| 12 | **قاعدة الثلاث لغات** · **SSOT** · **Zero Duplicate Logic** |
| 13 | الصلاحيات **لا** تعتمد على إخفاء الأزرار فقط |

### قواعد دستورية (مرآة [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md))

| | القاعدة |
|--|---------|
| **أ — الطبقة أولًا** | لا يجوز إنشاء أي شاشة/نموذج/ميزة قبل تحديد طبقتها: Product Language؟ · Request Experience؟ · **Validation Engine**؟ · Workflow؟ · Delegate Workspace؟ · Admin UX؟ · Family Engine؟ — إن تعذّر → **لا يبدأ التنفيذ**. |
| **ب — لغة المستخدم أولًا** | كل واجهة تُصمم بلغة المستخدم أولًا، ثم تُربط بالمحرك، ولا تُبنى مباشرة فوق قاعدة البيانات. |
| **ج — Truth Before Speed** | الصحة وجودة الشجرة أهم من سرعة الاعتماد؛ كل الفحوصات قبل طفرة الشجرة. |

بعدها: **لا تطوير خارج المسار** — أي فكرة: «في أي مرحلة/طبقة؟»

### قاعدة بوابة الميزة (Feature Gate) — قبل أي كود ميزة جديدة

0. **أي طبقة؟** (قاعدة أ) — بلا جواب → لا كود  
1. نية/عملية جديدة؟ → [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) أولًا + لغة المنتج  
2. صلاحية جديدة؟ → **Delegates v2**  
3. فحص حقائق/تعارض/اكتمال؟ → **Validation Engine**  
4. تضيف/تغيّر حالة طلب؟ → **Workflow Engine**  
5. عرض نية الزائر؟ → **Request Experience** (بعد البوابتين 🟢)  
6. عرض/تنفيذ مندوب أو مراقبة؟ → **Delegate Workspace** أو **Admin UX**

خرق البوابة = رفض التصميم قبل التنفيذ.

### مبادئ هندسية مكمّلة (ما زالت سارية)

| # | المبدأ |
|---|--------|
| E1 | هوية الشجرة / **Canonical Person Identity** أولًا عند الكتابة على البيانات العائلية |
| E2 | **لا قبول بلا تطبيق متحقَّق** (ADR-006) |
| E3 | محرك الموبايل = منطق الويب في بناء العقد (ADR-005) |
| E4 | حماية البيانات قبل أي إصلاح (Backup → Scan → تقرير) |
| E5 | كل Patch: Validation + مسار Rollback |
| E6 | السلّم: **بوابتان (Delegates · Foundation) → Request Experience → Validation Engine → Workflow Engine → Delegate Workspace → Admin UX → Family Engine Alignment → iOS** |
| E7 | ممنوع القفز إلى iOS قبل إغلاق البوابتين والمراحل الكانونية |
| E8 | **قواعد الانحدار (§8) غير قابلة للتفاوض** |
| E9 | تجميد الدستور الحي قبل Request Experience؛ إغلاق Workflow عند مرحلته (بعد Validation) — لا توسيع provisional قبلها |
| E10 | **قواعد أ+ب+ج** دستورية — بلا طبقة / بلا لغة مستخدم / بلا Truth Before Speed = لا تنفيذ |
| E11 | **Single Write Rule** — لا كتابة شجرة خارج Workflow + Validation |

---

## 2) Canonical Person Identity

```
person_id          ← هوية مستقلة مستقرة
    ↓
Node Path          ← موقع كانوني في الشجرة
    ↓
Display Name       ← للعرض
    ↓
Search Name        ← للبحث/التطبيع
```

كل كتابة تمر بـ `person_id`. ممنوع الربط الغامض بالاسم (`limit(1)` / `min()` على تطابق متعدد) **عند توفّر `person_id`**.

---

## 3) Versioning للبيانات

يظهر في Patch 0 وHealth Center وDiagnostics — التفاصيل الحيّة: [`DATA-VERSIONS.md`](./DATA-VERSIONS.md).

| الحقل | المعنى |
|--------|--------|
| **Schema Version** | إصدار هيكل الجداول/القيود |
| **Data Version** | إصدار منطق البيانات / قواعد الهوية |
| **Migration Version** | آخر migration script طُبّق بنجاح **على الإنتاج** |

الغرض: معرفة هل البيانات القديمة مرّت بالإصلاح قبل تشخيص أي عطل جديد. وجود الملف في المستودع ≠ تطبيق على الإنتاج (انظر 🟡).

---

## 4) البرامج الثلاثة (فصل صارم)

العمل يُصنَّف تحت أحد البرامج التالية — لا تُخلط أولويات «إصلاح بيانات» مع «مزايا تطبيق» في نفس الشريحة التنفيذية.

### أ) برنامج إصلاح البيانات

| المسار | المحتوى | الحالة |
|--------|---------|--------|
| **Integrity** | محرك سلامة قراءة فقط (views + RPC تقرير) | 🟡 v2 بانتظار SQL · أساس v1/تقرير 🟢 |
| **Health Center** | واجهة المدير للمشاكل (قراءة فقط) | 🟢 |
| **Tree Repair** | تنظيف/إصلاح يدوي + dry-run؛ لا auto-repair من UI | 🟢 مغلق للتخطيط (يدوي) |
| **Data Migration** | Versioning · COPY-ME · backfill هويات/تواريخ | 🔵/🟡 حسب الملف المعلّق |

**بوابة:** أي إصلاح يكتب على الشجرة → Backup → Dry Run → موافقة → Apply → Audit. Health Center **لا** يطلق إصلاحًا تلقائيًا.

### ب) برنامج تطوير الإدارة + سير الاعتماد

يتبع المعمارية (§1): صلاحيات → محرك مستقل → مساحات عمل. التفاصيل: §22.

| المسار | المحتوى | الحالة |
|--------|---------|--------|
| **Admin Hub / Redesign** | شِلّ موديولات (عرض فقط — بلا منطق سير) | 🟢 أساس · تكراري |
| **Delegates v2** | أدوار · عمليات · فرع · تفعيل/تعطيل · Audit صلاحيات | 🟡 **بوابة 1** — فشل دخول بعد القبول؛ إصلاح بانتظار إعادة اختبار — [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md) |
| **Product Foundation** | **دستور حي** + Truth Before Speed — تجميد لا «مرحلة docs» | 🟡 **بوابة 2** — بانتظار التجميد بعد 🟢 Delegates |
| **Request Experience** | نية المستخدم · نماذج قصيرة · تتبع إنساني | ⚪ — بعد البوابتين 🟢 |
| **Validation Engine** | تحقق قبل المندوب/الشجرة — لا تنفيذ | ⚪ — [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) |
| **Workflow Engine v1** | حالات · تعيين · سجل · إزالة مسار اعتماد قديم · إعلان استقرار | 🟡 أساس **provisional** — إغلاق عند مرحلته بعد Validation — [`WORKFLOW-ENGINE-V1-REPORT.md`](./WORKFLOW-ENGINE-V1-REPORT.md) |
| **Delegate Workspace v1** | موديولات تنفيذ المندوب فوق المحرك | ⚪ — بعد Workflow على السلّم |
| **Admin UX / مراقبة** | لوحة تدفق · تأخير · حمل مندوبين · بحث · bulk · تقارير | ⚪ |
| **Family Engine Alignment** | مواءمة + **Single Write Rule** · خدمات واضحة | ⚪ (مواءمة لا إعادة بناء) |
| **Audit** | سجل صلاحيات + انتقالات (المحرك يكتب انتقالات الحالة) | 🟡 بانتظار SQL أساس المناديب · واجهة موجودة |

### ج) برنامج تطوير التطبيق

| المسار | المحتوى | الحالة |
|--------|---------|--------|
| **Search** | محرك بحث حقيقي (اسم، كنية، فرع، …) | ⬛ بعد الإدارة |
| **Member page** | بطاقة عضو تفاعلية | ⬛ |
| **Notifications** | فئات + مركز إشعارات | ⬛ |
| **Activity / Timeline / Media / …** | بقية § UX الموبايل | ⬛ |

**صريح:** برنامج (ج) لا يُبدأ كمسار أول قبل تقدم برنامج (ب).

---

## 5) بنية تحتية — DateEngine (ADR-009)

**ليست ملاحظة جانبية.** DateEngine طبقة بنية تحتية مشتركة عبر البرامج الثلاثة.

| # | البند | الحالة |
|---|--------|--------|
| **DE-1** | مكتبة `assets/js/modules/date-engine.js` (parse / validate / convert / format / normalizeDigits / synced pair / timestamp guard) | 🟢 |
| **DE-2** | ADR-009 + حظر `new Date()` لتواريخ الأعمال | 🟢 موثّق |
| **DE-3** | مستهلك أول: حارس + تحقق حي على `polls-ends-at` | 🟢 |
| **DE-4** | ربط **كل** نماذج الميلاد/الوفاة/المناسبات بـ `toSyncedPair` | ⚪ دين تقني |
| **DE-5** | هجرة مخطط تدريجية لأعمدة `*_g` / `*_h` حيث ينقص الزوج | ⚪ — ليست في الجولة الحالية |

**قاعدة غير قابلة للتفاوض:** أي تاريخ أعمال جديد → DateEngine. أعمدة `timestamptz` ميلادية ترفض سنوات هجرية (`DATE-002`). انظر أيضًا § قواعد الانحدار.

---

## 6) Tree Engine v2 — مرحلة مستقلة

مرحلة منتج/معمارية **خاصة** (وليست بندًا فرعيًا تحت Admin UX). تُنفَّذ بعد استقرار الكتابة الحالية وIntegrity قراءة فقط، وقبل توسيع إصلاحات تلقائية واسعة.

| القدرة | الوصف | الحالة |
|--------|--------|--------|
| **person_id فقط** | كل نقل/ربط/دمج عبر UUID؛ لا مسار اسم عند وجود هوية | ⚪ (جزئيًا مفروض في مسارات الكتابة الحالية 🟢) |
| **لا name link** | إزالة بقايا حل الأب/الشخص بالاسم في محرك الشجرة | ⚪ دين تقني |
| **Duplicate detection** | اكتشاف أشخاص/عقد مكررة قبل الدمج | ⚪ |
| **Merge persons** | دمج هويتين مع سجل تدقيق + dry-run | ⚪ |
| **Move branches** | نقل فرع تحت أب جديد بـ `person_id` | ⚪ |
| **Dry Run** | كل عملية كتابة تعرض أثرًا متوقعًا قبل التنفيذ | ⚪ إلزامي عند البدء |
| **Rollback** | مسار استرجاع موثّق لكل عملية جماعية | ⚪ |

**علاقة بالبرامج:** تخدم برنامج (أ) إصلاح البيانات وتُغذي Admin عبر موديول الشجرة — دون خلطها مع مزايا تطبيق (ج).

---

## 7) الدين التقني (Technical Debt)

| # | البند | البرنامج | الأولوية |
|---|--------|----------|----------|
| **TD-1** | **DateEngine على كل النماذج** (ميلاد/وفاة/مناسبات وليس `ends_at` فقط) | بنية تحتية | عالية |
| **TD-2** | **إلغاء الربط بالاسم عند وجود `person_id`** في كل مسارات الكتابة/الاستيراد المتبقية | أ + Tree Engine v2 | حرجة |
| **TD-3** | **إزالة منطق أسماء الآباء القديمي** (`parent_name` كمصدر حقيقة / fallback صامت) | أ + Tree Engine v2 | حرجة |
| **TD-4** | **توحيد محرك البحث إدارة ↔ تطبيق** (نفس التطبيع والفهارس والقواعد) | ب + ج | متوسطة — بعد Admin UX بحث |
| **TD-5** | فرض صلاحيات Delegates داخل RPC الفعلية (ليس الكتالوج فقط) | ب | 🟡 SQL مُطبَّق — بانتظار قبول (`COPY-ME-delegates-v2-enforce.sql`) |
| **TD-6** | تحميل كسول لموديولات الإدارة الثقيلة | ب | منخفضة (تكراري P1) |

لا يُغلق «استقرار الشجرة» بينما TD-2/TD-3 ما زالا مفتوحين في مسارات نشطة.

---

## 8) قواعد الانحدار (Regression Rules) — غير قابلة للتفاوض

تُراجع في كل PR يمس الشجرة أو الاعتماد أو التواريخ أو الواجهة الإدارية:

| # | القاعدة |
|---|---------|
| **R-1** | **لا ربط بالاسم إذا توفّر `person_id`** — أي `limit(1)` / تطابق اسم متعدد مع تجاهل UUID = رفض |
| **R-2** | **لا حذف تدميري مباشر من الواجهة بلا ضمانات** — تأكيد صريح + (حيث ينطبق) dry-run / صلاحية / أثر معروض |
| **R-3** | **إصلاحات الشجرة تبدأ بـ Dry Run** — لا Apply صامت |
| **R-4** | **لا تُعرض أخطاء Supabase الخام للمستخدم النهائي** — رسالة عربية + رمز خطأ (`TREE-*` / `REQ-*` / …) |
| **R-5** | **كل تواريخ الأعمال عبر DateEngine** — لا `new Date(...)` لتواريخ المجال (ADR-009) |
| **R-6** | **تغييرات الشجرة → Audit** — كل اعتماد/إصلاح/دمج/نقل يكتب سجلًا قابلًا للمراجعة |
| **R-7** | **Health Center قراءة فقط** — ممنوع auto-repair من واجهة مركز الصحة |
| **R-8** | **لا منطق سير/تحقق في الواجهة** — الحالات عبر Workflow · التحقق عبر Validation Engine · الصلاحيات عبر Delegates (ADR-010) |
| **R-9** | **Single Write Rule** — لا كتابة شجرة مباشرة من UI؛ كل إضافة/تعديل عبر Workflow + Validation |

خرق أي قاعدة أعلاه يوقف إغلاق البند حتى التصحيح أو ADR جديد صريح.

---

## 9) Architecture Decisions (ADR)

قرارات معمارية **مقفلة** — ليست اقتراحات. التفاصيل الكاملة في [`docs/ADR.md`](./ADR.md).

| ADR | القرار |
|-----|--------|
| ADR-001 | الهوية تعتمد على `person_id` (Canonical Person Identity) |
| ADR-002 | ممنوع الربط بالاسم عند الكتابة |
| ADR-003 | Event Bus هو المصدر الوحيد لأحداث المجال بعد الاعتماد |
| ADR-004 | Health Center مرجع صحة البيانات (Diagnostics للتفاصيل التقنية) |
| ADR-005 | محرك شجرة واحد — Web / Admin / Mobile بلا محركات متباعدة |
| ADR-006 | لا قبول بلا تطبيق متحقَّق (سياسة عامة) |
| ADR-007 | حماية Patch 0 قبل أي إصلاح يمس البيانات |
| ADR-008 | سياسة الكاش مربوطة بالأحداث وإصدارات البيانات |
| ADR-009 | DateEngine موحّد (هجري⇄ميلادي) — بنية تحتية؛ لا `new Date()` لتواريخ الأعمال؛ لا هجري في timestamp ميلادي |
| ADR-010 | **Workflow Engine طبقة مستقلة** — بلا UI؛ كل أنواع الطلبات نفس العُقد؛ الواجهات بلا منطق أعمال |
| ADR-011 | **دستور المنصة** — أربع وثائق · SSOT · Zero Duplicate Logic · Family Engine Alignment · خارطة مراحل محدَّثة |

أي خرق لهذه القرارات يُوقف الإغلاق حتى يُصحَّح أو يُوثَّق ADR جديد صراحةً.

---

## 10) ترتيب التنفيذ (ملخص)

### مكتمل ومنشور/مختبر 🟢 (أساس)

```
Patch 0     Backup → Integrity Scan → تقرير → Versioning
Identity    Canonical Person Identity (مسارات الكتابة الأساسية)
Requests    قبول = إنشاء + ربط متحقَّق
Spouses     زوجات عبر person_id
Mobile 36   إزالة تكرار العقد (iOS)
News        مصدر ظهور واحد + سياسة كاش
Repair      تنظيف بيانات يدوي (مغلق للتخطيط)
Health      Health Center قراءة فقط
Admin P1    شِلّ Hub + موديولات (أساس)
DateEngine  مكتبة + حارس ends_at
TREE-004    عزل حالة الأبناء
```

### بانتظار نشر/قبول — البوابتان 🟡 + أساس مؤقت

```
بوابة 1  Delegates v2 — اختبار قبول (صلاحيات·أدوار·تفعيل·تدقيق) فقط
بوابة 2  Product Foundation — تجميد الدستور الحي + Truth Before Speed (ليس docs phase)
—        Workflow Engine v1 foundation = provisional على main (لا توسيع قبل Validation)
—        tree-import reuse SQL · Integrity Engine v2 SQL · Admin-SC-List (إن لزم)
```

### الترتيب الكانوني / السلّم الرسمي (معتمد — لا يُعاد ترتيبه بلا قرار)

```
═══ بوابتان قبل مراحل المنتج (🟡 الآن) ═══
★ 1  Delegates v2                 اختبار قبول — ليس برمجة جديدة                               🟡
★ 2  Product Foundation           دستور حي + Truth Before Speed — تجميد                       🟡
═══ بعد البوابتين 🟢 — السلّم ═══
     Request Experience           نية المستخدم                                               ⚪
     Validation Engine            تحقق مستقل — لا تنفيذ                                      ⚪
     Workflow Engine              سلامة + إزالة مسار اعتماد قديم + إعلان استقرار             🟡 provisional
     Delegate Workspace v1        تنفيذ فوق المحرك                                           ⚪
     Admin UX                     مراقبة تدفق                                                  ⚪
     Family Engine Alignment      مواءمة + Single Write Rule                                 ⚪
     iOS Experience Rebuild       إعادة بناء تجربة iOS                                       ⬛
  —  Tree Engine v2               مرحلة بيانات مستقلة (§6) — لا تستبدل السلّم                 ⚪
```

**بوابة صارمة:** ممنوع القفز من قبول Delegates إلى Request Experience دون تجميد الدستور الحي.  
**ليس بوابة:** Workflow Engine 🟢 — يحدث عند مرحلته بعد Validation Engine.  
**دستور حي:** [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) · [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md) · [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) · [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) · [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md).

Roadmap = ماذا نبني · Constitution = كيف نفكر وما يُمنع (دستور حي — ليس docs phase).

### مسارات متوازية بعد Patch 0

| مسار | المحتوى | البرنامج | الحالة |
|------|---------|----------|--------|
| **A كتابة** | هوية → طلبات → زوجات | أ | 🟢 |
| **B عرض** | Patch Mobile 36 | ج (عرض) | 🟢 |
| **C أخبار/كاش** | Patch News Expiry | ج/كاش | 🟢 |
| **D إصلاح/سلامة** | Repair · Integrity · Health | أ | 🟢 أساس · 🟡 Integrity v2 SQL |
| **E إدارة/سير** | بوابتان 🟡 → RX → Validation → Workflow → DW → Admin UX → Family Align → iOS | ب | 🟢 P1 · 🟡 Gates 1–2 · 🟡 WE provisional · ⚪ الباقي |
| **F UX موبايل / iOS** | إعادة بناء تجربة iOS · بحث · عضو · إشعارات · … | ج | ⬛ |
| **I بنية تحتية** | DateEngine عبر كل الأسطح | — | 🟢 أساس · ⚪ تعميم النماذج |
| **T Tree Engine v2** | person_id-only · merge · move · dry-run | أ/ب | ⚪ |
| **V Validation** | محرك تحقق مستقل | ب (طبقة) | ⚪ — [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) |
| **W Workflow** | محرك سير مستقل — provisional حتى مرحلة السلّم | ب (طبقة) | 🟡 أساس v1 — [`WORKFLOW-ENGINE-V1-REPORT.md`](./WORKFLOW-ENGINE-V1-REPORT.md) |

---

## 11) Patch 0 — حماية البيانات

| خطوة | التفاصيل |
|------|----------|
| Backup كامل | جداول الشجرة، الطلبات، الزوجات، الأخبار، … |
| Integrity Scan | تكرارات، أبناء بلا أب، `parent_person_id`، زوجات بلا زوج، cycles، طلبات مقبولة بلا أثر |
| تقرير | يُحفظ ويُراجع قبل Patch 1 |
| تسجيل Versioning | Schema / Data / Migration |
| **Rollback Plan** | طريقة الاسترجاع موثّقة ومجرَّبة على نسخة الاختبار إن أمكن |
| بوابة | لا Patch 1 بدون اكتمال ما سبق |

### قالب كل Patch لاحق

```
تطبيق الإصلاح
    ↓
Validation (معايير القبول + KPI جزئية)
    ↓
إن فشل → Rollback إلى Backup / Migration السابق
```

---

## 12) Risk Register

| الخطر | الاحتمال | التأثير | التخفيف |
|--------|----------|---------|---------|
| إصلاح الهوية يكسر بيانات قديمة | متوسط | عالٍ | Backup + Rollback |
| Migration في المستودع دون تطبيق إنتاج | عالٍ | عالٍ | نموذج 🟡 + DATA-VERSIONS |
| Cache لا يتحدث | متوسط | متوسط | Event Bus |
| اختلاف Web/Mobile | متوسط | عالٍ | Compatibility Matrix |
| قبول طلب بلا أثر على الشجرة | متوسط | عالٍ | سياسة الاعتماد المتحقَّق + `REQ-001` |
| انحراف قواعد Tree Engine بين المنصات | منخفض | عالٍ | Single Tree Engine (ADR-005) |
| auto-repair من Health Center | منخفض | عالٍ | R-7 — قراءة فقط |

---

## 13) جرد الـ Patches والبنود

| # | البند | الحالة | ملاحظات |
|---|--------|--------|---------|
| **0** | حماية + Versioning + Rollback | 🟢 | |
| **1** | هوية / منع الربط الغامض | 🟢 | أساس A |
| **2** | قبول إضافة ابن = تطبيق فعلي | 🟢 | سياسة الاعتماد العامة |
| **3** | زوجة عبر `person_id` | 🟢 | |
| **36** | تكرار الجد في التطبيق فقط | 🟢 | مسار B |
| **4** | توحيد انتهاء الأخبار + Cache Policy | 🟢 | مسار C |
| **Admin-SC** | حفظ البطاقات الخاصة عبر `invokeAdminRpc` | 🟢 | [`BUG-SPECIAL-CARDS-RPC.md`](./BUG-SPECIAL-CARDS-RPC.md) |
| **Admin-SC-List** | قائمة عبر RPC لا `.from()` | 🟡 | **بانتظار SQL** إن لم يُطبَّق — [`BUG-SPECIAL-CARDS-LIST.md`](./BUG-SPECIAL-CARDS-LIST.md) |
| **TREE-004** | عزل الأبناء + `parent_person_id` | 🟢 | **لا يُعاد** unlink اليدوي |
| **tree-import reuse** | إعادة استخدام الأب الموجود | 🟡 | **بانتظار SQL إنتاج** |
| **Repair** | تنظيف بيانات يدوي | 🟢 | مغلق للتخطيط |
| **Integrity** | قراءة فقط + تقرير | 🟢 أساس · 🟡 v2 SQL | |
| **Health** | Health Center | 🟢 | قراءة فقط (R-7) |
| **Tests** | Smoke + Regression | 🟢 أدوات | `verify:repair` · `verify:children-isolation` · `verify:tree-import-reuse` |
| **Admin-P1** | Redesign شِلّ | 🟢 | تكراري |
| **Admin-P2** | Delegates v2 | 🟡 | SQL أساس+فرض مُطبَّق — بانتظار قبول — [`DELEGATES-V2-PHASE2.md`](./DELEGATES-V2-PHASE2.md) |
| **PF-0** | Product Foundation | 🟡 | بوابة 2 — دستور حي + Truth Before Speed؛ بانتظار التجميد |
| **RX-v1** | Request Experience | ⚪ | بعد البوابتين 1+2 🟢 |
| **VE-v1** | Validation Engine | ⚪ | مرحلة مستقلة — [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) |
| **WE-v1** | Workflow Engine v1 | 🟡 | provisional على main — إغلاق عند مرحلته بعد VE |
| **DW-v1** | Delegate Workspace v1 | ⚪ | UX معتمد («ابدا») · كود بعد Workflow على السلّم |
| **Admin-P3** | Admin UX (مراقبة) | ⚪ | قائمة موسّعة أدناه |
| **FE-Align** | Family Engine Alignment | ⚪ | مواءمة + Single Write Rule |
| **Tree Engine v2** | مرحلة مستقلة | ⚪ | §6 |
| **Mobile-UX / iOS** | برنامج (ج) | ⬛ | بعد المراحل الكانونية |

---

## 13ب) إصلاحات لوحة الإدارة (مدرجة رسميًا)

بنود إصلاح مسارات الكتابة — مستقلة عن Redesign الكامل. تُعالَج بجذر المشكلة لا بالترقيع.

| # | البند | التشخيص المختصر | الحالة |
|---|--------|------------------|--------|
| **Admin-SC** | حفظ يتوقف عند RPC بلا Network | قفل `getSession` → `invokeAdminRpc` | 🟢 |
| **Admin-SC-List** | بعد حفظ 200 القائمة فارغة | RLS على `.from()` → يجب RPC list | 🟡 بانتظار SQL |
| **TREE-004** | تبديل الأب يعيد كتابة أبناء خاطئين | عزل جلسة + لا حل بالاسم | 🟢 |
| — | Logging + Error Handling حول كل `rpc` إداري | رسائل عربية + `ADMIN-RPC-001` | 🟢 مع Admin-SC |

**سياسة:** إصلاحات §13ب لا تغني عن مراحل الإدارة. **لا** يُقفز إلى UX الموبايل بدل برنامج (ب).

---

## 14) Event Bus

```
اعتماد (ابن / زوجة / صورة / دمج / تعديل)
    ↓
حدث واحد موثّق (RequestApplied / PersonLinked / …)
    ↓
الشجرة · الويب · الموبايل · الودجت · الكاش · الإشعارات · Audit
```

لا تحديث صامت في مسار واحد مع ترك الباقي.

---

## 15) Cache Policy

| السطح | متى Refresh | متى مسح الكاش | ملاحظات |
|--------|-------------|---------------|---------|
| **Web** | بعد اعتماد / تعديل شجرة / تغيير أخبار | عند فشل تطابق Version | |
| **Mobile** | فتح الشجرة / سحب للتحديث / بعد حدث | عند تغيير Data/Migration Version | |
| **Widget** | Timeline / App Group / بعد حدث أخبار | مسح عند انتهاء خبر أو فشل فلتر | إعادة بناء Timeline صراحة |

تُربط بـ Event Bus حتى لا تبقى أخبار منتهية في الكاش.

---

## 16) Error Codes

| رمز | معنى تقريبي |
|-----|----------------|
| `TREE-001` | ربط أب غامض / تطابق اسم متعدد |
| `TREE-002` | عقدة مكررة (قصير vs مسار) |
| `TREE-003` | `parent_person_id` مكسور بلا أب في الفهرس (v2: جذر الفرع ليس خطأ؛ التحذير = يحتاج ربط UUID) |
| `REQ-001` | قبول بلا تطبيق على الشجرة |
| `REQ-002` | فشل إنشاء/ربط بعد الاعتماد |
| `SPOUSE-001` | تعذر حل `person_id` للزوج/الزوجة |
| `NEWS-001` | خبر منتهٍ ما زال ظاهرًا |
| `NEWS-002` | فشل تجديد كاش/ودجت الأخبار |
| `MIG-001` | Migration فشلت / لم تُطبَّق على هذه النسخة |
| `REPAIR-001` | مرشّح إصلاح `parent_person_id` / نتيجة dry-run أو apply |
| `TREE-004` | عزل حالة الأبناء / اعتماد بلا `parent_person_id` فريد |
| `ADMIN-RPC-001` | استدعاء RPC إداري لم يُرسل أو توقّف بلا خطأ ظاهر |
| `DATE-001` | تاريخ أعمال غير قابل للتحليل / فارغ غير مسموح |
| `DATE-002` | قيمة هجرية في عمود timestamp ميلادي |
| `DATE-003` | تاريخ خارج النطاق الهجري/الميلادي المسموح |
| `DATE-004` | تاريخ ناقص أثناء الكتابة (تحقق حي) |

---

## 17) Health Center vs Diagnostics

| | **Health Center** | **Diagnostics** |
|--|-------------------|-----------------|
| الجمهور | المدير | الصيانة / المطوّر |
| الحالة | 🟢 أساس قراءة فقط | ⚪ لاحقًا ضمن موديولات الإدارة |
| يعرض | المشاكل (أبناء بلا أب، تكرار، طلبات عالقة، …) | Sync · Migration · Build · Widget · Backup · Scan · الإصدارات |
| قيد | **R-7:** لا auto-repair من الواجهة | |

---

## 18) Data Integrity Engine

محرك **دائم** (برنامج أ): أبناء بلا أب، أب بلا فرع، `parent_person_id`، `person_id` مكرر، cycles، زوجة بلا زوج، أبناء خارج الفرع، `approved` بلا أثر، أخبار منتهية ظاهرة. يغذي Health Center.

**للتخطيط:** مسار Integrity قراءة فقط = 🟢 مغلق كمفهوم. نشر v2 SQL = 🟡. أي إصلاح كتابة لاحق = dry-run + موافقة صريحة (R-3) → مسار Tree Engine v2 / Repair اليدوي فقط.

---

## 19) Compatibility Matrix

| المنصة | Affected | Verified | Not affected |
|--------|----------|----------|--------------|
| Web | | | |
| Admin | | | |
| iOS | | | |
| Android (مستقبلًا) | | | |
| Widget | | | |

---

## 20) KPI — متى يُعتبر الـ Patch ناجحًا؟

| المؤشر | الهدف |
|--------|--------|
| نجاح قبول الطلبات (تطبيق متحقَّق) | **100%** |
| روابط مكسورة | **0** |
| أبناء بلا أب | **0** |
| عقد مكررة في الموبايل (لنفس الشخص) | **0** |
| أخبار منتهية ظاهرة (تطبيق + ودجت) | **0** |
| أخطاء Widget بعد التحديث | **0** |
| طلبات `approved` بلا أثر في الشجرة | **0** |

لا يُغلق البند بـ «تم» دون تحقيق الأهداف (أو توثيق استثناء مؤقت). **🟢 يتطلب اختبار قبول**، وليس مجرد دمج كود.

---

## 21) Definition of Done

لا يُعتبر البند 🟢 إلا إذا:

- [ ] اجتاز **Smoke Tests**
- [ ] اجتاز **Regression Tests** (ومنها قواعد §8)
- [ ] لا أخطاء غير مفسَّرة في **Health Center**
- [ ] تم تحديث الوثائق ذات الصلة + **DATA-VERSIONS** إن لزم
- [ ] تم تحديث **Compatibility Matrix**
- [ ] لا **Error Codes** جديدة غير مفسَّرة
- [ ] إن وُجد SQL: طُبّق على الإنتاج (أو بيئة الهدف) وليس المستودع فقط

عبارة «خلصنا» بدون هذه القائمة = غير مقبول كإغلاق → أقصى ما يُسمح به 🟡.

---

## 22) المراحل الكانونية — برنامج (ب) + Validation + Workflow + دستور حي

**بدء الشرائح الجديدة:** عند قول المستخدم «ابدأ» أو تكليف صريح.  
**صريح:** «UX موبايل أولًا» أو القفز إلى Request Experience قبل البوابتين **مرفوض**.  
**السلّم:** بوابتان (Delegates · Foundation) → **Request Experience** → **Validation Engine** → **Workflow Engine** → **Delegate Workspace** → **Admin UX** → **Family Engine Alignment** → **iOS Experience Rebuild**.

Roadmap = ماذا نبني · Constitution = كيف نفكر / ما يُمنع — [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) (**دستور حي**).

### أساس الشِلّ — Admin Redesign 🟢 (تكراري · ليس مرحلة كانونية مستقلة)

[`ADMIN-REDESIGN-PHASE1.md`](./ADMIN-REDESIGN-PHASE1.md)

- لوحة تحكم (Hub) 🟢 · موديولات 🟢 · موديول واحد ظاهر 🟢
- شريط/عدّادات التدفق في الـ Hub تُغذَّى لاحقًا من **Workflow Engine** (لا منطق سير داخل الواجهة)
- موديولات: شجرة · أعضاء · مناسبات · ذكريات · مندوبون · صحة · سجل · طلبات · بطاقات · تصويت · إحصاءات · أدوات

### بوابة 1 — Delegates v2 🟡 ← **قبول فقط · ليس برمجة جديدة** ← **الخطوة الفورية الآن**

**المخرج:** اختبار قبول ناجح → 🟢 قبل تجميد الدستور.  
**نفّذ الآن:** [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md) (خانات عربية قابلة للتنفيذ)  
تفاصيل تقنية: [`DELEGATES-V2-PHASE2.md`](./DELEGATES-V2-PHASE2.md) · SQL `COPY-ME-delegates-v2.sql` + enforce

| بند قبول | الحالة |
|----------|--------|
| اختبار الصلاحيات | 🟡 بانتظار قبول — ACCEPTANCE §1 |
| اختبار الأدوار | 🟡 بانتظار قبول — ACCEPTANCE §2 |
| اختبار التفعيل والتعطيل | 🟡 بانتظار قبول — ACCEPTANCE §3 |
| اختبار سجل التدقيق | 🟡 بانتظار قبول — ACCEPTANCE §4 |
| اعتماد متعدد المراحل | ⚪ يُبنى لاحقًا فوق Workflow Engine — ليس بديلًا عنه |

### بوابة 2 — Product Foundation 🟡 = **دستور المنصة / دستور حي** (ليست مرحلة وثائق)

**المخرج:** تجميد الدستور الحي + **Truth Before Speed** — يمنع أي تطوير يخالفه إلا بقرار معماري.  
**ليس** «docs phase» — هذا **دستور المنصة**.

| وثيقة | المحتوى |
|--------|---------|
| [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md) | Human First · ثلاث لغات · محظورات · صياغة الأدوار |
| [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) | حالات · انتقالات · تعيين · Audit · إشعارات |
| [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) | فهرس النوايا — المرجع الوحيد لإضافة عملية |
| [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) | المبادئ · الطبقة · لغة المستخدم · Truth Before Speed · SSOT · Single Write Rule |

الوثائق الأربع **موجودة**؛ البوابة 🟡 حتى **التجميد الرسمي** بعد 🟢 Delegates.

### المرحلة — Request Experience ⚪

**بوابة:** البوابتان 🟢 فقط. **لا يُشترط** Workflow Engine 🟢. الواجهة تجمع النية/الحقائق — لا تبني الشجرة.

| بند | قاعدة |
|-----|--------|
| السؤال المحوري | **ماذا تريد أن تفعل؟** — ليس «اختر نوع البطاقة» |
| النوايا | من [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) فقط |
| نماذج قصيرة | النظام يبني الباقي — No Database Thinking + لغة المستخدم أولًا |
| التتبع | مسار حالات إنساني ([`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md)) |
| لا منطق اعتماد/تحقق | لا تعيين/انتقال/تحقق أعمال داخل الواجهة |

### المرحلة — Validation Engine ⚪ (مستقلة — ليست ملاحظة داخل Workflow)

**المرجع:** [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md)  
**الهدف:** قبل المندوب أو طفرة الشجرة — اكتشاف أخطاء/تعارضات وحماية جودة البيانات.  
**يتحقق ولا ينفّذ.**

| مسؤولية | المعنى |
|---------|--------|
| هوية | `person_id` لا الاسم وحده |
| تكرار | منع التكرارات |
| تعارض | تعارضات منطقية |
| هيكل | سلامة بنية الشجرة |
| اكتمال | حسب نوع الطلب |
| أسباب | رفض/تصحيح واضحة للمندوب |

### المرحلة — Workflow Engine v1 🟡 (عند الوصول في السلّم — ليست بوابة قبل RX)

> التحقق من سلامة المحرك وإزالة أي مسار اعتماد قديم متبقٍ.

أساس على `main` = **provisional** — لا توسيع إنتاج حتى وجود Validation Engine والمواءمة.  
المواصفة: [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) · التقرير: [`WORKFLOW-ENGINE-V1-REPORT.md`](./WORKFLOW-ENGINE-V1-REPORT.md)  
**SQL:** `supabase/sql/COPY-ME-workflow-engine-v1.sql`

| بند إغلاق المرحلة | المحتوى | الحالة |
|-------------------|---------|--------|
| انتقالات الحالات | التحقق من الانتقالات عبر المحرك | 🟡 |
| التعيين | التحقق من التعيين بالفرع/المندوب | 🟡 |
| السجل | التحقق من سجل الانتقال / Audit | 🟡 |
| الإشعارات | التحقق إن كانت ضمن الشريحة | 🟡 |
| مسار قديم | إزالة أي مسار اعتماد قديم متبقٍ | 🟡 |
| إعلان استقرار | إعلان المحرك مستقراً → 🟢 | 🟡 |
| أساس foundation | RPCs / deep links في المستودع | 🟡 provisional |

### المرحلة — Delegate Workspace v1 ⚪

**بوابة:** Validation + Workflow على مسار السلّم. الواجهة تستدعي المحركات فقط.  
**تصوّر UX:** [`DELEGATE-WORKSPACE-UX-v1.md`](./DELEGATE-WORKSPACE-UX-v1.md) — ✅ **معتمد من صاحب المنتج** (عبارة الاعتماد: **«ابدا»** / اعتماد المراجعة · 2026-08-09) · التنفيذ ❄️ **ما زال مجمَّدًا** حتى بوابات السلّم (Delegates → Foundation → RX → VE → Workflow) · requests-first · جلسة لكل طلب · الصفحة الحالية legacy مجمّدة تجميليًا · **لا كود DW الآن** · تنفيذ لاحق على مراحل (Inbox → Session → Next-queue) لا دفعة واحدة.  
**رحلة الطلب كاملة (قبل أي كود):** [`REQUEST-JOURNEY-REVIEW-v1.md`](./REQUEST-JOURNEY-REVIEW-v1.md) — الرئيسية ↔ إدارة ↔ Workflow ↔ مندوب ↔ أثر الشجرة كوحدة واحدة.

| بند | قاعدة |
|-----|--------|
| السؤال المحوري | **ماذا يجب أن أنجز الآن؟** |
| المنزل | الرئيسية ≡ طابور الطلبات — الشجرة/المناسبات/الذكريات = أدوات داخل الجلسة فقط · الحساب ثانوي |
| الطلب = جلسة عمل | الأدوات من سياق الطلب |
| الطابور أولًا | queue-first |
| لا أدوات إدارة | لا أدوات Admin داخل مساحة المندوب |
| لا حذف افتراضي | no default delete |
| التالي | انتقال تلقائي للطلب التالي بعد الإنجاز (زر احتياطي) |

### المرحلة — Admin UX (مراقبة التدفق) ⚪

الإدارة تراقب **التدفق** — لا تراجع الأشخاص كلغة أساسية.

| # | البند | ملاحظات |
|---|--------|---------|
| 1 | **لوحة مراقبة التدفق** | أين الطلبات · الاختناقات — بيانات من المحرك |
| 2 | طلبات متأخرة | Delayed requests |
| 3 | الحمل حسب المندوب | Load by delegate |
| 4 | **بحث إداري موحّد** | لاحقًا موحّد مع التطبيق (TD-4) |
| 5 | فلاتر محفوظة | Saved filters |
| 6 | عمليات جماعية (Bulk) | مع تأكيد + أثر عبر المحرك حيث ينطبق |
| 7 | تقارير | |
| 8 | سجل العمليات الأخيرة | بجانب Audit الكامل |
| 9 | تنبيهات الإدارة | Health حرجة · فشل RPC · SQL معلّق |
| 10 | مركز استيراد / تصدير · Backup · مهام/صيانة | |

### المرحلة — Family Engine Alignment ⚪

**ليست إعادة بناء** — الخدمات موجودة. **Single Write Rule** ساري.

| هدف | المعنى |
|-----|--------|
| توحيد نقطة الدخول | العمليات المعتمدة على طلب تدخل عبر Workflow + Validation |
| منع الكتابة المباشرة | لا مسارات UI→شجرة لعمليات الفهرس |
| خدمات واضحة | عقود استدعاء؛ الخدمات لا تعرف الواجهة |
| إزالة التكرار | Zero Duplicate Logic عبر الأدوار الثلاثة |

التفاصيل: [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md).

---

## 23) إعادة بناء تجربة iOS / UX الموبايل — برنامج (ج) ⬛

**البوابة:** البوابتان 🟢 ثم السلّم كاملًا حتى Family Engine Alignment — ثم فقط iOS.  
**لا يُبدأ الآن.**

| # | البند | ملاحظات |
|---|--------|---------|
| 1 | البحث في التطبيق | محرك حقيقي؛ لاحقًا موحّد مع الإدارة (TD-4) |
| 2 | صفحة العضو | بطاقة شخصية تفاعلية |
| 3 | الإشعارات | فئات + مركز إشعارات |
| 4 | شريط النشاط | Activity Feed |
| 5 | بطاقة الشخص الذكية | ضغط مطوّل: مشاركة، PDF، QR، … |
| 6 | خريطة الأقارب | المسار بين شخصين |
| 7 | السجل الزمني للعائلة | Timeline |
| 8 | ملفات الوسائط | لكل شخص |
| 9 | بطاقة العائلة | Family Card |
| 10 | تحسين التنقل | حديث، مفضّلات، breadcrumb |

مرجع الموبايل: `alzidan-family-mobile/docs/README.md`.

---

## 24) ما لا يُفعل الآن

- **iOS / UX الموبايل (§23)** قبل البوابتين والسلّم الكانوني — مرفوض كمسار أول
- القفز من قبول **Delegates** إلى **Request Experience** دون تجميد الدستور الحي
- اشتراط **Workflow Engine 🟢** قبل Request Experience (مخالف للسلّم المعتمد)
- توسيع أساس Workflow الـ provisional قبل مرحلة **Validation Engine** والمواءمة
- تنفيذ مراحل السلّم خارج الترتيب أو قبل البوابتين 🟢 — بما فيها **كود Delegate Workspace** رغم اعتماد UX («ابدا»)
- إنشاء شاشة/نموذج/ميزة بلا تحديد طبقة — بما فيها Validation Engine (خرق قاعدة أ)
- بناء واجهة فوق قاعدة البيانات بدل لغة المستخدم أولًا ثم المحرك (خرق قاعدة ب)
- تجاهل **Truth Before Speed** أو **Single Write Rule**
- وضع منطق حالات/تحقق/اعتماد داخل Admin أو Delegate أو Request UI
- محرك حالات منفصل لكل نوع طلب — الأنواع تشترك في نفس العُقد
- كتابة مباشرة من الواجهة إلى Family Engine/Memory/Cards لعمليات [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md)
- اعتبار Validation Engine «ملاحظة داخل Workflow» أو مجرد دوال مساعدة
- إعادة بناء الشجرة/المناسبات بدل **Family Engine Alignment**
- فتح مسار إصلاح بيانات تلقائي جديد قبل موافقة + dry-run (Integrity/Health = قراءة فقط)
- إحصائيات تجميلية فوق بيانات غير موثوقة
- «إصلاح بيانات صالح» كحل أول لمشكلة العرض 36
- كسر أي ADR مقفل دون وثيقة قرار جديدة
- `new Date(...)` لتواريخ أعمال خارج DateEngine (ADR-009 / R-5)
- تخزين سنة هجرية في أعمدة timestamp ميلادية
- اعتبار ملف SQL في المستودع = 🟢 دون تطبيق واختبار قبول
- اعتبار البوابتين 🟢 قبل القبول/التجميد
- تجاهل **قاعدة بوابة الميزة** (§1ب) أو الدستور الحي عند تصميم ميزة جديدة
- مصطلحات محظورة في الواجهة (JSON · Parent · tree_card كعنوان · …) — [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md)
- تكرار منطق التحقق في صفحات متعددة (خرق Zero Duplicate Logic)
- أي تطوير خارج المسار دون سؤال «في أي مرحلة/طبقة؟»

---

## 25) جاهزية البدء / الخطوات التالية

| الحالة | المحتوى |
|--------|---------|
| 🟢 | Patch 0–4 · Mobile 36 · News · Repair يدوي · Health · Admin P1 شِلّ · DateEngine أساس · TREE-004 |
| 🟡 | **البوابتان** — (1) Delegates قبول ← [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md) · (2) تجميد الدستور الحي + Truth Before Speed — **ما زالتا 🟡** · DW UX معتمد («ابدا») بلا كود · Workflow provisional · tree-import · Integrity v2 · (Admin-SC-List إن لزم) |
| ⚪ | Multi-stage فوق Workflow — ليس حاجزًا لإغلاق صلاحيات v2 |
| ⚪ | Request Experience · Validation Engine · (إغلاق Workflow عند مرحلته) · Delegate Workspace · Admin UX · Family Engine Alignment · Tree Engine v2 — **بعد البوابتين 🟢** |
| ⬛ | iOS Experience Rebuild — برنامج (ج) |

المرجع طويل الأمد: **الدستور الحي** · قاعدة الطبقة · لغة المستخدم · Truth Before Speed · Validation Engine · Single Write Rule · المعمارية · ADR · Versioning · Rollback · KPI · DoD · قواعد الانحدار · البرامج الثلاثة.

---

**الخطوة التالية الفورية (لا قفز) — بعد «ابدا» 2026-08-09:**  
(0) UX مساحة المندوب ✅ معتمد — **لا يفتح كود DW** · الصفحة الحالية legacy — راجع [`REQUEST-JOURNEY-REVIEW-v1.md`](./REQUEST-JOURNEY-REVIEW-v1.md)  
(1) **بوابة 1 الآن** — كود موثّق ✅ · تبقّى تشغيل 5 دقائق في [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md) → 🟢  
(2) **بوابة 2** — جاهزة كمسودة [`PRODUCT-FOUNDATION-FREEZE-DRAFT.md`](./PRODUCT-FOUNDATION-FREEZE-DRAFT.md) · تُنفَّذ فور 🟢 بوابة 1  
(3) **بعدها السلّم:** Request Experience → Validation Engine → Workflow Engine (سلامة + إزالة مسار قديم + إعلان استقرار) → Delegate Workspace (كود) → Admin UX → Family Engine Alignment → iOS Experience Rebuild  

لا توسيع Workflow provisional · لا كود Delegate Workspace / iOS الآن · **لا تطوير لواجهة المندوب الآن**.

**تسليم SQL لـ Supabase:** افتح ملف `.sql` → Select All → Copy. لا تنسخ من الشات (أسوار Markdown تكسر اللصق). قاعدة Cursor: `.cursor/rules/supabase-sql-delivery.mdc`.
