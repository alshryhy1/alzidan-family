# Delegate Workspace UX v1 — تصوّر واجهة مساحة عمل المندوب

**الحالة:** ✅ **معتمد من صاحب المنتج** — عبارة الاعتماد: **«ابدا»** (= اعتماد المراجعة + بدء المسار الصحيح) · 2026-08-09  
**التنفيذ:** ❄️ **مجمَّد صراحةً** — اعتماد UX **لا يفتح كود** مساحة المندوب · الشحن ينتظر بوابات السلّم: Request Experience → Validation Engine → Workflow Engine  
**التاريخ:** 2026-08-09  
**النوع:** تصميم / مواصفة تجربة معتمدة — **ليست** شريحة تنفيذ · **ليست** تحسينًا للصفحة الحالية  
**المراجع:**  
[`ENGINEERING-ROADMAP.md`](./ENGINEERING-ROADMAP.md) · [`PLATFORM-PRINCIPLES.md`](./PLATFORM-PRINCIPLES.md) · [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md) · [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) · [`WORKFLOW-SPECIFICATION-v1.md`](./WORKFLOW-SPECIFICATION-v1.md) · [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) · [`ADR.md`](./ADR.md)

---

## 0) حالة الاعتماد · تجميد التنفيذ

| بند | القرار |
|-----|--------|
| **دور الوثيقة** | تصوّر UX كامل **معتمد** — مرجع تصميم لمساحة المندوب لاحقًا · ليس إذن شحن |
| **اعتماد صاحب المنتج** | ✅ **معتمد** — عبارة الاعتماد: **«ابدا»** / اعتماد المراجعة (2026-08-09) · الأسئلة الخمسة (§14) كانت ✅ قبل الاعتماد |
| **ما لا يعنيه الاعتماد** | **ليس** إذن بدء كود DW · ليس Enhancement للصفحة الحالية · ليس تجاوز سلّم RX → VE → Workflow |
| **تجميد التنفيذ (قاعدة قفل)** | **ممنوع** أي كود منتج (JS/CSS/HTML/SQL واجهة) لمساحة المندوب الجديدة حتى تُغلق بوابات السلّم أدناه · الصفحة الحالية legacy مجمّدة تجميليًا |
| **الصفحة الحالية** | **legacy** — مجمّدة تجميليًا (§8) · التصميم هنا **لا يشبه** لوحة الشجرة/الإدارة الحالية |

### 0.1 موقع هذه الوثيقة في السلّم (مهم)

```
Delegates v2 → Product Foundation → Request Experience
        → Validation Engine → Workflow Engine
        → Delegate Workspace  ← هذه المرحلة (تنفيذ لاحقًا — UX معتمد الآن)
        → Admin UX → …
```

| حقيقة | المعنى |
|--------|--------|
| **هذه الوثيقة الآن** | ✅ **معتمدة من صاحب المنتج** («ابدا») — مرجع UX ثابت |
| **التنفيذ** | ❄️ مجمَّد — ينتظر: بوابة Delegates 🟢 · تجميد Foundation · ثم RX → VE → Workflow على السلّم |
| **الصفحة الحالية** | **legacy** — تُجمَّد تجميليًا؛ لا تُطوَّر تدريجيًا نحو هذا التصوّر |

> **قاعدة قفل صارمة:** اعتماد المراجعة («ابدا») **لا يفتح** كود مساحة المندوب. أي طلب «ابدأ كود DW الآن» يُرفض. الخطوة التنفيذية الحالية = إغلاق بوابات السلّم السابقة — انظر [`ENGINEERING-ROADMAP.md`](./ENGINEERING-ROADMAP.md) و[`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md).

---

## 1) الغرض والتموضع المعماري

### 1.1 ماذا هي مساحة عمل المندوب؟

واجهة تنفيذ للمندوب تجيب عن سؤال واحد:

> **ماذا يجب أن أنجز الآن؟**

المندوب يدخل على **طلبات تحتاج مراجعتك**، يفتح طلبًا كـ**جلسة عمل مستقلة**، يستخدم الأدوات التي يحتاجها **داخل الجلسة فقط**، يُصدر قرارًا (قبول / طلب تعديل / رفض)، ثم ينتقل مباشرة إلى **الطلب التالي**.

### 1.2 ماذا ليست؟

| ليست | لأن |
|------|-----|
| لوحة إدارة شجرة | الشجرة أداة داعمة داخل الجلسة — ليست الصفحة الرئيسية |
| Admin console | لا مراقبة تدفق، لا bulk، لا SQL، لا Health Center |
| شاشة «اختر شخصًا ثم عدّل» | نقطة الدخول = **الطلب / النية** لا الشخص |
| تطوّر تدريجي لـ `alzidan-tree.html` + `delegate.js` | إعادة تصميم كاملة (Redesign) — الصفحة الحالية legacy |

### 1.3 الطبقة في المعمارية

```
المستخدم (مندوب)
        │
        ▼
 Delegate Workspace     ← هذه الطبقة (عرض + أفعال بشرية فقط)
        │ يستدعي
        ▼
 Workflow Engine        ← حالة الطلب · التعيين · الانتقال · Audit · إشعار
        │
        ▼
 Validation Engine      ← نتيجة التحقق · أسباب · تعارضات (يُعرض ولا يُعاد اختراعه)
        │
        ▼
 Family Engine / …      ← أثر البيانات بعد approved → applied فقط (Single Write Rule)
```

- الواجهة **بلا منطق أعمال** (حالات · تحقق · صلاحيات مصدرها المحركات).
- الصلاحيات من **Delegates v2**؛ الواجهة تعكسها ولا تكون مصدرها الوحيد.

---

## 2) العلاقة بلغة المنتج · تجربة الطلب · التحقق · السير

| المصدر | ماذا تستهلك مساحة المندوب منه؟ | ماذا لا تفعله؟ |
|--------|-------------------------------|----------------|
| **Product Language** | كل العناوين والأزرار والحالات بلغة المستخدم (عمود المندوب) | لا `tree_card` · لا `request_id` كعنوان · لا Parent/JSON |
| **Request Catalog** | نوع النية المعروضة (أضف فردًا · صحح بيانات · …) | لا نوايا خارج الفهرس |
| **Request Experience** | يقدّم الزائر النية والحقائق؛ المندوب يراها كجلسة | لا نماذج «بدء نية» للمندوب كمسار رئيسي |
| **Validation Engine** | يعرض نتيجة التحقق وأسباب التعارض/النقص **قبل** القرار | لا يعيد كتابة قواعد التحقق في JS الصفحة |
| **Workflow Engine** | يقرأ الطابور والحالة والمالك؛ يستدعي الانتقالات فقط | لا يخزّن حالة موازية · لا يكتب Audit يدويًا |
| **Family Engine** | أدوات قراءة/سياق (شخص · شجرة) داخل الجلسة عند الحاجة | لا كتابة شجرة مباشرة من UI (Single Write Rule) |

### 2.1 سلسلة المنتج (من الزائر إلى المندوب)

```
زائر: ماذا تريد أن تفعل؟     (Request Experience)
        ↓
Validation: هل الحقائق سليمة؟ (Validation Engine — يتحقق ولا ينفّذ)
        ↓
Workflow: تعيين · حالة · إشعار
        ↓
مندوب: ماذا يجب أن أنجز الآن؟ (Delegate Workspace — هذه الوثيقة)
        ↓
قرار → انتقال Workflow → تطبيق خدمة عند الاعتماد المتحقَّق
```

**Truth Before Speed:** المندوب يرى تحذيرات/تعارضات التحقق قبل القبول؛ السرعة لا تتجاوز جودة الشجرة.

---

## 3) هندسة المعلومات والتدفقات الأساسية

### 3.1 الهيكل (IA)

```
دخول المندوب (جلسة/هوية — خارج نطاق إعادة تصميم العمل)
        │
        ▼
┌─────────────────────────────┐
│  الرئيسية = صندوق الطلبات   │  ← الشاشة الافتراضية بعد الدخول
│  «طلبات تحتاج مراجعتك»      │
└──────────────┬──────────────┘
               │ فتح طلب
               ▼
┌─────────────────────────────┐
│  جلسة عمل الطلب             │  ← شاشة مستقلة لكل طلب
│  ملخص النية · الحقائق ·     │
│  نتيجة التحقق · أدوات سياقية│
│  قرارات: قبول / تعديل / رفض │
└──────────────┬──────────────┘
               │ بعد القرار
               ▼
        الطلب التالي في الطابور
        (أو فراغ إن انتهى الطابور)
```

**موديولات داعمة (ليست المنزل):**

| موديول | الدور في v1 |
|--------|-------------|
| الطلبات | **المنزل الوحيد بعد الدخول** / الطابور |
| جلسة الطلب | العمل الفعلي |
| الشجرة (أداة) | تُفتح **من داخل الجلسة فقط** عند الحاجة — **ممنوع** تبويب يومي منافس للطابور |
| المناسبات / الذكريات | أدوات سياقية حسب نوع الطلب من الفهرس — **داخل الجلسة فقط** |
| الحساب | خروج · فرع · حالة الحساب — مدخل ثانوي واحد في الرأس، لا قسم عمل |

> في هذا التصوّر: **الرئيسية ≡ صندوق الطلبات**. لا شريط تبويب (شجرة · مناسبات · ذكريات) بجانب الطابور. أي إشارة قديمة في الخارطة لموديولات متوازية تُقرأ عبر هذه القاعدة — لا «لوحة إدارة فرع».

### 3.2 التدفق الأساسي (Happy path)

1. **خلال ≤3 ثوانٍ بعد الدخول** يرى المندوب بلا بحث ولا اختيار شخص:  
   - عنوان «طلبات تحتاج مراجعتك»  
   - **عدد الطلبات** ظاهر فورًا (مثال: «3 طلبات بانتظارك»)  
   - **أول طلب** (الأقدم/الأولوية) مميّز في أعلى القائمة  
   - زر أساسي **«ابدأ»** (أو «ابدأ المراجعة») يفتح ذلك الطلب مباشرة  
2. فتح الطلب (من «ابدأ» أو من البطاقة) → Workflow: `assigned → in_review` (إن لزم).  
3. يراجع في **صفحة واحدة داخل الجلسة**: النية · من أرسل · الحقائق · نتيجة Validation · مقارنة الحالي vs المقترح عند التعديل · تحذير التعارض إن وُجد.  
4. يفتح أدوات السياق **فقط عند الحاجة** (شخص · موقع عائلي · وسائط) — **ليست** خطوة إلزامية · **ممنوع** القفز لتبويب شجرة/مناسبات/ذكريات خارج الجلسة.  
5. يقرّر من شريط أسفل ثابت دون مغادرة الجلسة: **قبول** · **نحتاج معلومة إضافية** (أسهل للمشاكل الصغيرة) · **لم يُقبل** (ثانوي/صارم).  
6. المحرك ينفّذ الانتقال + Audit + إشعار.  
7. تأكيد قصير جدًا → **انتقال تلقائي إلى الطلب التالي** مع عنوان واضح «الطلب التالي» (الزر احتياطي إن تعذّر التلقائي) — **لا** عودة لقائمة رئيسية/شجرة بين كل قرار.

**مسار الحد الأدنى (طلب نظيف قابل للقبول):** دخول → يرى العدد + «ابدأ» → قبول → التالي.  
لا خطوات وسطى إلزامية (لا بحث · لا اختيار شخص · لا فتح شجرة · لا لوحة إحصاء) قبل القرار.  
**إن احتاج المندوب للبحث أو اختيار شخص قبل أول عمل → فشل التصميم.**

### 3.3 مبدأ الجلسة

- كل طلب = **جلسة عمل مستقلة** (سياق · أدوات · قرار).  
- لا «مساحة عمل عامة» تخلط عدة طلبات مفتوحة في لوحة واحدة معقّدة.  
- العودة للطابور متاحة دائمًا؛ الافتراضي بعد الإنجاز = **التالي تلقائيًا** — **ممنوع** الهبوط على الشجرة أو لوحة إدارة بين الطلبات.

---

## 4) الشاشات والحالات

### 4.1 شاشة الطابور (Inbox)

| عنصر | محتوى |
|------|--------|
| العنوان | طلبات تحتاج مراجعتك |
| **العدد (إلزامي)** | رقم ظاهر فورًا تحت العنوان أو بجانبه: «N طلبات بانتظارك» (و«طلب واحد» عند 1) — يُدرَك في نظرة واحدة مع العنوان |
| السؤال الضمني | ماذا يجب أن أنجز الآن؟ |
| **زر ابدأ (إلزامي عند وجود طابور)** | زر أساسي واحد فوق القائمة أو ملتصق بأعلى البطاقة الأولى: **«ابدأ»** — يفتح **أول طلب في الترتيب** دون تصفّح أو بحث |
| أول طلب | البطاقة الأولى مميّزة بصريًا (أقدم/أولوية المحرك) — هي هدف «ابدأ» |
| بطاقة طلب | نية بشرية + اسم/موضوع + من أرسل + حالة مفهومة + وقت الانتظار |
| ترتيب | من المحرك؛ الافتراضي الأقدم أولًا |
| تفاعل | «ابدأ» → جلسة أول طلب · أو نقرة على أي بطاقة → جلسة ذلك الطلب |

**حالات الطابور:**

| الحالة | العرض |
|--------|--------|
| تحميل | هيكل خفيف / هيكل عظمي — بلا جداول إدارية |
| قائمة غير فارغة | **العدد + زر ابدأ + بطاقات عمودية** (mobile-first) — أول بطاقتين على الأقل ظاهرتان دون تمرير على شاشة هاتف شائعة |
| فارغ | «لا طلبات بانتظارك الآن» + جملة طمأنة قصيرة — **بلا** زر ابدأ · **بلا** توجيه لإدارة الشجرة |
| خطأ قراءة | رسالة عربية من المحرك — بلا أخطاء Supabase خام |

**ما لا يظهر في الطابور:** أعمدة تقنية · فلاتر bulk · إحصائيات كثيفة · شجرة · محرّر أشخاص · حقل بحث كخطوة أولى · اختيار شخص.

### 4.2 شاشة جلسة الطلب (Work Session)

جلسة عمل **مكتفية ذاتيًا** — كل ما يلزم للقرار داخل هذه الشاشة. لا انتقال إجباري بين شجرة / مناسبات / ذكريات لإنهاء الطلب.

هيكل عمودي بسيط (عمود واحد · شريط قرار ثابت لا يختفي بالتمرير):

1. **رأس الجلسة:** النية (مثال: أضف فردًا للعائلة) · الحالة بلغة المستخدم · رجوع للطابور.  
2. **من أرسل / متى** — سطر واحد.  
3. **الحقائق المقدَّمة** — ما أدخله الزائر بلغة بشرية.  
4. **نتيجة التحقق** (من Validation Engine) — قبول للمسار / تحذيرات / نواقص / **تعارضات بأسباب واضحة قبل أي قبول**.  
5. **مقارنة المسجّل الآن vs المقترح** (إن انطبق — خاصة تصحيح البيانات): تمييز بصري واضح لما يتغيّر فقط (§4.3).  
6. **أدوات السياق** (أزرار ثانوية تظهر حسب الحاجة فقط — طبقة/لوحة داخل الجلسة تُغلق وتعود لنفس الطلب).  
7. **شريط القرار الثابت أسفل الشاشة (موبايل) — ترتيب يمنع الخطأ:**  
   - **قبول الطلب** (أساسي عند صلاحية المسار)  
   - **نحتاج معلومة إضافية** (مسار التصحيح الصغير — أسهل وأوضح من الرفض؛ زر واضح غير مخفي)  
   - **لم يُقبل** (ثانوي/أقل بروزًا — للرفض الصريح فقط)

### 4.3 مقارنة الحالي vs المقترح · منع الأخطاء (Compare & Conflict)

| متى | العرض |
|-----|--------|
| تصحيح بيانات / تعديل حقائق | قائمتان متتابعتان على الموبايل (لا عمودين ضيقينين يجبران التكبير): **المسجّل الآن** · **المقترح في الطلب** — كل فرق مُعلَّم |
| إضافة فرد جديد | لا مقارنة حقول قديمة؛ عرض الحقائق + موقع مقترح في العائلة إن وُجد من التحقق |
| مناسبة / ذكرى / بطاقة | ملخص المحتوى المقترح؛ مقارنة فقط إن وُجدت نسخة سابقة ذات صلة |
| **تعارض Validation** | شريط/كتلة تحذير واضحة **قبل** أزرار القرار: السبب بلغة بشرية · تعطيل القبول إن كان التعارض حاجزًا |

قواعد العرض: أسماء حقول بشرية («تاريخ الميلاد» لا `birth_date_g`). التمييز البصري للتغيير فقط — بلا JSON.  
**قاعدة سهولة التصحيح:** إن كانت المشكلة صغيرة (حقل ناقص · توضيح · تاريخ غير واضح) فالمسار المقصود والأقل احتكاكًا = **نحتاج معلومة إضافية** لا **لم يُقبل**. الرفض للصريح فقط (لا تطابق · خارج النطاق · مخالفة واضحة).

### 4.4 نتائج القرار (Outcomes)

| قرار المندوب (واجهة) | انتقال Workflow النموذجي | ما يُطلب من المندوب |
|----------------------|---------------------------|---------------------|
| **قبول** | `in_review → approved` ثم المحرك → `applied` → `done` | تأكيد قصير؛ إن وُجدت تحذيرات Validation غير حاجزة تُعرض قبل التأكيد |
| **نحتاج معلومة إضافية** | `in_review → needs_changes` | **سبب مقروء إلزامي** (يصل للزائر بلغة المنتج) |
| **لم يُقبل** | `in_review → rejected` | **سبب مقروء إلزامي** |

بعد نجاح الانتقال:

1. رسالة قصيرة («تم قبول الطلب» / «أُرسل طلب التعديل» / «لم يُقبل الطلب») — ثوانٍ فقط، بلا شاشة وسيطة معقّدة.  
2. **الافتراضي:** فتح **جلسة الطلب التالي** مباشرة (نفس إيقاع العمل) — ليس الهبوط على قائمة الطابور كصفحة منزل بين كل قرار. عنوان/زر واضح: **«الطلب التالي»** (ظاهر كاحتياطي إن تعذّر التلقائي أو أراد المندوب التأكيد).  
3. إن لم يبقَ طلب: شاشة الفراغ (§4.1) — بلا توجيه لإدارة الشجرة.  
4. **إيقاع المهام:** بعد كل قرار يبقى المندوب في مسار «مهمة → التالية» — **ممنوع** إرجاعه لقائمة رئيسية / شجرة / لوحة إدارة بعد كل قبول.

**ADR-006:** زر القبول لا يعني «نجاحًا نهائيًا» في الواجهة إلا بعد مسار التطبيق المتحقَّق الذي يوفّره المحرك؛ الواجهة لا تكتب الشجرة بنفسها.

### 4.5 حالات الجلسة الإضافية

| الحالة | السلوك |
|--------|--------|
| الطلب لم يعد ملك المندوب | رسالة + عودة للطابور |
| انتقال مرفوض بالصلاحية | رسالة Delegates عربية — بلا إعادة محاولة صامتة |
| Validation حاجز قبل القبول | تعطيل القبول أو مسار «لا يمكن القبول حتى تُصحَّح الحقائق» مع الأسباب |
| انقطاع شبكة | حفظ محلي غير مطلوب في v1؛ إعادة محاولة واضحة |

---

## 5) مبادئ التخطيط Mobile-first — وما لا يُعرض

> معظم المندوبين على الجوال. إن احتاجت الواجهة تكبيرًا (zoom) أو تمريرًا لا ينتهي لإدراك العمل → يُعاد التفكير في الكثافة قبل أي كود.

### 5.1 يجب

- عمود واحد · إبهامي · قرارات في أسفل الشاشة **دائمًا ظاهرة** (شريط ثابت).  
- بطاقات طلب قصيرة قابلة للمسح البصري في ثوانٍ.  
- **فوق الثنية على هاتف شائع (~≤390px عرضًا):** العنوان + العدد + زر **ابدأ** + أول طلب على الأقل — بلا تمرير لفهم «ماذا أفعل؟».  
- جلسة طلب = صفحة كاملة (أو طبقة كاملة) لا لوحة متعددة الأعمدة ولا مقارنة جنبًا-إلى-جنب تجبر التكبير.  
- **ممنوع** التمرير الأفقي للعمل اليومي · **ممنوع** الاعتماد على pinch-zoom لقراءة الحقائق أو المقارنة.  
- قائمة الطابور: بطاقات قصيرة متتالية — ليست جدولًا لا نهائيًا بلا نهاية واضحة؛ إن طال الطابور يبقى المسح سريعًا (نية + موضوع + انتظار) لا تفاصيل كثيفة في البطاقة.  
- نصوص عربية RTL · أفعال قصيرة.  
- سرعة الإدراك: النية أولًا، التفاصيل التقنية أبدًا في الواجهة الأولى.

### 5.2 ممنوع أن تشعر كـ Admin console

لا تُعرض افتراضيًا:

| ممنوع في مساحة المندوب | بديل |
|------------------------|------|
| جداول عريضة متعددة الأعمدة | بطاقات |
| شريط جانبي بموديولات كثيرة | طابور → جلسة |
| لوحة إحصائيات / اختناقات / حمل مندوبين | Admin UX لاحقًا |
| SQL · Health · Integrity · Bulk | خارج الطبقة |
| الشجرة كصفحة منزل | أداة داخل الجلسة |
| مصطلحات DB / Admin | لغة المنتج |
| حذف كإجراء افتراضي ظاهر | no default delete (الخارطة) |
| اختيار شخص كخطوة أولى للعمل اليومي | فتح طلب أولًا |

### 5.3 كثافة المعلومات

الشاشة الأولى بعد الدخول: **قائمة طلبات فقط** (+ هوية مختصرة في الرأس).  
كل ما عدا ذلك يُكشف عند الحاجة داخل الجلسة.

---

## 6) أمثلة لغة (Human labels)

المصدر الحاكم: [`PRODUCT-LANGUAGE.md`](./PRODUCT-LANGUAGE.md). أمثلة تشغيلية لمساحة المندوب:

### 6.1 عناوين وأزرار

| سياق | نص معتمد | تجنّب |
|------|----------|--------|
| المنزل | طلبات تحتاج مراجعتك | Queue · Inbox · Pending rows |
| العدد | N طلبات بانتظارك / طلب واحد بانتظارك | Pending count · Open items |
| السؤال | ماذا يجب أن أنجز الآن؟ | إدارة الشجرة · اختر الأب |
| بدء العمل | **ابدأ** (يفتح أول طلب) | اختر شخصًا · بحث · Open queue |
| فتح بطاقة | مراجعة الطلب | Open record |
| قبول | قبول الطلب | Approve / Apply row |
| تعديل (أسهل للصغير) | نحتاج معلومة إضافية | needs_changes · Request changes (إنجليزي في UI) |
| رفض (ثانوي) | لم يُقبل | Reject |
| التالي | الطلب التالي | Next ticket · Back to dashboard |
| مقارنة | المسجّل الآن · المقترح | Current payload · Diff JSON |
| تعارض | لا يمكن القبول حتى… (+ سبب) | Conflict code · TREE-001 كعنوان |
| أداة | عرض الشخص · عرض موقعه في العائلة | Open node · Parent path |

### 6.2 أول شاشة طابور (مثال إدراك ≤3 ثوانٍ)

> **طلبات تحتاج مراجعتك**  
> 3 طلبات بانتظارك  
> 〔 **ابدأ** 〕  
>  
> **أضف فردًا للعائلة** ← أول طلب  
> محمد بن حبيب — أرسله خالد  
> وصل للمندوب · منذ ساعتين

### 6.3 رأس جلسة (مثال)

> **صحّح بيانات شخص**  
> تحت المراجعة  
> المقدَّم: سارة · فرع مزيد  

### 6.4 أسباب (للزائر عبر المحرك)

| نوع | مثال صياغة |
|-----|-------------|
| تعديل | نحتاج تاريخ الميلاد بالهجري أو الميلادي بوضوح. |
| رفض | لم نتمكن من مطابقة الشخص في العائلة بالمعلومات المتاحة. |

لا تُعرض رموز مثل `TREE-001` كعنوان للمندوب اليومي؛ إن لزم التشخيص فطبقة ثانوية لاحقة — ليس v1 الأساسي.

---

## 7) أدوات تظهر فقط داخل جلسة الطلب

الأدوات **مشروطة بنوع الطلب** من [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) وبنتيجة التحقق — ليست شريط أدوات دائمًا.

| أداة | متى تظهر | ماذا تفعل (عرض) |
|------|----------|------------------|
| عرض الشخص | عند وجود شخص مرتبط / مرشّح | بطاقة حقائق للقراءة |
| عرض موقعه في العائلة | عند الحاجة لتأكيد النسب/الموقع | قراءة شجرة/سياق — ليست محرّر إدارة |
| مقارنة المسجّل vs المقترح | خاصة تصحيح البيانات | §4.3 |
| وسائط مرفقة | ذكرى / مناسبة / بطاقة فيها ملفات | معاينة |
| نتيجة التحقق التفصيلية | دائمًا إن وُجدت مخرجات Validation | أسباب واضحة |
| قبول / طلب معلومة / رفض | دائمًا في الجلسة النشطة للمالك | §4.4 |

**قواعد الأدوات:**

1. لا تظهر أداة شجرة كاملة كـ«إدارة فرع» داخل الجلسة.  
2. أي كتابة على البيانات العائلية تمر Workflow + Validation فقط.  
3. لا حذف افتراضي ظاهر.  
4. الصلاحية إن منعت الأداة: إخفاء أو تعطيل + رسالة عربية من Delegates — الفرض في المحرك.

---

## 8) الصفحة الحالية = Legacy — تجميد التحسين التجميلي

| البند | القرار |
|-------|--------|
| الصفحة الحالية (`pages/alzidan-tree.html` + `assets/js/delegate.js` + لوحات العائلة/المناسبات) | **legacy** — مبنية على **إدارة شجرة / اختيار شخص** |
| التحسين التجميلي عليها | **موقوف** — لا ألوان · لا إعادة ترتيب بطاقات · لا «تحسين UX» تدريجي نحو هذا التصوّر |
| الإصلاحات الحرجة فقط | أمان · كسر دخول · انحدار بيانات — خارج نطاق «تجميل» |
| الاستبدال | **إعادة تصميم كاملة** وفق هذه الوثيقة عند مرحلة Delegate Workspace على السلّم |
| لا Enhancement | ممنوع مسار «نحوّل الداشبورد الحالي لطابور» كحل وسط طويل الأمد |

نص الخارطة الحالي للمندوب («إدارة العائلة: اختر شخصًا…») **يناقض** السؤال المحوري الجديد — يُستبدل لا يُرقَّع.

---

## 9) جرد: ما يُحفظ محتملًا vs ما يُستبعد

اطّلاع تشغيلي على الوضع الحالي (ليس للتمديد):

### 9.1 مرشّح للإنقاذ لاحقًا (مفهوم / نمط — لا نسخ التصميم)

| عنصر | سبب الإنقاذ المحتمل |
|------|---------------------|
| تدفق دخول المندوب + رسائل الرفض العربية (معطّل / بلا صلاحية / سر غير مطابق) | نمط لغة وصلاحيات Delegates v2 — يُعاد توظيفه لا نسخه البصري |
| `confirmTypedText` / تأكيد كتابة لنص حسّاس | نمط أمان لقرارات حساسة إن لزم لاحقًا |
| معاينة وسائط المندوب (أنماط CSS خفيفة) | قد تُستلهم للمعاينة داخل الجلسة |
| وحدات قراءة الشخص / الهوية الكانونية (`canonical-person` وغيرها) | كـ**خدمات قراءة** تُستدعى من الجلسة — لا كواجهة منزل |
| جلسات المندوب / فرض الصلاحية في RPC | طبقة بيانات — تبقى؛ الواجهة الجديدة تستهلكها |

### 9.2 يُستبعد من المنزل والتجربة الأساسية

| عنصر | سبب الاستبعاد |
|------|----------------|
| الشجرة العامة + «إضافة أبناء» كنقطة دخول | tree-first |
| تلميح الداشبورد «اختر شخصًا…» | يناقض requests-first |
| `family-management-panel` كسطح العمل اليومي | محرّر شجرة إداري الشعور |
| بطاقات المناسبات/الذاكرة كأقسام منزل موازية للطابور | تُحوَّل لأدوات جلسة حسب النوع |
| كتابة مباشرة على `tree_children` من واجهة المندوب لمسار الفهرس | يخالف Single Write Rule عند اكتمال المحركات |
| مصطلحات/حقول تفكير قاعدة بيانات ظاهرة للمندوب | Product Language · No Database Thinking |
| تخطيط متعدد البطاقات/الأقسام كـ console | يخالف البساطة وmobile-first |

**الخلاصة:** لا يُعاد استخدام تصميم الصفحة الحالية كأساس. الإنقاذ = أنماط صلاحيات/دخول وخدمات قراءة — لا IA ولا هيكل الشاشات.

---

## 10) خارج نطاق هذه الوثيقة / ما ينتظر مراحل لاحقة

| خارج النطاق الآن | أين يُعالَج |
|------------------|------------|
| تنفيذ UI/JS/CSS لمساحة المندوب الجديدة | مرحلة Delegate Workspace بعد Workflow على السلّم |
| بناء Request Experience | مرحلة RX |
| قواعد Validation التفصيلية لكل نوع | [`VALIDATION-ENGINE.md`](./VALIDATION-ENGINE.md) + مرحلة VE |
| إغلاق Workflow الإنتاجي · إزالة مسار اعتماد قديم | مرحلة Workflow Engine |
| Admin UX (اختناقات · حمل · bulk) | بعد Delegate Workspace |
| Multi-stage approval التفصيلي | فوق Workflow لاحقًا |
| إعادة بناء iOS | بعد المراحل الكانونية |
| تصميم بصري نهائي (هوية ألوان/خطوط نهائية) | عند التنفيذ — مع الالتزام بمبادئ البساطة هنا |
| Deep link تفصيلي للقوالب | المحرك يولّد؛ الواجهة تستهلك عند التنفيذ |
| أدوات شجرة كتابة متقدمة (دمج · نقل فرع) | Tree Engine / Family Alignment — ليست منزل المندوب |

---

## 11) معايير قبول التصوّر قبل بدء التنفيذ

يُعتبر هذا التصوّر **جاهزًا للتحويل لكود** فقط عندما يتحقق **كل** ما يلي.  
**اعتماد المراجعة:** ✅ تم عبر عبارة صاحب المنتج **«ابدا»** (2026-08-09). المتبقي = بوابات السلّم فقط.

### 11.0 بوابة الأسئلة الخمسة (مغلقة — كانت شرط الاعتماد)

انظر **§14** للجدول الكامل Pass/Fail. الخمس كانت ✅ قبل الاعتماد:

1. إدراك ≤3 ثوانٍ: عدد + أول طلب + **ابدأ** — بلا بحث/شخص.  
2. إنهاء الطلب داخل الجلسة فقط.  
3. منع أخطاء: مقارنة · تحذير تعارض · تعديل أسهل من رفض.  
4. بعد القرار → **الطلب التالي** كمهمة لا قائمة منزل.  
5. جوال أولًا بلا zoom ولا تمرير لا نهائي لإدراك العمل.

### 11.0-ب النقاط الثلاث الحرجة (فلسفة التجربة — مكملة)

| # | السؤال | معيار النجاح في هذا التصوّر |
|---|--------|------------------------------|
| 1 | **أول شاشة يراها المندوب** — هل تعرض العمل المطلوب فورًا؟ | بعد الدخول: طابور فقط (+ هوية مختصرة) + **العدد** + **ابدأ** + بطاقة أولى مميّزة. **لا** شجرة · لا بحث · لا اختيار شخص · لا فلاتر bulk · لا تبويبات عمل منافسة. |
| 2 | **جلسة العمل داخل الطلب** — هل يُنهى الطلب بأقل عدد خطوات؟ | فتح → صفحة واحدة (حقائق · Validation · مقارنة) → قرار من الشريط السفلي. الأدوات اختيارية داخل الجلسة. **لا** قفز لشاشات إدارة. |
| 3 | **الانتقال للطلب التالي** — هل يشعر بطابور مهام لا لوحة إدارة؟ | بعد القرار: جلسة **الطلب التالي** تلقائيًا (زر احتياطي). ممنوع الهبوط على لوحة/شجرة/قائمة منزل بين المهام. |

إن ضعفت أي نقطة → يُراجع التصوّر هنا قبل أي شريحة كود. الصفحة الناتجة يجب أن تختلف جذريًا عن الحالية — لا نسخة محسّنة من `alzidan-tree` / admin.

### 11.1 بوابات السلّم (إلزامية قبل كود DW)

- [x] صاحب المنتج اعتمد المراجعة — عبارة **«ابدا»** (§14 كلها ✅) — 2026-08-09  
- [ ] Delegates v2 🟢 (قبول) — [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md)  
- [ ] Product Foundation 🟢 (تجميد الدستور الحي)  
- [ ] Request Experience منفَّذ بما يكفي لتغذية الطلبات بلغة النية  
- [ ] Validation Engine يوفّر نتائج/أسباب قابلة للعرض للمندوب  
- [ ] Workflow Engine مغلق/مستقر على السلّم (حالات · تعيين · انتقال · Audit · إشعارات من الانتقال)

### 11.2 اتساق الدستور

- [ ] السؤال المحوري للمندوب = **ماذا يجب أن أنجز الآن؟**  
- [ ] **الطلبات أولًا · الأدوات ثانيًا · البساطة قبل كثرة الخيارات**  
- [ ] المنزل = طابور طلبات — ليس شجرة · بلا تبويبات عمل منافسة  
- [ ] الطلب = جلسة عمل مستقلة  
- [ ] الأدوات داخل الجلسة فقط  
- [ ] بعد الإنجاز → الطلب التالي تلقائيًا  
- [ ] لا منطق حالات/تحقق في الواجهة  
- [ ] Single Write Rule محترم في التصميم  
- [ ] لغة المنتج بلا مصطلحات محظورة  
- [ ] الصفحة الحالية معلّمة legacy ومجمّدة تجميليًا (هذه الوثيقة §8)

### 11.3 جاهزية التصميم للتنفيذ

- [ ] أنواع الفهرس الستة لها مسار جلسة موصوف (أدوات + مقارنة إن لزم)  
- [ ] مسارات القبول / التعديل / الرفض موصوفة مع إلزام السبب حيث يلزم  
- [ ] حالات الفراغ والتحميل والخطأ موصوفة  
- [ ] فصل واضح عن Admin UX  

عندها فقط تُفتح شريحة تنفيذ **Delegate Workspace v1** في الخارطة — على **مراحل** (§13) لا دفعة واحدة.

---

## 13) شرائح تنفيذ مقترحة (بعد اعتماد UX + بوابات السلّم — ليست إذن بدء الآن)

> اعتماد UX («ابدا») **تم**. التنفيذ يُفتح **فقط بعد** بوابات السلّم (§11.1). ترتيب مقترح — لا big bang:

| مرحلة | النطاق | نتيجة ملموسة |
|--------|--------|----------------|
| **A — Inbox** | شاشة الطابور فقط: قائمة · بطاقة · فراغ · خطأ · فتح جلسة | المندوب يرى «ماذا يجب أن أنجز الآن؟» فورًا |
| **B — Session** | جلسة الطلب: ملخص · حقائق · Validation · مقارنة · شريط قرار · أسباب | إنهاء طلب بأقل خطوات داخل سياق واحد |
| **C — Next-queue** | بعد القرار: تأكيد قصير → التالي تلقائيًا · فراغ الطابور | إيقاع طابور مهام لا لوحة إدارة |
| **D — Contextual tools** | أدوات الجلسة المشروطة بالنوع (شخص · موقع عائلي · وسائط) | أدوات ثانيًا — تظهر عند الحاجة فقط |
| **E — Engine hard-wiring** | ربط كامل Workflow/Validation/Delegates · إزالة أي مسار كتابة موازٍ | الواجهة بلا منطق أعمال |

الصفحة الحالية تبقى legacy حتى استبدال واضح بمسار A→E — لا Enhancement تدريجي نحو هذا التصوّر.

---

## 12) ملخص قرارات UX (للمراجعة السريعة)

1. **Requests-first** — لا person-first · لا بحث قبل أول عمل.  
2. **إعادة تصميم** — لا Enhancement للصفحة الحالية · لا شبه بلوحة الشجرة/Admin.  
3. **دخول فوري:** عدد الطلبات + أول طلب + زر **ابدأ**.  
4. **جلسة لكل طلب** — أدوات سياقية فقط داخل الجلسة.  
5. **منع أخطاء:** مقارنة مسجّل/مقترح · تحذير تعارض · **نحتاج معلومة** أسهل من الرفض للصغير.  
6. **قرار → الطلب التالي تلقائيًا** — إيقاع مهام لا قائمة منزل.  
7. **Mobile-first وبسيط** — بلا zoom · بلا تمرير أفقي · قرارات ثابتة أسفل.  
8. **لغة بشرية** من Product Language.  
9. **المحركات تقرر** — الواجهة تعرض وتنفّذ أفعالًا بشرية فقط.  
10. **معتمد من صاحب المنتج («ابدا») · التنفيذ مجمّد** — لا كود DW حتى RX → VE → Workflow · وعلى مراحل (§13).

---

## 14) بوابة الاعتماد النهائية (5 أسئلة) — مغلقة

**الغرض:** كانت آخر فرصة لإصلاح التجربة قبل الاعتماد.  
**الحكم:** الخمس ✅ · **اعتمد صاحب المنتج المراجعة** بعبارة **«ابدا»** بتاريخ 2026-08-09.

| # | السؤال | النتيجة | دليل من التصوّر |
|---|--------|---------|-----------------|
| 1 | هل المندوب يعرف ماذا يفعل خلال 3 ثوانٍ؟ | ✅ Pass | §3.2 · §4.1: العدد ظاهر · أول طلب مميّز · زر **ابدأ** يفتحه · بلا بحث/اختيار شخص |
| 2 | هل يُنهي الطلب دون مغادرة جلسة العمل؟ | ✅ Pass | §3.1 · §4.2 · §7: كل القرار داخل الجلسة · أدوات عند الحاجة فقط · ممنوع تبويبات شجرة/مناسبات/ذكريات منافسة |
| 3 | هل يمنع التصميم الأخطاء؟ | ✅ Pass | §4.3 · §4.2 (شريط القرار): مقارنة مسجّل/مقترح · تحذير تعارض قبل القبول · **نحتاج معلومة إضافية** أبرز وأسهل من **لم يُقبل** للمشاكل الصغيرة |
| 4 | هل يشعر أنه يعمل على مهام؟ | ✅ Pass | §3.3 · §4.4: بعد القرار → جلسة **الطلب التالي** تلقائيًا · لا عودة لقائمة منزل/شجرة بين كل قبول |
| 5 | هل الصفحة مناسبة للجوال؟ | ✅ Pass | §5: فوق الثنية (عنوان+عدد+ابدأ+أول طلب) · شريط قرار ثابت · بلا zoom/تمرير أفقي · بطاقات قصيرة لا تمرير لا نهائي لإدراك العمل |

**قرار الاعتماد:** ✅ **معتمد من صاحب المنتج** — عبارة الاعتماد: **«ابدا»** / اعتماد المراجعة.  
**تجميد التنفيذ:** ❄️ ما زال ساريًا — **لا كود** مساحة المندوب حتى تُغلق بوابات السلّم (§11.1). الاعتماد ≠ شحن.

| حالة الوثيقة الآن | المعنى |
|-------------------|--------|
| ✅ معتمد من صاحب المنتج («ابدا») | مرجع UX ثابت |
| ❄️ التنفيذ مجمّد | بانتظار Delegates → Foundation → RX → VE → Workflow |
| ❌ ليس شبه الصفحة الحالية | Redesign كامل · legacy مجمّد (§8) |

---

**هذه الوثيقة معتمدة كمواصفة UX — وليست إذن بدء كود.**  
أي طلب «حسّن صفحة المندوب الحالية» أو «ابدأ كود DW الآن» يُرفض.  
المسار الصحيح الآن = إغلاق [`DELEGATES-V2-ACCEPTANCE.md`](./DELEGATES-V2-ACCEPTANCE.md) → تجميد الدستور الحي → Request Experience → … → ثم بناء مساحة العمل على مراحل (§13).
