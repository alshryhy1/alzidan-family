# Patch TREE-004 Unlink — إزالة الروابط الخاطئة تحت علي صالح الأحم

**التاريخ:** 2026-08-07  
**النطاق:** بيانات فقط — حذف صفوف `tree_children` المكرّرة خطأ تحت الأحم  
**الحالة:** ✅ مكتمل على الإنتاج (يدوي) — `577–583` محذوفة · `1417–1423` باقية · **لا يُعاد تشغيل SQL الحذف**

**SQL الجاهز للنسخ:** [`PATCH-TREE004-UNLINK-SQL.md`](./PATCH-TREE004-UNLINK-SQL.md)

---

## الآباء

| الأب | المسار | `person_id` | صف الأب |
|------|--------|-------------|---------|
| علي صالح (الأحم) | `لاحم…/صالح/علي` | `5299acff-5413-4503-9aec-57fde0ef5c26` | 1626 |
| علي صالح ناصر صالح | `لاحم…/صالح/ناصر/صالح/علي` | `8634e81c-fc8a-486d-865f-6b495b81dc23` | 634 |

ملاحظة: الأبناء الخاطئون تحت الأحم ما زال `parent_person_id` لديهم يشير إلى UUID ميت `9a3b4a7a-…`؛ الربط الظاهر للواجهة يتم عبر `parent_name`.

---

## Dry-run — صفوف للحذف فقط

### أ) روابط مباشرة خاطئة (أب = علي صالح الأحم)

| id | الابن | يقابل تحت ناصر |
|---:|-------|----------------|
| 577 | رضاء | 1417 |
| 580 | نايف | 1420 |
| 582 | وليد | 1422 |
| 583 | عبدالمجيد | 1423 |

### ب) أحفاد مكرّرون تحت النسخ الخاطئة (لازم حذفهم وإلا تبقى يتامى مسار تحت الأحم)

| id | المسار تحت الأحم | يقابل تحت ناصر |
|---:|------------------|----------------|
| 578 | …/علي/رضاء/يزيد | 1418 |
| 579 | …/علي/رضاء/زياد | 1419 |
| 581 | …/علي/نايف/وليد | 1421 |

**مجموع الحذف:** `577,578,579,580,581,582,583`

### لا يُمس

- كل صفوف ناصر: `1417–1423` (بما فيها الأحفاد).
- صف الأب علي صالح الأحم نفسه (`1626`).
- لا يوجد أبناء مباشرون آخرون لعلي صالح الأحم في المسح الحي — المجموعة أعلاه هي كامل شجرته المباشرة الحالية.

---

## Backup

`backups/patch-tree004-ali-unlink-20260807/`  
- `dry-run-delete-plan.json`  
- `rows-to-delete.json`  
- `naser-rows-to-keep.json`  
- `RESTORE.md`

لقطة سابقة للأبناء: `backups/patch-tree004-ali-20260807/`

---

## Apply (يدوي — SQL Editor فقط)

نفّذ الخطوات A→B→C→D من [`PATCH-TREE004-UNLINK-SQL.md`](./PATCH-TREE004-UNLINK-SQL.md) في Supabase SQL Editor.

- الحذف مقيّد بـ `WHERE id IN (577,578,579,580,581,582,583)` فقط.
- صفوف ناصر `1417–1423` تبقى كما هي.
- **لا** يُعاد ربط `parent_person_id` الميت إلى UUID الأحم الحي (يفاقم الخلل في واجهات تعتمد UUID).

السكربت `scripts/repair-ali-dual-unlink.mjs` يبقى للـ dry-run المحلي فقط؛ التطبيق المعتمد هو SQL اليدوي أعلاه.

---

## نتيجة التطبيق (متحقَّقة)

المستخدم أكمل الحذف يدويًا بنجاح (2026-08-07):

| فحص | النتيجة |
|------|---------|
| `577–583` | محذوفة ✅ |
| ناصر `1417–1423` | باقية ✅ |

**سياسة:** لا تُدرَج هذه الصفوف في خطة Integrity ولا يُعاد تشغيل [`PATCH-TREE004-UNLINK-SQL.md`](./PATCH-TREE004-UNLINK-SQL.md).

تنظيف إضافي مكتمل يدويًا: حذف المختصرات `321` و`1730` مع الإبقاء على `491` و`492` — انظر [`PATCH-DATA-CLEANUP-DONE.md`](./PATCH-DATA-CLEANUP-DONE.md).
