# بوابة 1 — اختبار قبول Delegates v2 (عربي · تنفيذي)

**الحالة:** 🟡 **فشل قبول حي** — طلب الصلاحية + قبول الإدارة نجحا؛ **تفعيل الدخول بعد القبول كان مكسورًا** (إصلاح في المستودع · بانتظار SQL + نشر واجهة + إعادة اختبار)  
**التاريخ:** 2026-08-09  
**آخر تحقق كود:** 2026-08-09 (مسار قبول→دخول)  
**الغرض:** إغلاق بوابة Delegates v2 → 🟢 حتى يُسمح بتجميد الدستور الحي ثم Request Experience  
**المراجع:** [`DELEGATES-V2-PHASE2.md`](./DELEGATES-V2-PHASE2.md) · [`ENGINEERING-ROADMAP.md`](./ENGINEERING-ROADMAP.md) · مسودة بوابة 2: [`PRODUCT-FOUNDATION-FREEZE-DRAFT.md`](./PRODUCT-FOUNDATION-FREEZE-DRAFT.md)  
**SQL مطلوب الآن:** من الإدارة → **⚙ أدوات الصيانة → أوامر الصيانة الجاهزة** → «إصلاح دخول المندوب بعد القبول (بوابة 1)» (`maint.fix_delegate_portal_path_v1`) ثم علّمه **مُنفذ** (+ الأساس/الفرض إن لم يُطبَّقا). ملف المصدر: `supabase/sql/COPY-ME-fix-delegate-portal-path.sql`

> **قاعدة:** لا ميزات مندوب جديدة · لا تحسين تجميلي · لا كود Delegate Workspace · لا Request Experience حتى 🟢 بوابة 1 ثم 🟢 بوابة 2.  
> النجاح هنا = قبول حي على الإنتاج + تعليم §6.  
> **لا تُعلَن بوابة 1 🟢** ولا تُقال «بوابة 1 نجحت» قبل إعادة اختبار الدخول بعد القبول.

---

## عطل القبول الحي (2026-08-09) — مسار التفعيل بعد «قبول»

| خطوة | نتيجة الاختبار |
|------|----------------|
| إرسال طلب صلاحية مندوب | ✅ |
| قبول من الإدارة (+ انتقال إداري / Legacy approved) | ✅ |
| دخول المندوب بنفس الرقم السري بعد القبول | ❌ «تعذر التحقق من بيانات الدخول حاليًا» |

**سبب جذري (كود):**  
1. زر «قبول» كان يحدّث `approval_requests` فقط (Legacy) **دون** إنشاء/تحديث `delegates_v2`.  
2. دخول المندوب عبر `check_tree_delegate_access` يفضّل **Delegates v2**؛ عند النجاح كان يعيد `allowed` **بدون** `request_id` فترفض الواجهة الدخول برسالة عامة.

**إصلاح جذري في المستودع:** تفعيل/مزامنة `delegates_v2` عند اعتماد طلب مندوب (RPC + trigger) + إصلاح `request_id` في `check_*` + قبول `delegate_id` في `delegate.js`.

---

## خلاصة التحقق الآلي (الآن)

| الطبقة | النتيجة |
|--------|---------|
| واجهة إدارة (تفعيل/تعطيل · دور · مزامنة · تدقيق) | ✅ **موثّق بالكود** — مربوطة وRPCs صحيحة |
| فرض الصلاحيات في SQL (Enforce) | ✅ **موثّق بالكود** — `disabled` / `no_permission` / `bad_secret` |
| رسائل عربية في بوابة المندوب | ✅ **موثّق بالكود** — شجرة + مناسبات + دخول |
| قبول → تفعيل دخول مندوب (نفس المصدر) | ✅ **كود** — ⏳ **حي بعد COPY-ME-fix + Hard Refresh** |
| تطبيق SQL على الإنتاج + سلوك حي | ⏳ **يلزم أمر الصيانة الجاهز من SQL Workspace** |
| دخان الموقع من الآلة | ⛔ `https://alzidan.org` أعاد **403** لطلبات آلية — افتح من متصفحك |

**بوابة 1 ليست 🟢** حتى ينجح دخول المندوب بعد القبول وتُغلق خانات §6.

---

## تشغيل إعادة الاختبار (مسار القبول→الدخول أولًا)

**قبل أي شيء:**  
1. إدارة → `#module=tools` → **أوامر الصيانة الجاهزة** → شغّل «ترقية منفّذ SQL Workspace» إن لزم ثم «إصلاح دخول المندوب بعد القبول» → علّم **مُنفذ**.  
2. انشر/ادفع واجهة الموقع إن كانت تُبنى من git.  
3. Hard Refresh للصفحتين.

**روابط:** إدارة → `https://alzidan.org/pages/admin.html#module=requests` · مندوبون → `#module=delegates` · بوابة مندوب → `https://alzidan.org/pages/alzidan-tree.html?view=delegate`

| # | أنت تفعل | ✅ |
|---|----------|---|
| A | طلب صلاحية مندوب جديد (أو استخدم طلبًا معتمدًا) بنفس الفرع/الجوال/الرقم السري | ☐ |
| B | إدارة → قبول الطلب → يجب رسالة تفيد تفعيل دخول المندوب (أو يظهر في المندوبون بعد التحديث) | ☐ |
| C | بوابة المندوب → نفس البيانات → **دخول ينجح** (لا «تعذر التحقق…») | ☐ |
| D | ثم أكمل اختبارات الدور/التعطيل/التدقيق أدناه | ☐ |

### تشغيل 5 دقائق (صلاحيات v2 — بعد نجاح A–C)

| دقيقة | أنت تفعل | تضع ✅ هنا إن نجح |
|-------|----------|------------------|
| 0 | Hard Refresh · سجّل دخول مدير · الشريط **Delegates v2** | ☐ |
| 1 | مندوب اختبار → دور **عرض فقط (`viewer`)** → تدقيق `delegate.role_set` + `previous_role_key` | ☐ |
| 2 | بوابة المندوب: حفظ شجرة/مناسبة → «دورك الحالي لا يسمح…» | ☐ |
| 3 | أعد `branch_editor`/`full_delegate` → كتابة تنجح · عطّل → «حساب المندوب معطّل…» · فعّل | ☐ |
| 4 | تدقيق: `delegate.role_set` + `delegate.disable` + `delegate.enable` | ☐ |

إذا **A–C** و**0–4** كلها ✅ → علّم §6 → قل للوكيل **«بوابة 1 نجحت»**. حتى ذلك الحين البوابة **🟡**.

---

## مصفوفة التحقق (كود ↔ حي)

أساطير: **✅ كود** = مثبت في المستودع · **⏳ حي** = يحتاج جلسة مدير/مندوب · **⛔ آلي** = تعذّر من هذه البيئة

### 0) جاهزية

| # | البند | كود | حي | ملاحظة |
|---|--------|-----|-----|--------|
| 0.1 | COPY-ME أساس ثم Enforce في المستودع ونظيفين | ✅ | ⏳ | PHASE2 تقول مطبّق على الإنتاج — أكّد إن الشريط ليس «وضع مؤقت» |
| 0.2 | Hard Refresh | — | ⏳ | |
| 0.3 | حساب مدير + مندوب اختبار | — | ⏳ | |

### 1) الصلاحيات

| # | البند | كود | حي |
|---|--------|-----|-----|
| 1.1 | مزامنة → `admin_delegates_v2_sync_from_requests_v1` + زر في UI | ✅ | ⏳ |
| 1.2 | تعيين `viewer` → `admin_delegates_v2_set_role_v1` + `<select data-delegate-role>` | ✅ | ⏳ |
| 1.3 | رفض كتابة `viewer`: لا `tree.write`/`events.write` في الكتالوج · Enforce يرد `no_permission` · UI عربي | ✅ | ⏳ |
| 1.4 | `branch_editor` / `full_delegate` يملكان الكتابة في الكتالوج | ✅ | ⏳ |
| 1.5 | بلا صف v2 → مسار legacy في SQL/UI | ✅ | ⏳ إن وُجد |

### 2) الأدوار

| # | البند | كود | حي |
|---|--------|-----|-----|
| 2.1 | تغيير الدور من الإدارة يعيد التحميل بعد RPC | ✅ | ⏳ |
| 2.2 | تدقيق الدور: Enforce يكتب `previous_role_key` في payload | ✅ | ⏳ (تأكد الظهور في عمود التفاصيل) |
| 2.3 | `branch_key` عمود إلزامي/معروض في القائمة | ✅ | ⏳ |

### 3) تفعيل / تعطيل

| # | البند | كود | حي |
|---|--------|-----|-----|
| 3.1 | زر تعطيل/تفعيل → `admin_delegates_v2_set_enabled_v1` + pill مفعّل/معطّل | ✅ | ⏳ |
| 3.2 | Enforce `is_enabled=false` → `reason: disabled` · رسائل عربية في `delegate.js` / login | ✅ | ⏳ |
| 3.3 | إعادة التفعيل مسار معاكس في نفس RPC | ✅ | ⏳ |
| 3.4 | تدقيق `delegate.enable` / `delegate.disable` | ✅ | ⏳ |

### 4) سجل التدقيق

| # | الحدث | action_key (كود) | حي |
|---|--------|------------------|-----|
| 4.1 | مزامنة | `delegates.sync_from_requests` | ⏳ / لا تنطبق |
| 4.2 | تغيير دور | `delegate.role_set` + `previous_role_key` | ⏳ |
| 4.3 | تعطيل | `delegate.disable` | ⏳ |
| 4.4 | تفعيل | `delegate.enable` | ⏳ |

واجهة التدقيق: `admin_audit_log_list_v1` مع `p_entity_type: "delegates_v2"` — تعرض `action_key` + JSON للـ payload.

---

## خريطة RPC ↔ UI (مرجع سريع)

| عملية UI | RPC | ملف |
|----------|-----|-----|
| قائمة | `admin_delegates_v2_list_v1` | `admin-delegates-v2.js` |
| أدوار | `admin_delegate_roles_list_v1` | نفسه |
| مزامنة | `admin_delegates_v2_sync_from_requests_v1` | نفسه |
| تفعيل/تعطيل | `admin_delegates_v2_set_enabled_v1` | نفسه |
| دور | `admin_delegates_v2_set_role_v1` (نسخة Enforce أقوى بالتدقيق) | نفسه + enforce SQL |
| تدقيق | `admin_audit_log_list_v1` | نفسه |
| فرض كتابة شجرة | `tree_delegate_allowed_v1` ← `delegate_v2_check_op_v1(..., 'tree.write')` | enforce SQL |
| فرض كتابة مناسبات | `events_delegate_allowed_v1` ← `... 'events.write'` | enforce SQL |
| أسباب الوصول | `check_tree_delegate_access` / `check_events_delegate_access` | enforce SQL |

**أسلاك الواجهة:** `pages/admin.html` يحمّل `admin-delegates-v2.js?v=20260808p2` · الشِلّ يفتح موديول `delegates`.

**رسائل عربية مثبتة في الكود:**

- معطّل → «حساب المندوب معطّل. تواصل مع الإدارة لإعادة التفعيل.»
- بلا صلاحية → «دورك الحالي لا يسمح…» (شجرة / مناسبات)
- سر غير مطابق → «بيانات الدخول لا تطابق…»

---

## 5) ما ليس جزءًا من هذه البوابة

| خارج النطاق | لماذا |
|-------------|--------|
| اعتماد متعدد المراحل | ⚪ فوق Workflow Engine لاحقًا |
| واجهة منح/سحب عمليات أدق من الدور | خارج شريحة القبول |
| Delegate Workspace / تحسين `delegate.js` تجميليًا | مجمّد |
| Request Experience | بعد بوابة 1 🟢 ثم تجميد Foundation |

---

## 6) نتيجة البوابة (يملأها صاحب المنتج بعد النقرات الحية)

| بند قبول (الخارطة) | ☐ مغلق |
|--------------------|--------|
| اختبار الصلاحيات | ☐ |
| اختبار الأدوار | ☐ |
| اختبار التفعيل والتعطيل | ☐ |
| اختبار سجل التدقيق | ☐ |

| قرار | المعنى |
|------|--------|
| **كل الخانات في §6 ✅** | بوابة 1 → 🟢 — حدّث PHASE2 + الخارطة · ثم نفّذ [`PRODUCT-FOUNDATION-FREEZE-DRAFT.md`](./PRODUCT-FOUNDATION-FREEZE-DRAFT.md) كبوابة 2 في commit واحد |
| **أي فشل** | ابقَ 🟡 — سجّل رقم الخطوة + نص الرسالة على الشاشة · **لا** برمجة ميزات جديدة |

**لا** يُفتح كود Delegate Workspace من هذه البوابة.

---


---

## 7) قبول إضافي — إعادة تعيين الرقم السري (ليس Workflow عام)

**الحالة:** 🟡 بانتظار تشغيل أمر الصيانة + اختبار حي  
**Preset:** `maint.delegate_secret_reset_v1` · المصدر: `supabase/sql/COPY-ME-delegate-secret-reset.sql`

| # | أنت تفعل | ☐ |
|---|----------|---|
| S0 | أدوات الصيانة → تشغيل أمر «طلب إعادة تعيين الرقم السري» → مُنفذ | ☐ |
| S1 | بوابة المندوب → نسيت الرقم السري → طلب جديد بعنوان واضح للإدارة | ☐ |
| S2 | الإدارة ترى بطاقة «طلب إعادة تعيين الرقم السري» (ليس events_delegate / مُعيَّن) | ☐ |
| S3 | اعتماد → دخول بالرقم الجديد ينجح · رفض يُغلق الطلب دون تغيير السر | ☐ |

> بوابة 1 تبقى 🟡 حتى ينجح مسار القبول→الدخول (A–C) حيًا. هذا البند إضافي ولا يُعلن بوابة 1 🟢 وحده.


## Definition of Done

- [x] تحقق كود: UI + RPCs + Enforce + رسائل عربية (2026-08-09)
- [ ] SQL أساس+فرض + أمر بوابة 1 من SQL Workspace مُنفذ على بيئة الهدف
- [ ] تشغيل 5 دقائق (§ أعلاه) ناجح
- [ ] §6 كل البنود الأربعة ✅
- [ ] تحديث PHASE2 + ENGINEERING-ROADMAP إلى 🟢 بعد إبلاغ صاحب المنتج
- [ ] بعده مباشرة: تجميد بوابة 2 من المسودة — **ثم فقط** Request Experience (تصميم أولًا)
