# Tree Correction Operations Map v1

**الحالة:** §2.0 **مجمّد** (كتالوج واقع + جاهزية) — شرائح live: `add_person` · `reorder_children` · `name_correction` · `phone_correction` · `birth_date_correction` · `parent_change` · `city_correction`  
**التاريخ:** 2026-08-15  
**آخر تحديث:** 2026-08-17 — إغلاق `tree_edit` العام للعضو + `city_correction` live  
**العبارة:** `operation` مصدر الحقيقة؛ خيار عضو = `live` فقط؛ التجميد للكتالوج لا يمنع ترقية صف من missing→live بشريحة كاملة  
**المراجع:** [`REQUEST-CATALOG.md`](./REQUEST-CATALOG.md) · [`PERSON-VISIBILITY-SPEC-v1.md`](./PERSON-VISIBILITY-SPEC-v1.md) · [`ENGINEERING-ROADMAP.md`](./ENGINEERING-ROADMAP.md) §Tree Engine v2 · تدقيق المرسل↔المستقبل (2026-08-15)

---

## 0) القرار الأحمر

1. **لا محرر بطاقة شجرة عام** لكل طلب تصحيح.
2. **لا زر «حفظ» عام** — كل عملية تعرف ماذا تغيّر وعلى من وبأي مسار تحقق.
3. **`tree_card` = إضافة فرد فقط** — ليست «أي شيء متعلق بالشجرة». **العملية تحدد النوع؛ نوع البيانات لا يحدد العملية.**
4. النية في الفهرس تبقى **«صحح بيانات»**؛ تحتها **`operation`** واضح.
5. الأسماء التقنية مفاهيمية — **لا أعمدة DB** قبل مراجعة schema حيّة وفتح تنفيذ.
6. أي عملية تلمس النساء/العرض تلتزم [`PERSON-VISIBILITY-SPEC-v1.md`](./PERSON-VISIBILITY-SPEC-v1.md).
7. لا اختراع `person_id` / `parent_person_id` من الاسم وحده — تطابق غامض = `needs_review`.
8. كل عملية خطيرة = **معاينة أثر (ما يتغير + ما لا يتغير) → موافقة → حفظ → سجل**.
9. **Cross-Client Contract (§0.1)** + **تحقق عند الإنشاء والاستقبال (§0.2)**.
10. **قاعدة الحفظ الحاكمة:** لا حفظ يغيّر غير ما عاينته الإدارة ووافقت عليه.
11. **قاعدة عرض العضو:** خيار قابل للإرسال في واجهة العضو = حالة جاهزية **`live` فقط**. `partial` و`missing_*` و`legacy_only` **لا** تُعرض للعضو كخيار إرسال (قد تظهر داخليًا في كتالوج الإدارة كـ«قيد التطوير»). لا يُستثنى `partial` إلا بنطاق معلن ومقفول صراحة في هذا الملف.
12. **لا إصلاح بـ parser أذكى:** تفكيك `tree_edit` العام إلى `operation` حقيقية عند المصدر — ممنوع استنتاج العملية من النص الحر للطلبات الجديدة.

---

## 0.1) Cross-Client Contract Rule — ملزم

> كل عملية تصحيح أو تعديل يمكن إنشاؤها من التطبيق يجب أن يكون لها **عقد canonical واحد** تشترك فيه **التطبيق** و**الويب** و**الإدارة**. لا يجوز لأي عميل إنشاء صيغة خاصة به لنفس العملية، ولا يجوز للإدارة تفسير نص حر لتحديد العملية إذا كان يمكن إرسال نوع العملية وبياناتها المنظمة.

> عند تطوير تحسين لمسار قائم، تتم مراجعة **المرسل والمستقبل ومسار الحفظ** معًا، وليس تعديل الطرف المتأثر فقط.

```text
Mobile / Web
      ↓
Correction Contract  (operation + canonical payload)
      ↓
Server-side validation   ← إلزامي؛ لا يكفي التزام الواجهة
      ↓
approval_requests
      ↓
Admin Router
      ↓
محرر العملية الصحيح
      ↓
Validation + Preview (ما يتغير / ما لا يتغير)
      ↓
مسار الحفظ الصحيح
      ↓
الشجرة / الهوية
```

### ما يُمنع

| ممنوع | السبب |
|-------|--------|
| جوال بنص حر لنفس عملية لها payload منظّم على الويب | يعيد التصنيف الخاطئ |
| ويب بعقد مختلف عن الجوال لنفس `operation` | إدارتان ضمنيًا |
| إصلاح الإدارة فقط والمرسل قديم | تحسين غير مكتمل |
| استنتاج `operation` من النص عند وجود حقل منظم | هش |
| اكتمال مسار عميل واحد فقط | يكسر العقد الموحّد |
| عميل قديم يُدرج payload غير صالح دون رفض | طلبات مشوّهة في الطابور |

### اختبارات القبول

**عالميًا (عند اكتمال العملية لكل العملاء المعتمدين):**

```text
Mobile → Request → Admin/Delegate → Preview → Save → Tree
Web    → Request → Admin/Delegate → Preview → Save → Tree
Legacy → Safe Recovery/Review → no wrong reinterpretation
```

**شريحة التنفيذ الحالية (§8):** يكفي  
`Web → Admin/Delegate → reorder_children → Preview → Save → Tree`  
+ Legacy. الجوال **خارج النطاق** حتى فتح صريح لاحقًا.

---

## 0.2) تحقق العقد عند الإنشاء والاستقبال — ملزم

> العقد يُتحقق منه عند **الإنشاء** وعند **الاستقبال**، وليس عند الواجهة فقط.

- إصدار جوال قديم بعد أشهر **لا يجوز** أن يمرّر payload غير صالح إلى `approval_requests` كطلب «سليم».
- الرفض عند الإنشاء برسالة واضحة؛ والصفوف التي تفلت (تراث/يدوي) تُصنَّف عند الاستقبال: `needs_review` / `unsupported` — **بلا تفسير تلقائي خاطئ**.

---

## 0.3) فصل الأنواع عن «كل ما يخص الشجرة» — ملزم

| المفهوم | المعنى |
|---------|--------|
| `tree_card` | **إضافة شخص / بطاقة إضافة فرد** فقط |
| `tree_edit` | **غلاف تراثي / partial** — طلب «صحح بيانات» بلا `operation`؛ يُفكَّك لاحقًا إلى عمليات حقيقية. ليس مسار حفظ آمن للحقول المختلطة |
| `tree_correction` (مفهوم مستقبلي) | غلاف تصحيح بعمليات متعددة إن لزم للـschema |
| `reorder_children` | تغيير ترتيب مجموعة أبناء تحت أب |
| `parent_change` | تغيير علاقة الأب |
| `phone_correction` | تصحيح جوال لشخص معروف |
| `phone_link` | ربط جوال غير مرتبط بشخص موجود |
| `phone_duplicate_conflict` | جوال مرتبط بشخص آخر → تعارض هوية |
| … | كل عملية في §2 |

**ممنوع مستقبلًا:** «هذا متعلق بالشجرة → `tree_card`».

---

## 1) النموذج المفاهيمي للطلب

```text
request
├── intent          → صحح بيانات (كتالوج)
├── kind            → tree_edit | tree_correction (لاحقًا) | tree_card فقط للإضافة
├── operation       → انظر §2
├── targets         → person_ids[] / parent_person_id / branch_key
├── match_inputs    → أسماء / جوالات للمطابقة فقط (ليست مرجع التنفيذ)
├── requested_changes
├── source          → web_rx | mobile | delegate | admin | legacy
└── review_state    → complete | needs_review | unsupported | conflict
```

النص البشري للعرض والتدقيق؛ **مصدر التنفيذ** = `operation` + أهداف بالهوية + التغييرات المعاينة.

---

## 2) سجل العمليات (Operation Registry)

| # | العملية | `operation` | النطاق | مرسل اليوم | مرجع التنفيذ (canonical) | واجهة الإدارة | مسار الحفظ | تحقق قبل الحفظ | عند الفشل |
|---|---------|-------------|--------|------------|---------------------------|---------------|------------|----------------|-----------|
| 1 | تغيير الأب / ابن تحت أب خطأ | `parent_change` | علاقة | نص حر فقط | `person_id`, `new_parent_person_id` (+ قديم اختياري) | محرر علاقة + أثر | Tree Engine نقل أب | أب فريد؛ لا غموض | `needs_review` |
| 2 | ترتيب الأبناء | `reorder_children` | مجموعة تحت أب | **Web RX live** (+ Legacy Safe Review للأرشيف) | `parent_person_id` + `ordered_children: person_id[]` بعد مطابقة آمنة | محرر مجموعة (`AlzidanTreeCorrectionReorder`) | تحديث `birth_order` دفعة + اعتماد الطلب | الكل تحت نفس الأب؛ مطابقة كاملة أو مراجعة | لا تخمين؛ لا ترتيب جزئي صامت |
| 3 | تصحيح جوال شخص معروف | `phone_correction` | شخص | **Web RX live** | `person_id`, `phone_new`, `phone_old?` | `AlzidanTreeCorrectionPerson` | تحديث `member_profiles` | تنسيق؛ الرقم غير لشخص آخر | تعارض → رفض صريح (مسار `phone_duplicate_conflict` لاحقًا) |
| 4 | ربط جوال غير مرتبط بشخص | `phone_link` | شخص↔جوال | لا | `person_id`, `phone` | محرر جوال (ربط) | ربط هوية | الشخص موجود؛ الرقم حر | مراجعة |
| 5 | تعارض جوال مع شخص آخر | `phone_duplicate_conflict` | هوية | لا | `phone`, `claimed_person_id?`, `existing_person_id` | محرر تعارض / مراجعة | لا حفظ صامت؛ قرار صريح | إظهار الطرفين | إيقاف حتى الحسم |
| 6 | إضافة شخص تحت أب | `add_person` | فرد | RX / جوال / `tree_card` | حمولة `tree_card.v1` | مسار **إضافة فرد** (ليس Router تصحيح) | import/upsert | TREE-003؛ حارس تكرار | رفض الاعتماد |
| 7 | تصحيح اسم | `name_correction` | شخص | **Web RX live** | `person_id`, `name_new` | `AlzidanTreeCorrectionPerson` | `admin_tree_child_upsert_v1` | كلمة واحدة؛ لا تكرار تحت الأب | مراجعة إن التباس |
| 8 | تصحيح حقول أب/أم (عرض) | `parent_fields_correction` | شخص+عرض | لا | أهداف + حقول مسموحة | محرر شخص + Visibility | حقول مصرّح بها فقط | لا إظهار محظور؛ لا اختراع علاقة | إيقاف |
| 9 | نقل شخص بين أبوين | `move_person` | شخص+علاقة | أدوات مباشرة فقط | `person_id`, `from_parent_id`, `to_parent_id` | محرر علاقة + أثر | Move UUID | Dry-run | إيقاف |
| 10 | تصحيح مجموعة أبناء | `group_correction` | مجموعة | لا | `parent_person_id` + تغييرات لكل `person_id` | محرر مجموعة | بعد حسم كل عنصر | عملية فرعية صريحة لكل صف | العناصر غير المحسومة تبقى مفتوحة |
| 11 | إضافة مجموعة (أرقام/أسماء) | `add_children_group` | مجموعة | لا كطلب موحّد | بعد المطابقة: قرارات per-row | جدول مطابقة ثم إجراءات | ربط و/أو إنشاء حسب الحالة | **لا «حفظ المجموعة» الواحد** حتى تُحسم كل الحالات | تعارض/غير صالح يوقف الإغلاق |
| 12 | تصحيح فرع | `branch_correction` | شخص | تعديل فرع الطلب فقط | `person_id`, `branch_key_new` | محرر شخص/علاقة | نقل فرع آمن | اتساق الأب/الفرع | مراجعة |
| 13 | دمج/تعارض هوية | `identity_merge_review` | شخصان+ | حارس يمنع | `person_ids[]`, قرار | مسار دمج | dry-run | لا دمج تلقائي | موافقة صريحة |
| 14 | تصحيح مسار نسب | `lineage_path_correction` | سلسلة | لا | آباء بالـUUID | محرر مسار + أثر | سلسلة عبر UUID | لا بناء من أسماء فقط | مراجعة |

**مرشّح لاحقًا:** زوجة/أم · مولود · وفاة مرتبطة · فصل مكرر · إصلاح `parent_person_id` الناقص — بنفس السجل. **لا تُضاف للسجل كخيارات عضو** حتى يثبت مسار الحفظ.

---

## 2.0) جدول الجاهزية — لغة الطلب التي يفهمها النظام (جرد 2026-08-15)

> **مجمّد رسميًا 2026-08-15** — تجميد **كتالوج الواقع الحالي وحالات الجاهزية** فقط.  
> **لم يتجمّد:** التنفيذ النهائي لمحرك التصحيح كاملًا (تفكيك `tree_edit` ومسارات name/phone/parent/… عند دورها).  
> أثناء شريحة UX لـ `reorder_children`: **لا إضافة `operation` جديد** إلا إذا ظهر واقع قائم يمنع صحة التنفيذ.
>
> يحدد لكل عملية: من يرسل → ماذا يرسل → من يستقبل → أي محرر → ماذا يتغير / لا يتغير → كيف تحفظ → وهل جاهزة فعلًا.

### حالات الجاهزية

| الحالة | المعنى | يظهر كخيار إرسال للعضو؟ | يظهر في كتالوج الإدارة؟ |
|--------|--------|--------------------------|-------------------------|
| `live` | إنشاء منظّم + محرر + Preview + حفظ أثري + اعتماد حالة | **نعم** | نعم |
| `partial` | جزء من المسار فقط (غالبًا إنشاء بلا حفظ أثري، أو العكس) | **لا** (إلا نطاق معلن ومقفول هنا) | نعم — «قيد التطوير» |
| `missing_create` | لا نموذج عضو يُنشئ `operation` | لا | نعم |
| `missing_editor` | لا محرر استقبال للعملية | لا | نعم |
| `missing_save` | لا مسار حفظ يطابق Preview | لا | نعم |
| `legacy_only` | أرشيف / Safe Review فقط — لا إنتاج طلبات جديدة بهذا الشكل | لا | نعم (أرشيف) |

**قاعدة مقفولة:** واجهة العضو تعرض للإرسال **`live` فقط**. `partial` ليس عملية جاهزة للعضو.

### محرك التصحيح اليوم (ملخص)

```text
Family Correction Engine (Web)

                 operation
                    │
     ┌──────────────┼──────────────┐
     │              │              │
add_person   reorder_children   name_correction   phone_correction   tree_edit (عام)
     │              │                  │                  │                │
   live           live               live               live            partial
                    │
                    ▼
            Preview + Save + approve
```

### جدول الجاهزية التفصيلي

| ماذا يريد العضو؟ (لغة منتج) | UX مستقبلي (مجموعة) | `operation` | البيانات المطلوبة (canonical) | الملاحظة | المستلم | محرر الاستقبال | يتغير | لن يتغير | مسار الحفظ | الجاهزية | خيار عضو؟ |
|----------------------------|---------------------|-------------|-------------------------------|----------|---------|----------------|-------|----------|------------|----------|-----------|
| أضف فردًا تحت أب | إضافة / شجرة | `add_person` (`kind=tree_card`) | حمولة `tree_card.v1` + هوية أب عند الإمكان | اختيارية | إدارة + مندوب فرع | محرر إضافة فرد | صف الشجرة الجديد | علاقات/حقول غير معاينة | `importTreeCardToTree` ثم اعتماد | `live` | نعم |
| إعادة ترتيب أبناء تحت أب | تصحيح الشجرة | `reorder_children` (`kind=tree_edit`) | `parent_person_id` + `ordered_children: person_id[]` مرتبة | اختيارية | إدارة + مندوب (صلاحية شجرة) | `AlzidanTreeCorrectionReorder` | `birth_order` 1…N للمجموعة | أسماء، جوالات، ميلاد، آباء، هويات | clear→assign `birth_order` + تحقق + `admin_set_request_status_v2` | `live` | نعم |
| «صحح بيانات» عام (اسم+جوال+ميلاد+… مختلط) | — | *(بلا operation)* `kind=tree_edit` | حقول حرّة / JSON حقول + notes | — | أرشيف قديم فقط | مراجعة آمنة | — | — | **مغلق لإنشاء عضو جديد** (توجيه لخيارات live) | `partial` → **مغلق للإرسال** | **لا** |
| تصحيح الاسم فقط | تصحيح شخص | `name_correction` | `person_id`, `name_new` | اختيارية | إدارة (المندوب: استقبال؛ حفظ الاسم إدارة فقط) | `AlzidanTreeCorrectionPerson` | الاسم + مسار الصف | الجوال/الأب/الترتيب؛ مسارات الأبناء لا تُحدَّث تلقائيًا | `admin_tree_child_upsert_v1` + اعتماد | `live` | نعم |
| تصحيح / إضافة جوال لشخص معروف | تصحيح شخص | `phone_correction` | `person_id`, `phone_new`, `phone_old?` | اختيارية | إدارة + مندوب | `AlzidanTreeCorrectionPerson` | الجوال في `member_profiles` | الاسم/الأب/الترتيب/الميلاد | حفظ ملف العضو + رفض تعارض رقم لشخص آخر | `live` | نعم |
| ربط جوال حر بشخص | تصحيح شخص | `phone_link` | `person_id`, `phone` | اختيارية | إدارة | محرر ربط — غير مبني | ربط الهوية | بقية الحقول | غير مبني | `missing_*` | لا |
| تعارض جوال مع شخص آخر | هوية | `phone_duplicate_conflict` | `phone`, أطراف الهوية | اختيارية | إدارة | محرر تعارض — غير مبني | قرار صريح فقط | لا حفظ صامت | إيقاف حتى الحسم | `missing_*` | لا |
| تصحيح تاريخ الميلاد | تصحيح شخص | `birth_date_correction` | `person_id`, `birth_date_new` | اختيارية | إدارة (+ مندوب إن وُجد مسار) | `AlzidanTreeCorrectionPerson` | `birth_date_g` (+ سنة) | الاسم/الأب/الجوال/الترتيب | upsert شجرة + اعتماد | `live` | نعم |
| تصحيح مدينة/قرية | تصحيح شخص | `city_correction` | `person_id`, `city_new?`, `area_new?` (واحد على الأقل) | اختيارية | إدارة + مندوب | `AlzidanTreeCorrectionPerson` | `city` / `area` في `tree_children` | الاسم/الأب/الجوال/الميلاد/الترتيب | upsert شجرة أو `tree_children_update_v1` + اعتماد | `live` | نعم |
| الأب خطأ / تصحيح الأب | تصحيح الشجرة | `parent_change` | `person_id`, `new_parent_person_id` | اختيارية | إدارة (حفظ الإدارة فقط في هذه الشريحة) | `AlzidanTreeCorrectionPerson` | `parent_person_id` + مسار الشخص | الاسم/الجوال/الميلاد؛ مسارات الأبناء لا تُحدَّث تلقائيًا | upsert شجرة + اعتماد | `live` | نعم |
| نقل شخص إلى أب آخر | تصحيح الشجرة | `move_person` | `person_id`, `from_parent_id`, `to_parent_id` | اختيارية | إدارة (أدوات مباشرة اليوم) | محرر علاقة — غير كطلب عضو | العلاقة/المسار | غير المعاين | Move UUID + dry-run | `missing_create` (أدوات مباشرة ≠ طلب عضو) | لا |
| إضافة عدة أبناء تحت أب | إضافة جماعية | `add_children_group` | `parent_person_id` + صفوف أبناء + قرارات per-row | اختيارية | إدارة + مندوب | جدول مطابقة — غير مبني | فقط الصفوف المحسومة | لا إنشاء/ربط لغير المحسوم | ممنوع حفظ مجموعة عمياء | `missing_*` | لا |
| تصحيح مجموعة حقول لأبناء | إضافة/تصحيح جماعي | `group_correction` | `parent_person_id` + تغييرات لكل `person_id` | اختيارية | إدارة | محرر مجموعة — غير مبني | حسب العمليات الفرعية | غير المحسوم | بعد حسم كل صف | `missing_*` | لا |
| تصحيح فرع شخص | علاقة/فرع | `branch_correction` | `person_id`, `branch_key_new` | اختيارية | إدارة | جزئي: تعديل فرع **الطلب** لا الشخص | غير مكتمل كتصحيح شجرة | — | نقل فرع آمن — غير مكتمل كعملية عضو | `partial` / `missing_save` | لا |
| دمج/تعارض هوية | هوية | `identity_merge_review` | `person_ids[]` + قرار | اختيارية | إدارة | مسار دمج — غير كطلب عضو عادي | روابط الدمج المعاينة | لا دمج صامت | dry-run | `missing_create` (حارس يمنع) | لا |
| تصحيح مسار نسب | علاقة | `lineage_path_correction` | آباء بالـUUID | اختيارية | إدارة | غير مبني | السلسلة المعاينة | لا بناء من أسماء فقط | غير مبني | `missing_*` | لا |
| طلب قديم: نص حر / `tree_card` مصنّف خطأ | — | *(مستنتج عند الاستقبال فقط)* | نص + مطابقة يدوية/استعادة | هو الجسم | إدارة | **Safe Review** | حسب العملية المستعادة بعد موافقة | لا import ابن أعمى | Recovery → محرر العملية إن أمكن | `legacy_only` | لا |

### ملحق خارج قمع التصحيح (لا شجرة «ماذا تريد تصحيحه؟»)

| نية RX اليوم | kind / خدمة | ملاحظة |
|--------------|-------------|--------|
| مناسبة / حالة صحية / وفاة | `event_card` | مسارات نشر المناسبات — خارج هذا المحرك |
| ذكرى | `memory_card` | Memory Service |
| بطاقة خاصة | `special_card` | Cards / CMS |
| جوال (تطبيق) | — | Cross-Client لاحقًا؛ لا شاشات جوال في هذه الخطوة |

### أكبر فجوة — `tree_edit` العام — **مُغلقة للإرسال الجديد (2026-08-17)**

العضو **لا** يُنشئ بعد طلبًا مختلطًا بلا `operation`. الواجهة والـhash `#send-tree-edit` يوجّهان لخيارات live، و`AlzidanHomeRequestCreate` يرفض `tree_edit` بلا عملية منظمة.

```text
محاولة tree_edit العام
        ↓
إعادة توجيه / رفض GENERIC_TREE_EDIT_CLOSED
        ↓
name_correction · phone_correction · birth_date_correction · city_correction · parent_change · reorder_children
```

- ممنوع إصلاح ذلك بـ parser أذكى على النص.
- الطلبات القديمة بلا operation تبقى `legacy_only` / مراجعة آمنة.
- kind=`tree_edit` يبقى للتوافق مع DB للعمليات live التي تحمل `operation`.

### بعد إقفال الكتالوج — شريحة UX التالية (مجدولة · ليست الآن)

```text
ماذا تريد؟ → تصحيح في الشجرة → إعادة ترتيب الأبناء
```

تعيد استخدام المسار `live` الحالي فقط. بقية العمليات تبقى في كتالوج الإدارة كقيد تطوير / missing حتى تكتمل create+editor+preview+save.

---

## 2.1) `reorder_children` — تعريف أدق

| طبقة | الدور |
|------|--------|
| مدخلات المندوب (أسماء / أرقام ترتيب) | **للمطابقة فقط** |
| بعد مطابقة آمنة | `parent_person_id` + `ordered_children: [person_id, …]` |
| مرجع الحفظ | **الهوية فقط** — ليس الاسم |
| فشل مطابقة أي فرد | `needs_review` — **لا تخمين** |

---

## 2.2) عمليات الجوال — ثلاثة مسارات منفصلة

| عملية | متى | محرر |
|-------|-----|------|
| `phone_correction` | شخص معروف؛ تغيير رقمه | تصحيح جوال |
| `phone_link` | رقم حر → ربط بشخص موجود | ربط |
| `phone_duplicate_conflict` | الرقم لشخص آخر | تعارض — لا يمرّ بمحرر التصحيح العادي |

---

## 2.3) إضافة/تصحيح مجموعة — مطابقة قبل أي حفظ

مثال 10 أرقام:

| نتيجة المطابقة | الإجراء |
|----------------|--------|
| موجود تحت الأب / قابل للربط | جاهز للربط (بعد موافقة) |
| غير موجود | يحتاج إنشاء (قرار صريح) |
| مرتبط بشخص آخر | تعارض — لا يُغلق الطلب |
| غير صالح | مراجعة — لا يُغلق الطلب |

> العملية الجماعية **لا تُغلق** حتى تُحسم كل الصفوف. ممنوع «حفظ المجموعة» دفعة عمياء.

---

## 3) موجّه الإدارة (Router)

```text
طلب تصحيح
    │
 تثبيت operation (من العقد — لا من تفسير النص)
    │
┌───────────┼───────────┐
↓           ↓           ↓
شخص      مجموعة      علاقة/هوية
    │           │           │
    └───────────┼───────────┘
                ↓
    Preview: ما يتغير + ما لن يتغير
                ↓
         تحقق نهائي
                ↓
         مسار الحفظ = نفس الأثر المعاين
                ↓
         سجل العملية
```

| عائلة | عمليات |
|-------|--------|
| محرر شخص | `name_correction`, `parent_fields_correction`, `branch_correction` |
| محرر جوال | `phone_correction` · `phone_link` · `phone_duplicate_conflict` (شاشات منفصلة) |
| محرر مجموعة | `reorder_children`, `group_correction`, `add_children_group` |
| محرر علاقة | `parent_change`, `move_person`, `lineage_path_correction` |
| مراجعة هوية | `identity_merge_review` |
| إضافة فرد | `add_person` / `tree_card` — **خارج Router التصحيح كافتراضي** |

---

## 4) معاينة الأثر + «ما لن يتغير» — ملزم

كل Preview يعرض قسمين:

1. **يتغير:** …  
2. **لن يتغير:** …

| عملية | يتغير (حد أدنى) | لن يتغير (مثال إلزامي) |
|-------|------------------|-------------------------|
| `reorder_children` | ترتيب الأبناء تحت الأب فقط | الأسماء، الجوالات، تواريخ الميلاد، الآباء، الهويات |
| `parent_change` / `move_person` | علاقة الأب، مسار نسب، فرع مستنتج إن لزم | حقول غير معروضة في المعاينة |
| `phone_correction` | الجوال | الاسم، الأب، الترتيب، الميلاد ما لم يُعاين |
| `add_children_group` | حسب قرارات الصفوف المحسومة فقط | لا إنشاء/ربط لصفوف غير محسومة |
| `name_correction` | الاسم | الجوال/الأب ما لم يُعاين |
| `identity_merge_review` | روابط الدمج المعاينة | لا دمج صامت لطرف غير معروض |

**قاعدة الحفظ:** مسار الحفظ ينفّذ **نفس الأثر المعاين فقط** — لا تسريب تعديلات جانبية من محرر عام.

---

## 5) المرسل اليوم ↔ الفجوة (محدَّث بعد إثبات live)

| مصدر | اليوم | الحالة / الفجوة |
|------|-------|-----------------|
| RX ويب | `add_person` / `tree_card` | **`live`** |
| RX ويب | `reorder_children` عبر نموذج مرتّب + عقد | **`live`** — Web → مطابقة/أب → Preview → `birth_order` → حفظ → اعتماد |
| RX ويب | `tree_edit` عام (حقول مختلطة بلا `operation`) | **مغلق للإرسال الجديد** — توجيه لعمليات live؛ الأرشيف → مراجعة آمنة |
| جوال ≥ 2026-08-12 | غالبًا `tree_edit` نص/حقول | خارج الشريحة؛ نفس فجوة غياب `operation` |
| جوال قديم | نص تصحيح + أحيانًا `tree_card` | **`legacy_only`** → Safe Review (مثل طلب عبدالرحمن قبل الاستعادة) |
| مندوب | كتابة مباشرة + استقبال طلبات منظمة | `reorder_children` بنفس العقد عند الصلاحية؛ `tree_edit` العام بلا تطبيق شجرة |
| إدارة | Router: إضافة فرد · ترتيب · Safe Review | لا محررات لباقي §2 بعد |

**إثبات `reorder_children` (2026-08-15):** مسار حي شمل استعادة طلب قديم → تحديد الأب → مطابقة 7/7 → Preview → حفظ `birth_order` للأبناء السبعة. المطلوب كان إعادة ترتيب لا إضافة أشخاص.

---

## 6) دراسة حالة أرشيف (لاحم — ترتيب أسماء) — لماذا نحتاج العملية لا النص

| | |
|--|--|
| ليس `tree_card` | نية ترتيب/تصحيح مجموعة |
| لا يُحفظ بالواجهة الحالية | محرر إضافة فرد = أداة خاطئة |
| المسار الصحيح | مطابقة → `reorder_children` بالـ`person_id` → Preview (ترتيب فقط) → حفظ |
| بلا مطابقة آمنة | `needs_review` — لا تخمين |
| إعادة JSON بطاقة | **ممنوع** |

---

## 7) سياسة الصفوف القديمة + اختبار Legacy

| صنف | إجراء لاحق |
|-----|------------|
| `tree_card` + نص «طلب تصحيح بيانات من تطبيق…» | عرض/تصنيف كتصحيح؛ لا import ابن |
| `tree_edit` بلا منفّذ | Router أو مراجعة يدوية صريحة |
| `tree_card` canonical | مسار إضافة الفرد |
| ترتيب أسماء بلا أب/هويات | `needs_review` |

إصلاح الأرشيف: تشخيص → idempotent → **لا UPDATE أعمى** · **لا إعادة تفسير تلقائي خاطئ**.

### اختبار قبول ثالث — إلزامي

```text
Legacy Request
      ↓
Classification
      ↓
Safe Recovery / Review
      ↓
Admin
      ↓
NO automatic reinterpretation as tree_card apply
```

الهدف: **القديم لا ينهار، والجديد لا يُفسَّر خطأ.**

---

## 8) نطاق التنفيذ الحالي — معتمد

### السلسلة المعتمدة لهذه الشريحة

```text
المصدر الرئيسي: الويب
      ↓
إنشاء طلب التصحيح (Correction Contract)
      ↓
approval_requests
      ↓
الاستقبال: الإدارة + المناديب
      ↓
Correction Router
      ↓
المحرر المناسب للعملية
      ↓
Preview (ما يتغير / ما لن يتغير)
      ↓
الحفظ في مسار الشجرة الصحيح
```

### أدوار الأطراف

| الطرف | دوره في هذا التنفيذ |
|-------|---------------------|
| **الويب** | المصدر الرئيسي لإنشاء طلبات التصحيح |
| **الإدارة** | استقبال، تصنيف، مراجعة، معاينة، حفظ |
| **المناديب** | استقبال/تنفيذ **المسار المخصص لهم** حسب نوع الطلب والصلاحية — بنفس العقد؛ ليسوا نسخة مصغّرة من الإدارة ولا يعيدون كتابة الطلب كنص حر |
| **الجوال** | **خارج هذا التنفيذ** — لا تعديل عليه الآن |

### اختبار القبول لهذه الشريحة

```text
Web → Admin/Delegate (حسب الصلاحية) → reorder_children → Preview → Save → Tree
```

+ اختبار Legacy → Safe Review (بلا إعادة تفسير كـ `tree_card` apply).

**Mobile → … غير مطلوب لإغلاق هذه الشريحة.** يُفتح لاحقًا عندما يأتي تحسين يحتاجه — مع العودة لقاعدة Cross-Client قبل اعتبار العملية مكتملة عالميًا.

### قاعدة المندوب في هذه الشريحة

- إن كانت العملية من صلاحية المندوب: يصل بـ `operation` + بيانات منظمة، وينفّذ/يحيل عبر **نفس العقد**.
- ممنوع أن يتحول طلب منظم عند المندوب إلى «نص جديد» يُعاد إدخاله.
- من يستلم (`Admin` vs `Delegate`) يُحدَّد بالصلاحية ونوع العملية — لا بتخمين من الرسالة.

### ما يتوقف / أول شريحة عند الفتح

**يتوقف الآن**

- ترقيع «تعديل كامل» لكل التصحيحات.
- قبول `tree_edit` = تطبيق شجرة ضمني.
- إنشاء تصحيح جديد تحت `tree_card`.
- إصلاح طرف واحد فقط لنفس العملية.
- إدخال الجوال في نطاق هذه الشريحة.

**شريحة `reorder_children` — منفَّذة ومثبتة على الويب (live)**

1. Correction Contract + بوابة إنشاء ويب.
2. Admin/Delegate Router + محرر مجموعة + Preview (يتغير/لن يتغير).
3. حفظ `birth_order` + تحقق + اعتماد حالة الطلب.
4. Legacy → Safe Review (بلا إعادة تفسير كـ `tree_card` apply).

**§2.0 مقفل.** شريحة التنفيذ التالية (ويب فقط):

```text
ماذا تريد؟ → تصحيح في الشجرة → إعادة ترتيب الأبناء
→ إنشاء canonical → Admin/Delegate Router → محرر → مطابقة → Preview → birth_order → تحقق العرض
```

بعدها فقط: تفكيك `tree_edit` العام → عمليات حقيقية شريحةً شريحة. **لا** توسيع قائمة عضو بعمليات `missing_*`/`partial`.

**لا يبدأ الآن**

- واجهة خيارات كاملة · جوال Cross-Client · schema بلا حاجة مثبتة · محرر عام بدل العملية.

---

## 9) معيار النجاح (هذه الشريحة)

- طلب التصحيح من **الويب** يصل بـ `operation` أو `needs_review`/`unsupported` بسبب صريح.
- الإدارة (والمندوب عند الصلاحية) لا تفتح أداة لا تخص العملية.
- **لا حفظ يغيّر غير ما عاينته الجهة المنفّذة.**
- `tree_card` للإضافة فقط؛ التصحيح لا يُخفى داخله.
- المندوب لا يعيد كتابة طلب منظم كنص حر.
- اختبار Web→Admin/Delegate→Tree ناجح؛ Legacy→Safe Review ناجح.
- **عدم** اشتراط Mobile لإغلاق هذه الشريحة؛ عند فتح Mobile لاحقًا يُعاد Cross-Client كاملًا قبل إعلان الاكتمال العالمي.
- المجموعة لا تُغلق وفيها صف تعارض/غير صالح/غير مطابق.