-- Readable source note for literal-aware helpers.
-- Operator install path (CREATE OR REPLACE only, no pg_proc):
--   20260809_admin_sql_workspace_run_v2.sql
--   COPY-ME-admin-sql-workspace-run-v2.sql
-- Preset: maint.sql_workspace_run_v2
--
-- Bodies live in the v2 install file (stripper + split + execute_v1 + run_v2).
select 1;
