(function () { const SUPABASE_URL = "https://wbskjfdqpugnwvrykqcn.supabase.co"; const SUPABASE_ANON_KEY = "sb_publishable_JhgwBIXhs6z4yBZOoE2EqA_UlzjzW9c"; let sbClient = null; const sbStatus = document.getElementById("sb-status"); const adminLockedHint = document.getElementById("admin-locked-hint"); const adminProtectedInline = null; const adminProtectedSections = document.getElementById("admin-protected-sections"); const alertEl = document.getElementById("alert"); const adminUsername = document.getElementById("admin-username"); const adminPassword = document.getElementById("admin-password"); const adminLoginControls = document.getElementById("admin-login-controls"); const adminLoginFields = document.getElementById("admin-login-fields"); const adminCurrentUser = document.getElementById("admin-current-user"); const adminLoginBtn = document.getElementById("admin-login"); const adminLogoutBtn = document.getElementById("admin-logout"); const adminRefreshBtn = document.getElementById("admin-refresh"); const adminEnableNotifsBtn = document.getElementById("admin-enable-notifs"); const adminForgotBtn = document.getElementById("admin-forgot"); const filterKind = document.getElementById("filter-kind"); const filterStatus = document.getElementById("filter-status"); const requestSearchInput = document.getElementById("request-search"); const requestsPageSizeSelect = document.getElementById("requests-page-size"); const requestsPrevPageBtn = document.getElementById("requests-prev-page"); const requestsNextPageBtn = document.getElementById("requests-next-page"); const requestsPageInfo = document.getElementById("requests-page-info"); const requestsBody = document.getElementById("requests-body"); let requestsAllRows = []; let requestsCurrentPage = 1; const ADMIN_TOKEN_KEY = "alzidan_admin_token_v1"; const ADMIN_TOKEN_SESSION_KEY = "alzidan_admin_token_session_v1"; let adminToken = ""; const ADMIN_NOTIF_LAST_KEY = "alzidan_admin_notif_last_pending_v1"; const ADMIN_EMAIL_KEY = "alzidan_admin_notify_email_v1"; const ADMIN_EMAIL_PENDING_KEY = "alzidan_admin_email_pending_v1"; const ADMIN_EMAIL_AUDIT_KEY = "alzidan_admin_email_audit_v1"; const ADMIN_EMAIL_AUTO_OPEN_KEY = "alzidan_admin_email_auto_open_v1"; const ADMIN_EMAIL_LAST_AUDIT_KEY = "alzidan_admin_email_last_audit_v1"; let lastNotifiedPendingKey = ""; let lastEmailedAuditKey = ""; let didInitialPendingSync = false; let didInitialAuditSync = false; let pendingPollTimer = null; const copyTreeSqlBtn = document.getElementById("copy-tree-sql"); const treeSqlEl = document.getElementById("tree-sql"); const refreshDelegateAuditBtn = document.getElementById("refresh-delegate-audit"); const delegatesListEl = document.getElementById("delegates-list"); const delegateAuditSelect = document.getElementById("delegate-audit-select"); const delegateAuditStatus = document.getElementById("delegate-audit-status"); const delegateAuditBody = document.getElementById("delegate-audit-body"); const delegatePermsStatus = document.getElementById("delegate-perms-status"); const delegatePermsTreeBtn = document.getElementById("delegate-perms-tree"); const delegatePermsEventsBtn = document.getElementById("delegate-perms-events"); const delegatePermsBothBtn = document.getElementById("delegate-perms-both"); const delegatePermsDisableBtn = document.getElementById("delegate-perms-disable"); const delegateDeleteBtn = document.getElementById("delegate-delete-btn"); const copyViewsSqlBtn = document.getElementById("copy-views-sql"); const viewsSqlEl = document.getElementById("views-sql"); const refreshViewsStatsBtn = document.getElementById("refresh-views-stats"); const viewsStatsEl = document.getElementById("views-stats"); const refreshRequestsStatsBtn = document.getElementById("refresh-requests-stats"); const requestsStatsEl = document.getElementById("requests-stats"); const copyDelegatesSqlBtn = document.getElementById("copy-delegates-sql"); const delegatesSqlEl = document.getElementById("delegates-sql"); const copyRequestEditSqlBtn = document.getElementById("copy-request-edit-sql"); const requestEditSqlEl = document.getElementById("request-edit-sql"); const copyLahmSalehFixSqlBtn = document.getElementById("copy-lahm-saleh-fix-sql"); const lahmSalehFixSqlEl = document.getElementById("lahm-saleh-fix-sql"); const delegateAuditDetails = document.getElementById("delegate-audit-details"); const adminNotifyEmail = document.getElementById("admin-notify-email"); const adminEmailNotifPending = document.getElementById("admin-email-notif-pending"); const adminEmailNotifAudit = document.getElementById("admin-email-notif-audit"); const adminEmailAutoOpen = document.getElementById("admin-email-auto-open"); const adminEmailTestBtn = null; const adminEmailStatus = document.getElementById("admin-email-status"); const treeImportDownloadBtn = document.getElementById("tree-import-download"); const treeImportWhatsappBtn = document.getElementById("tree-import-whatsapp"); const treeImportCopyBtn = document.getElementById("tree-import-copy"); const treeImportFileEl = document.getElementById("tree-import-file"); const treeImportRunBtn = document.getElementById("tree-import-run"); const treeImportTemplateEl = document.getElementById("tree-import-template"); const treeImportStatusEl = document.getElementById("tree-import-status"); const waFileEl = document.getElementById("wa-file"); const waFileBuildBtn = document.getElementById("wa-file-build"); const waFileCopyBtn = document.getElementById("wa-file-copy"); const waFileOpenBtn = document.getElementById("wa-file-open"); const waFileTextEl = document.getElementById("wa-file-text"); const waFileStatusEl = document.getElementById("wa-file-status"); const batchTreeBranch = document.getElementById("batch-tree-branch"); const batchTreeInput = document.getElementById("batch-tree-input"); const batchTreeSql = document.getElementById("batch-tree-sql"); const batchTreeBuild = document.getElementById("batch-tree-build"); const batchTreeCopy = document.getElementById("batch-tree-copy"); const batchTreeDownload = document.getElementById("batch-tree-download"); const batchTreeSummary = document.getElementById("batch-tree-summary"); const batchTreeStatus = document.getElementById("batch-tree-status"); const batchTreeNotes = document.getElementById("batch-tree-notes"); const treeCardEditDialog = document.getElementById("tree-card-edit-dialog"); const treeCardEditForm = document.getElementById("tree-card-edit-form"); const treeCardEditCancel = document.getElementById("tree-card-edit-cancel"); const treeCardEditError = document.getElementById("tree-card-edit-error"); const treeCardRelations = document.getElementById("tree-card-relations"); const treeCardAddRelation = document.getElementById("tree-card-add-relation"); let treeCardEditRow = null; const sourceTreeBranch = document.getElementById("source-tree-branch"); const sourceTreeLoad = document.getElementById("source-tree-load"); const sourceTreeNew = document.getElementById("source-tree-new"); const sourceTreeList = document.getElementById("source-tree-list"); const sourceTreeForm = document.getElementById("source-tree-form"); const sourceTreeId = document.getElementById("source-tree-id"); const sourceTreePersonId = document.getElementById("source-tree-person-id"); const sourceTreeParent = document.getElementById("source-tree-parent"); const sourceTreeName = document.getElementById("source-tree-name"); const sourceTreeOrder = document.getElementById("source-tree-order"); const sourceTreeExtraChildren = document.getElementById("source-tree-extra-children"); const sourceTreeAddExtraChild = document.getElementById("source-tree-add-extra-child"); const sourceTreeBirthG = document.getElementById("source-tree-birth-g"); const sourceTreeBirthH = document.getElementById("source-tree-birth-h"); const sourceTreeAge = document.getElementById("source-tree-age"); const sourceTreeDeathG = document.getElementById("source-tree-death-g"); const sourceTreeDeathH = document.getElementById("source-tree-death-h"); const sourceTreeCity = document.getElementById("source-tree-city"); const sourceTreeArea = document.getElementById("source-tree-area"); const sourceTreeDeceased = document.getElementById("source-tree-deceased"); const sourceTreeDelete = document.getElementById("source-tree-delete"); const sourceTreeStatus = document.getElementById("source-tree-status"); let sourceTreeRows = []; const eventsSourceLoad = document.getElementById("events-source-load"); const eventsSourceList = document.getElementById("events-source-list"); const eventsSourceForm = document.getElementById("events-source-form"); const eventsSourceId = document.getElementById("events-source-id"); const eventsSourceBranch = document.getElementById("events-source-branch"); const eventsSourceType = document.getElementById("events-source-type"); const eventsSourcePerson = document.getElementById("events-source-person"); const bannerGeneralForm = document.getElementById("banner-general-form"); const bannerGeneralBranch = document.getElementById("banner-general-branch"); const bannerGeneralShowDays = document.getElementById("banner-general-show-days"); const bannerGeneralText = document.getElementById("banner-general-text"); const bannerGeneralClear = document.getElementById("banner-general-clear"); const bannerGeneralStatus = document.getElementById("banner-general-status"); const bannerMessagesLoad = document.getElementById("banner-messages-load"); const bannerMessagesNew = document.getElementById("banner-messages-new"); const bannerMessagesList = document.getElementById("banner-messages-list"); const bannerMessagesForm = document.getElementById("banner-messages-form"); const bannerMessagesId = document.getElementById("banner-messages-id"); const bannerMessagesBranch = document.getElementById("banner-messages-branch"); const bannerMessagesShowDays = document.getElementById("banner-messages-show-days"); const bannerMessagesActive = document.getElementById("banner-messages-active"); const bannerMessagesText = document.getElementById("banner-messages-text"); const bannerMessagesDelete = document.getElementById("banner-messages-delete"); const bannerMessagesStatus = document.getElementById("banner-messages-status"); const adminTickerSpeed = document.getElementById("admin-ticker-speed"); const adminTickerSpeedSave = document.getElementById("admin-ticker-speed-save"); const adminTickerMobileSpeed = document.getElementById("admin-ticker-mobile-speed"); let bannerMessagesRows = []; const eventsSourceTitle = document.getElementById("events-source-title"); const eventsSourceGregorian = document.getElementById("events-source-gregorian"); const eventsSourceText = document.getElementById("events-source-text"); const eventsSourceImage = document.getElementById("events-source-image"); const eventsSourceVideo = document.getElementById("events-source-video"); const eventsSourceDelete = document.getElementById("events-source-delete"); const eventsSourceStatus = document.getElementById("events-source-status"); let eventsSourceRows = []; const TREE_SETUP_SQL = `
create table if not exists public.tree_children ( id bigserial primary key, branch_key text not null, parent_name text not null, name text not null, child_name text null, parent text null, person_id uuid null default gen_random_uuid(), parent_person_id uuid null, birth_date_g date null, birth_date_h text null, birth_year int null, birth_order int null, city text null, area text null, is_deceased boolean null default false, deceased boolean null default false, created_at timestamptz not null default now(), created_by uuid null
); alter table public.tree_children add column if not exists id bigserial;
alter table public.tree_children add column if not exists branch_key text;
alter table public.tree_children add column if not exists parent_name text;
alter table public.tree_children add column if not exists name text;
alter table public.tree_children add column if not exists child_name text;
alter table public.tree_children add column if not exists parent text;
alter table public.tree_children add column if not exists person_id uuid;
alter table public.tree_children add column if not exists parent_person_id uuid;
alter table public.tree_children add column if not exists birth_date_g date;
alter table public.tree_children add column if not exists birth_date_h text;
alter table public.tree_children add column if not exists birth_year int;
alter table public.tree_children add column if not exists birth_order int;
alter table public.tree_children add column if not exists death_date_g date;
alter table public.tree_children add column if not exists death_date_h text;
alter table public.tree_children add column if not exists city text;
alter table public.tree_children add column if not exists area text;
alter table public.tree_children add column if not exists is_deceased boolean;
alter table public.tree_children add column if not exists deceased boolean;
alter table public.tree_children add column if not exists created_at timestamptz;
alter table public.tree_children add column if not exists created_by uuid;
alter table public.tree_children alter column person_id set default gen_random_uuid(); update public.tree_children
set person_id = gen_random_uuid()
where person_id is null; update public.tree_children c
set parent_person_id = matches.person_id
from ( select child.id, min(parent.person_id::text)::uuid as person_id from public.tree_children child join public.tree_children parent on parent.branch_key = child.branch_key and coalesce(parent.child_name, parent.name) = coalesce(child.parent_name, child.parent) and parent.person_id is not null where child.parent_person_id is null group by child.id having count(distinct parent.person_id) = 1
) matches
where c.id = matches.id; create unique index if not exists tree_children_person_id_key on public.tree_children (person_id) where person_id is not null; update public.tree_children
set birth_order = case when coalesce(child_name, name) = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/دوخي' then 1 when coalesce(child_name, name) = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/حضيري' then 2 when coalesce(child_name, name) = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/عبدالله' then 3 when coalesce(child_name, name) = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/عبيد' then 4 when coalesce(child_name, name) = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/زيد' then 5 when coalesce(child_name, name) in ( 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/مبارك', 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم/مبارك وزيد' ) then 6 else birth_order
end
where branch_key = 'مزيد' and parent_name = 'مزيد بن مطلق بن زيدان/صلف/دوخي/سالم'; create unique index if not exists tree_children_parent_birth_order_key on public.tree_children (branch_key, parent_name, birth_order) where birth_order is not null; alter table public.tree_children enable row level security; drop policy if exists "tree_children_select_all" on public.tree_children;
create policy "tree_children_select_all" on public.tree_children for select using (true); revoke insert, update, delete on table public.tree_children from anon, authenticated;
grant select on table public.tree_children to anon, authenticated; create or replace function public.tree_delegate_allowed_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text
) returns boolean
language sql
stable
security definer
set search_path = public
as $$ select exists ( select 1 from public.approval_requests r where r.kind = 'tree_delegate' and regexp_replace(btrim(coalesce(r.branch_key, '')), '\s+', ' ', 'g') = regexp_replace(btrim(coalesce(p_branch_key, '')), '\s+', ' ', 'g') and regexp_replace(btrim(coalesce(r.phone, '')), '\s+', '', 'g') = regexp_replace(btrim(coalesce(p_phone, '')), '\s+', '', 'g') and lower(regexp_replace(btrim(coalesce(r.email, '')), '\s+', '', 'g')) = lower(regexp_replace(btrim(coalesce(p_email, '')), '\s+', '', 'g')) and r.status = 'approved' and nullif(trim(coalesce(r.secret_hash, '')), '') is not null and nullif(trim(coalesce(p_secret_hash, '')), '') is not null and r.secret_hash = p_secret_hash limit 1 )
$$; create or replace function public.tree_audit_log_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text, p_payload jsonb
) returns void
language plpgsql
security definer
set search_path = public
as $$
declare v_actor_name text; v_req_id text;
begin select r.name into v_actor_name from public.approval_requests r where r.kind = 'tree_delegate' and regexp_replace(btrim(coalesce(r.branch_key, '')), '\s+', ' ', 'g') = regexp_replace(btrim(coalesce(p_branch_key, '')), '\s+', ' ', 'g') and regexp_replace(btrim(coalesce(r.phone, '')), '\s+', '', 'g') = regexp_replace(btrim(coalesce(p_phone, '')), '\s+', '', 'g') and lower(regexp_replace(btrim(coalesce(r.email, '')), '\s+', '', 'g')) = lower(regexp_replace(btrim(coalesce(p_email, '')), '\s+', '', 'g')) and r.status = 'approved' and nullif(trim(coalesce(r.secret_hash, '')), '') is not null and nullif(trim(coalesce(p_secret_hash, '')), '') is not null and r.secret_hash = p_secret_hash order by r.created_at desc limit 1; v_req_id := 'AUD-' || upper(substr(md5(random()::text || clock_timestamp()::text), 1, 4)) || '-' || upper(substr(md5(random()::text || clock_timestamp()::text), 5, 4)); insert into public.approval_requests ( request_id, kind, branch_key, name, phone, email, secret_hash, message, status, created_at ) values ( v_req_id, 'tree_audit', p_branch_key, v_actor_name, p_phone, p_email, p_secret_hash, coalesce(p_payload, '{}'::jsonb)::text, 'approved', now() );
exception when others then return;
end;
$$; create or replace function public.tree_children_insert_v1( p_branch_key text, p_parent_name text, p_child_name text, p_phone text, p_email text, p_secret_hash text, p_row jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_id bigint; v_person_id uuid; v_parent_person_id uuid; v_child_base text; v_deceased boolean; v_birth_order int;
begin if not public.tree_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; v_deceased := case when p_row ? 'is_deceased' then (p_row->>'is_deceased')::boolean when p_row ? 'deceased' then (p_row->>'deceased')::boolean else null end; v_birth_order := nullif(p_row->>'birth_order', '')::int; v_person_id := nullif(p_row->>'person_id', '')::uuid; v_parent_person_id := nullif(p_row->>'parent_person_id', '')::uuid; v_child_base := nullif(btrim(regexp_replace(coalesce(p_child_name, ''), '^.*/', '')), ''); if v_birth_order is not null and v_birth_order< 1 then raise exception 'birth_order_invalid'; end if; if v_parent_person_id is null then select min(c.person_id::text)::uuid into v_parent_person_id from public.tree_children c where c.branch_key = p_branch_key and coalesce(c.child_name, c.name) = p_parent_name having count(distinct c.person_id) = 1; end if; select c.id into v_id from public.tree_children c where c.branch_key = p_branch_key and ( (v_person_id is not null and c.person_id = v_person_id) or ( v_person_id is null and (c.parent_name = p_parent_name or c.parent = p_parent_name) and (c.name = p_child_name or c.child_name = p_child_name) ) ) order by c.id desc limit 1; if v_person_id is null and exists ( select 1 from public.tree_children c where c.branch_key = p_branch_key and ( ( v_parent_person_id is not null and c.parent_person_id = v_parent_person_id ) or ( v_parent_person_id is null and coalesce(c.parent_name, c.parent) = p_parent_name ) ) and btrim(regexp_replace(coalesce(c.child_name, c.name, ''), '^.*/', '')) = v_child_base ) then raise exception 'child_already_exists'; end if; if v_birth_order is not null and exists ( select 1 from public.tree_children c where c.branch_key = p_branch_key and c.parent_name = p_parent_name and c.birth_order = v_birth_order and (v_id is null or c.id<>v_id) ) then raise exception 'birth_order_conflict'; end if; if v_id is not null then update public.tree_children c set person_id = coalesce(c.person_id, v_person_id, gen_random_uuid()), parent_person_id = coalesce(v_parent_person_id, c.parent_person_id), birth_date_g = nullif(p_row->>'birth_date_g', '')::date, birth_date_h = nullif(p_row->>'birth_date_h', ''), birth_year = nullif(p_row->>'birth_year', '')::int, birth_order = v_birth_order, city = nullif(p_row->>'city', ''), area = nullif(p_row->>'area', ''), is_deceased = coalesce(v_deceased, c.is_deceased), deceased = coalesce(v_deceased, c.deceased) where c.id = v_id; perform public.tree_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'tree_audit', 'op', 'upsert_update', 'branch_key', p_branch_key, 'parent_name', p_parent_name, 'child_name', p_child_name, 'row', coalesce(p_row, '{}'::jsonb), 'at', now()::timestamptz ) ); return true; end if; insert into public.tree_children ( branch_key, parent_name, parent, name, child_name, person_id, parent_person_id, birth_date_g, birth_date_h, birth_year, birth_order, city, area, is_deceased, deceased, created_at ) values ( p_branch_key, p_parent_name, p_parent_name, p_child_name, p_child_name, coalesce(v_person_id, gen_random_uuid()), v_parent_person_id, nullif(p_row->>'birth_date_g', '')::date, nullif(p_row->>'birth_date_h', ''), nullif(p_row->>'birth_year', '')::int, v_birth_order, nullif(p_row->>'city', ''), nullif(p_row->>'area', ''), coalesce(v_deceased, false), coalesce(v_deceased, false), coalesce(nullif(p_row->>'created_at', '')::timestamptz, now()) ); perform public.tree_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'tree_audit', 'op', 'insert', 'branch_key', p_branch_key, 'parent_name', p_parent_name, 'child_name', p_child_name, 'row', coalesce(p_row, '{}'::jsonb), 'at', now()::timestamptz ) ); return true;
end;
$$; create or replace function public.tree_children_update_v1( p_branch_key text, p_parent_name text, p_child_name text, p_phone text, p_email text, p_secret_hash text, p_patch jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_deceased boolean; v_birth_order int; v_id bigint;
begin if not public.tree_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; v_deceased := case when p_patch ? 'is_deceased' then (p_patch->>'is_deceased')::boolean when p_patch ? 'deceased' then (p_patch->>'deceased')::boolean else null end; v_birth_order := case when p_patch ? 'birth_order' then nullif(p_patch->>'birth_order', '')::int else null end; if v_birth_order is not null and v_birth_order< 1 then raise exception 'birth_order_invalid'; end if; select c.id into v_id from public.tree_children c where c.branch_key = p_branch_key and ( ( nullif(p_patch->>'person_id', '') is not null and c.person_id = nullif(p_patch->>'person_id', '')::uuid ) or ( nullif(p_patch->>'person_id', '') is null and (c.parent_name = p_parent_name or c.parent = p_parent_name) and (c.name = p_child_name or c.child_name = p_child_name) ) ) order by c.id desc limit 1; if v_id is null then return false; end if; if p_patch ? 'birth_order' and v_birth_order is not null and exists ( select 1 from public.tree_children c where c.branch_key = p_branch_key and c.parent_name = p_parent_name and c.birth_order = v_birth_order and c.id<>v_id ) then raise exception 'birth_order_conflict'; end if; update public.tree_children c set birth_date_g = case when p_patch ? 'birth_date_g' then nullif(p_patch->>'birth_date_g', '')::date else c.birth_date_g end, birth_date_h = case when p_patch ? 'birth_date_h' then nullif(p_patch->>'birth_date_h', '') else c.birth_date_h end, birth_year = case when p_patch ? 'birth_year' then nullif(p_patch->>'birth_year', '')::int else c.birth_year end, birth_order = case when p_patch ? 'birth_order' then v_birth_order else c.birth_order end, city = case when p_patch ? 'city' then nullif(p_patch->>'city', '') else c.city end, area = case when p_patch ? 'area' then nullif(p_patch->>'area', '') else c.area end, is_deceased = coalesce(v_deceased, c.is_deceased), deceased = coalesce(v_deceased, c.deceased) where c.branch_key = p_branch_key and c.id = v_id; if found then perform public.tree_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'tree_audit', 'op', 'update', 'branch_key', p_branch_key, 'parent_name', p_parent_name, 'child_name', p_child_name, 'patch', coalesce(p_patch, '{}'::jsonb), 'at', now()::timestamptz ) ); end if; return found;
end;
$$; create or replace function public.tree_children_delete_v1( p_branch_key text, p_parent_name text, p_child_name text, p_phone text, p_email text, p_secret_hash text
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
begin if not public.tree_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; delete from public.tree_children c where c.branch_key = p_branch_key and (c.parent_name = p_parent_name or c.parent = p_parent_name) and (c.name = p_child_name or c.child_name = p_child_name); if found then perform public.tree_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'tree_audit', 'op', 'delete', 'branch_key', p_branch_key, 'parent_name', p_parent_name, 'child_name', p_child_name, 'at', now()::timestamptz ) ); end if; return found;
end;
$$; create or replace function public.tree_children_delete_by_id_v1( p_branch_key text, p_person_id uuid, p_phone text, p_email text, p_secret_hash text
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_parent_name text; v_child_name text;
begin if p_person_id is null then return false; end if; if not public.tree_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; select c.parent_name, coalesce(c.child_name, c.name) into v_parent_name, v_child_name from public.tree_children c where c.branch_key = p_branch_key and c.person_id = p_person_id limit 1; if v_child_name is null then return false; end if; delete from public.tree_children c where c.branch_key = p_branch_key and c.person_id = p_person_id; if found then perform public.tree_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'tree_audit', 'op', 'delete_by_id', 'branch_key', p_branch_key, 'person_id', p_person_id, 'parent_name', v_parent_name, 'child_name', v_child_name, 'at', now()::timestamptz ) ); end if; return found;
end;
$$; drop function if exists public.tree_children_wipe_branch_v1(text, text, text, text); grant execute on function public.tree_delegate_allowed_v1(text, text, text, text) to anon, authenticated;
grant execute on function public.tree_children_insert_v1(text, text, text, text, text, text, jsonb) to anon, authenticated;
grant execute on function public.tree_children_update_v1(text, text, text, text, text, text, jsonb) to anon, authenticated;
grant execute on function public.tree_children_delete_v1(text, text, text, text, text, text) to anon, authenticated;
grant execute on function public.tree_children_delete_by_id_v1(text, uuid, text, text, text) to anon, authenticated;
`.trim(); if (treeSqlEl) treeSqlEl.value = TREE_SETUP_SQL; if (copyTreeSqlBtn) { copyTreeSqlBtn.addEventListener("click", async () =>{ const ok = await copyText(TREE_SETUP_SQL); showAlert(ok ? "success" : "error", ok ? "تم نسخ أمر الصيانة." : "تعذر النسخ."); }); } const VIEWS_SETUP_SQL = `
create table if not exists public.site_view_counts ( day date not null, path text not null, count bigint not null default 0, primary key (day, path)
); alter table public.site_view_counts enable row level security;
revoke all on table public.site_view_counts from anon, authenticated; create or replace function public.site_track_view_v1(p_path text)
returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_path text;
begin v_path := nullif(trim(coalesce(p_path, '')), ''); if v_path is null then v_path := '/'; end if; insert into public.site_view_counts (day, path, count) values (current_date, v_path, 1) on conflict (day, path) do update set count = public.site_view_counts.count + 1; return true;
end;
$$; create or replace function public.site_view_summary_v1(p_days int default 30)
returns jsonb
language sql
stable
security definer
set search_path = public
as $$ with base as ( select day, path, count from public.site_view_counts where day >= (current_date - greatest(coalesce(p_days, 30), 1) + 1) ), by_day as ( select day, sum(count) as total from base group by day order by day desc ), by_path as ( select path, sum(count) as total from base group by path order by total desc, path asc limit 20 ) select jsonb_build_object( 'total', coalesce((select sum(count) from public.site_view_counts), 0), 'today', coalesce((select sum(count) from public.site_view_counts where day = current_date), 0), 'last_7', coalesce((select sum(count) from public.site_view_counts where day >= current_date - 6), 0), 'days', coalesce((select jsonb_agg(jsonb_build_object('day', day::text, 'total', total)) from by_day), '[]'::jsonb), 'paths', coalesce((select jsonb_agg(jsonb_build_object('path', path, 'total', total)) from by_path), '[]'::jsonb) );
$$; grant execute on function public.site_track_view_v1(text) to anon, authenticated;
grant execute on function public.site_view_summary_v1(int) to anon, authenticated;
`.trim(); if (viewsSqlEl) viewsSqlEl.value = VIEWS_SETUP_SQL; if (copyViewsSqlBtn) { copyViewsSqlBtn.addEventListener("click", async () =>{ const ok = await copyText(VIEWS_SETUP_SQL); showAlert(ok ? "success" : "error", ok ? "تم نسخ أمر الصيانة." : "تعذر النسخ."); }); } const DELEGATES_SETUP_SQL = `
do $$
begin if exists ( select 1 from information_schema.tables where table_schema = 'public' and table_name = 'approval_requests' ) then begin execute 'alter table public.approval_requests drop constraint if exists kind_check'; execute 'alter table public.approval_requests add constraint kind_check check (kind is null or length(btrim(kind)) >0)'; exception when others then null; end; end if;
end
$$; create table if not exists public.family_events ( id bigserial primary key, branch_key text not null, type text null, person text null, date_label text null, event_date date null, details text null, hospital_name text null, hospital_dept text null, contact_method text null, contact_phone text null, visit_date_from date null, visit_date_to date null, visit_time_from text null, visit_time_to text null, created_at timestamptz not null default now()
); alter table public.tree_children add column if not exists death_date_g date;
alter table public.tree_children add column if not exists death_date_h text;
alter table public.tree_children add column if not exists is_deceased boolean;
alter table public.tree_children add column if not exists deceased boolean; alter table public.family_events add column if not exists id bigserial;
alter table public.family_events add column if not exists branch_key text;
alter table public.family_events add column if not exists type text;
alter table public.family_events add column if not exists person text;
alter table public.family_events add column if not exists date_label text;
alter table public.family_events add column if not exists event_date date;
alter table public.family_events add column if not exists details text;
alter table public.family_events add column if not exists hospital_name text;
alter table public.family_events add column if not exists hospital_dept text;
alter table public.family_events add column if not exists contact_method text;
alter table public.family_events add column if not exists contact_phone text;
alter table public.family_events add column if not exists visit_date_from date;
alter table public.family_events add column if not exists visit_date_to date;
alter table public.family_events add column if not exists visit_time_from text;
alter table public.family_events add column if not exists visit_time_to text;
alter table public.family_events add column if not exists created_at timestamptz; do $$
begin begin execute 'alter table public.family_events alter column event_date type date using nullif(event_date::text, '''')::date'; exception when others then null; end; begin execute 'alter table public.family_events alter column visit_date_from type date using nullif(visit_date_from::text, '''')::date'; exception when others then null; end; begin execute 'alter table public.family_events alter column visit_date_to type date using nullif(visit_date_to::text, '''')::date'; exception when others then null; end; begin execute 'alter table public.family_events alter column created_at type timestamptz using nullif(created_at::text, '''')::timestamptz'; exception when others then null; end;
end
$$; alter table public.family_events enable row level security; drop policy if exists "family_events_select_all" on public.family_events;
create policy "family_events_select_all" on public.family_events for select using (true); revoke insert, update, delete on table public.family_events from anon, authenticated;
grant select on table public.family_events to anon, authenticated; insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
values ( 'event-media', 'event-media', true, 52428800, array['image/jpeg', 'image/png', 'image/webp', 'image/gif', 'image/heic', 'image/heif', 'video/mp4', 'video/quicktime', 'video/webm']
)
on conflict (id) do update set public = excluded.public, file_size_limit = excluded.file_size_limit, allowed_mime_types = excluded.allowed_mime_types; drop policy if exists "event_media_public_read" on storage.objects;
create policy "event_media_public_read"
on storage.objects for select
using (bucket_id = 'event-media'); drop policy if exists "event_media_public_insert" on storage.objects;
create policy "event_media_public_insert"
on storage.objects for insert
with check (bucket_id = 'event-media'); drop function if exists public.family_events_insert_v1(text, text, text, text, jsonb);
drop function if exists public.family_events_update_v1(text, text, text, text, text, text, jsonb);
drop function if exists public.family_events_delete_v1(text, text, text, text, text, text); create or replace function public.events_delegate_allowed_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text
) returns boolean
language sql
stable
security definer
set search_path = public
as $$ select exists ( select 1 from public.approval_requests r where r.kind in ('events_delegate', 'tree_delegate') and regexp_replace(btrim(coalesce(r.branch_key, '')), '\s+', ' ', 'g') = regexp_replace(btrim(coalesce(p_branch_key, '')), '\s+', ' ', 'g') and regexp_replace(btrim(coalesce(r.phone, '')), '\s+', '', 'g') = regexp_replace(btrim(coalesce(p_phone, '')), '\s+', '', 'g') and lower(regexp_replace(btrim(coalesce(r.email, '')), '\s+', '', 'g')) = lower(regexp_replace(btrim(coalesce(p_email, '')), '\s+', '', 'g')) and r.status = 'approved' and nullif(trim(coalesce(r.secret_hash, '')), '') is not null and nullif(trim(coalesce(p_secret_hash, '')), '') is not null and r.secret_hash = p_secret_hash limit 1 )
$$; create or replace function public.check_events_delegate_access( p_branch_key text, p_phone text, p_email text, p_secret_hash text
) returns jsonb
language plpgsql
security definer
set search_path = public
as $$
declare v_row record; v_allowed boolean := false;
begin select r.request_id, r.status into v_row from public.approval_requests r where r.kind in ('events_delegate', 'tree_delegate') and regexp_replace(btrim(coalesce(r.branch_key, '')), '\s+', ' ', 'g') = regexp_replace(btrim(coalesce(p_branch_key, '')), '\s+', ' ', 'g') and regexp_replace(btrim(coalesce(r.phone, '')), '\s+', '', 'g') = regexp_replace(btrim(coalesce(p_phone, '')), '\s+', '', 'g') and lower(regexp_replace(btrim(coalesce(r.email, '')), '\s+', '', 'g')) = lower(regexp_replace(btrim(coalesce(p_email, '')), '\s+', '', 'g')) order by r.created_at desc limit 1; if v_row.request_id is null then return jsonb_build_object('allowed', false, 'status', null, 'request_id', null); end if; if v_row.status = 'approved' then select public.events_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) into v_allowed; end if; return jsonb_build_object('allowed', coalesce(v_allowed, false), 'status', v_row.status, 'request_id', v_row.request_id);
end;
$$; create or replace function public.events_audit_log_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text, p_payload jsonb
) returns void
language plpgsql
security definer
set search_path = public
as $$
declare v_actor_name text; v_req_id text;
begin select r.name into v_actor_name from public.approval_requests r where r.kind in ('events_delegate', 'tree_delegate') and regexp_replace(btrim(coalesce(r.branch_key, '')), '\s+', ' ', 'g') = regexp_replace(btrim(coalesce(p_branch_key, '')), '\s+', ' ', 'g') and regexp_replace(btrim(coalesce(r.phone, '')), '\s+', '', 'g') = regexp_replace(btrim(coalesce(p_phone, '')), '\s+', '', 'g') and lower(regexp_replace(btrim(coalesce(r.email, '')), '\s+', '', 'g')) = lower(regexp_replace(btrim(coalesce(p_email, '')), '\s+', '', 'g')) and r.status = 'approved' and nullif(trim(coalesce(r.secret_hash, '')), '') is not null and nullif(trim(coalesce(p_secret_hash, '')), '') is not null and r.secret_hash = p_secret_hash order by r.created_at desc limit 1; v_req_id := 'EVA-' || upper(substr(md5(random()::text || clock_timestamp()::text), 1, 4)) || '-' || upper(substr(md5(random()::text || clock_timestamp()::text), 5, 4)); insert into public.approval_requests ( request_id, kind, branch_key, name, phone, email, secret_hash, message, status, created_at ) values ( v_req_id, 'events_audit', p_branch_key, v_actor_name, p_phone, p_email, p_secret_hash, coalesce(p_payload, '{}'::jsonb)::text, 'approved', now() );
exception when others then return;
end;
$$; create or replace function public.family_events_insert_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text, p_row jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
begin if not public.events_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; insert into public.family_events ( branch_key, type, person, date_label, event_date, details, hospital_name, hospital_dept, contact_method, contact_phone, visit_date_from, visit_date_to, visit_time_from, visit_time_to, created_at ) values ( p_branch_key, nullif(p_row->>'type', ''), nullif(p_row->>'person', ''), nullif(p_row->>'date_label', ''), nullif(p_row->>'event_date', '')::date, nullif(p_row->>'details', ''), nullif(p_row->>'hospital_name', ''), nullif(p_row->>'hospital_dept', ''), nullif(p_row->>'contact_method', ''), nullif(p_row->>'contact_phone', ''), nullif(p_row->>'visit_date_from', '')::date, nullif(p_row->>'visit_date_to', '')::date, nullif(p_row->>'visit_time_from', ''), nullif(p_row->>'visit_time_to', ''), coalesce(nullif(p_row->>'created_at', '')::timestamptz, now()) ); perform public.events_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'events_audit', 'op', 'insert', 'branch_key', p_branch_key, 'type', coalesce(p_row->>'type', ''), 'person', coalesce(p_row->>'person', ''), 'event_date', coalesce(p_row->>'event_date', ''), 'at', now()::timestamptz ) ); return true;
end;
$$; create or replace function public.family_events_update_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text, p_pk_col text, p_pk_value text, p_patch jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_updated boolean := false;
begin if not public.events_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; if p_pk_col = 'id' then update public.family_events e set type = case when p_patch ? 'type' then nullif(p_patch->>'type', '') else e.type end, person = case when p_patch ? 'person' then nullif(p_patch->>'person', '') else e.person end, date_label = case when p_patch ? 'date_label' then nullif(p_patch->>'date_label', '') else e.date_label end, event_date = case when p_patch ? 'event_date' then nullif(p_patch->>'event_date', '')::date else nullif(e.event_date::text, '')::date end, details = case when p_patch ? 'details' then nullif(p_patch->>'details', '') else e.details end, hospital_name = case when p_patch ? 'hospital_name' then nullif(p_patch->>'hospital_name', '') else e.hospital_name end, hospital_dept = case when p_patch ? 'hospital_dept' then nullif(p_patch->>'hospital_dept', '') else e.hospital_dept end, contact_method = case when p_patch ? 'contact_method' then nullif(p_patch->>'contact_method', '') else e.contact_method end, contact_phone = case when p_patch ? 'contact_phone' then nullif(p_patch->>'contact_phone', '') else e.contact_phone end, visit_date_from = case when p_patch ? 'visit_date_from' then nullif(p_patch->>'visit_date_from', '')::date else nullif(e.visit_date_from::text, '')::date end, visit_date_to = case when p_patch ? 'visit_date_to' then nullif(p_patch->>'visit_date_to', '')::date else nullif(e.visit_date_to::text, '')::date end, visit_time_from = case when p_patch ? 'visit_time_from' then nullif(p_patch->>'visit_time_from', '') else e.visit_time_from end, visit_time_to = case when p_patch ? 'visit_time_to' then nullif(p_patch->>'visit_time_to', '') else e.visit_time_to end where e.id = p_pk_value::bigint; v_updated := found; else update public.family_events e set type = case when p_patch ? 'type' then nullif(p_patch->>'type', '') else e.type end, person = case when p_patch ? 'person' then nullif(p_patch->>'person', '') else e.person end, date_label = case when p_patch ? 'date_label' then nullif(p_patch->>'date_label', '') else e.date_label end, event_date = case when p_patch ? 'event_date' then nullif(p_patch->>'event_date', '')::date else nullif(e.event_date::text, '')::date end, details = case when p_patch ? 'details' then nullif(p_patch->>'details', '') else e.details end, hospital_name = case when p_patch ? 'hospital_name' then nullif(p_patch->>'hospital_name', '') else e.hospital_name end, hospital_dept = case when p_patch ? 'hospital_dept' then nullif(p_patch->>'hospital_dept', '') else e.hospital_dept end, contact_method = case when p_patch ? 'contact_method' then nullif(p_patch->>'contact_method', '') else e.contact_method end, contact_phone = case when p_patch ? 'contact_phone' then nullif(p_patch->>'contact_phone', '') else e.contact_phone end, visit_date_from = case when p_patch ? 'visit_date_from' then nullif(p_patch->>'visit_date_from', '')::date else nullif(e.visit_date_from::text, '')::date end, visit_date_to = case when p_patch ? 'visit_date_to' then nullif(p_patch->>'visit_date_to', '')::date else nullif(e.visit_date_to::text, '')::date end, visit_time_from = case when p_patch ? 'visit_time_from' then nullif(p_patch->>'visit_time_from', '') else e.visit_time_from end, visit_time_to = case when p_patch ? 'visit_time_to' then nullif(p_patch->>'visit_time_to', '') else e.visit_time_to end where e.created_at = p_pk_value::timestamptz and e.branch_key = p_branch_key; v_updated := found; end if; if v_updated then perform public.events_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'events_audit', 'op', 'update', 'branch_key', p_branch_key, 'type', coalesce(p_patch->>'type', ''), 'person', coalesce(p_patch->>'person', ''), 'event_date', coalesce(p_patch->>'event_date', ''), 'at', now()::timestamptz ) ); end if; return v_updated;
end;
$$; create or replace function public.family_events_delete_v1( p_branch_key text, p_phone text, p_email text, p_secret_hash text, p_pk_col text, p_pk_value text
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_deleted boolean := false;
begin if not public.events_delegate_allowed_v1(p_branch_key, p_phone, p_email, p_secret_hash) then return false; end if; if p_pk_col = 'id' then delete from public.family_events e where e.id = p_pk_value::bigint; v_deleted := found; else delete from public.family_events e where e.created_at = p_pk_value::timestamptz and e.branch_key = p_branch_key; v_deleted := found; end if; if v_deleted then perform public.events_audit_log_v1( p_branch_key, p_phone, p_email, p_secret_hash, jsonb_build_object( 'v', 1, 'kind', 'events_audit', 'op', 'delete', 'branch_key', p_branch_key, 'pk_col', p_pk_col, 'pk_value', p_pk_value, 'at', now()::timestamptz ) ); end if; return v_deleted;
end;
$$; create or replace function public.admin_token_ok_v1(p_token text)
returns boolean
language plpgsql
security definer
set search_path = public
as $$
begin perform * from public.admin_list_requests(p_token, null, null, 1); return true;
exception when others then return false;
end;
$$; create or replace function public.admin_create_delegate_request_v1( p_token text, p_kind text, p_branch_key text, p_name text, p_phone text, p_email text, p_secret_hash text
) returns text
language plpgsql
security definer
set search_path = public
as $$
declare v_kind text; v_req_id text; v_now timestamptz := now();
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; v_kind := nullif(trim(coalesce(p_kind, '')), ''); if v_kind is null or (v_kind<>'tree_delegate' and v_kind<>'events_delegate') then raise exception 'invalid kind'; end if; v_req_id := (case when v_kind = 'events_delegate' then 'EVT' else 'TRD' end) || '-' || upper(substr(md5(random()::text || clock_timestamp()::text), 1, 4)) || '-' || upper(substr(md5(random()::text || clock_timestamp()::text), 5, 4)); insert into public.approval_requests ( request_id, kind, branch_key, name, phone, email, secret_hash, message, status, created_at ) values ( v_req_id, v_kind, nullif(trim(coalesce(p_branch_key, '')), ''), nullif(trim(coalesce(p_name, '')), ''), nullif(trim(coalesce(p_phone, '')), ''), nullif(lower(trim(coalesce(p_email, ''))), ''), nullif(trim(coalesce(p_secret_hash, '')), ''), jsonb_build_object('v', 1, 'kind', 'admin_grant', 'grant', v_kind, 'at', v_now)::text, 'approved', v_now ); return v_req_id;
end;
$$; create or replace function public.admin_delete_delegate_v1( p_token text, p_branch_key text, p_phone text, p_email text
) returns bigint
language plpgsql
security definer
set search_path = public
as $$
declare v_deleted bigint := 0;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; delete from public.approval_requests r where r.branch_key = nullif(trim(coalesce(p_branch_key, '')), '') and r.phone = nullif(trim(coalesce(p_phone, '')), '') and r.email = nullif(trim(coalesce(p_email, '')), '') and r.kind in ('tree_delegate', 'events_delegate', 'tree_audit', 'events_audit'); get diagnostics v_deleted = row_count; return v_deleted;
end;
$$; create or replace function public.admin_tree_children_import_v1( p_token text, p_rows jsonb
) returns jsonb
language plpgsql
security definer
set search_path = public
as $$
declare v_row jsonb; v_branch text; v_parent text; v_child text; v_id bigint; v_person_id uuid; v_parent_person_id uuid; v_deceased boolean; v_death_date_g date; v_death_date_h text; v_inserted bigint := 0; v_updated bigint := 0; v_skipped bigint := 0;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; if to_regclass('public.tree_children') is null then raise exception 'tree_children table missing'; end if; if p_rows is null or jsonb_typeof(p_rows)<>'array' then return jsonb_build_object('inserted', 0, 'updated', 0, 'skipped', 0); end if; for v_row in select value from jsonb_array_elements(p_rows) loop v_branch := nullif(btrim(coalesce(v_row->>'branch_key', '')), ''); v_parent := nullif(btrim(coalesce(v_row->>'parent_name', '')), ''); v_child := nullif(btrim(coalesce(v_row->>'child_name', '')), ''); if v_branch is null or v_parent is null or v_child is null then v_skipped := v_skipped + 1; continue; end if; v_deceased := case when v_row ? 'is_deceased' then (v_row->>'is_deceased')::boolean when v_row ? 'deceased' then (v_row->>'deceased')::boolean else false end; v_death_date_g := nullif(v_row->>'death_date_g', '')::date; v_death_date_h := nullif(v_row->>'death_date_h', ''); v_person_id := nullif(v_row->>'person_id', '')::uuid; v_parent_person_id := nullif(v_row->>'parent_person_id', '')::uuid; if v_parent_person_id is null then select min(c.person_id::text)::uuid into v_parent_person_id from public.tree_children c where c.branch_key = v_branch and coalesce(c.child_name, c.name) = v_parent having count(distinct c.person_id) = 1; end if; select c.id into v_id from public.tree_children c where c.branch_key = v_branch and c.parent_name = v_parent and coalesce(c.child_name, c.name) = v_child order by c.id desc limit 1; if v_id is not null then update public.tree_children c set person_id = coalesce(c.person_id, v_person_id, gen_random_uuid()), parent_person_id = coalesce(v_parent_person_id, c.parent_person_id), parent_name = v_parent, parent = v_parent, name = v_child, child_name = v_child, birth_date_g = nullif(v_row->>'birth_date_g', '')::date, birth_date_h = nullif(v_row->>'birth_date_h', ''), birth_year = nullif(v_row->>'birth_year', '')::int, birth_order = nullif(v_row->>'birth_order', '')::int, death_date_g = v_death_date_g, death_date_h = v_death_date_h, city = nullif(v_row->>'city', ''), area = nullif(v_row->>'area', ''), is_deceased = coalesce(v_deceased, false), deceased = coalesce(v_deceased, false) where c.id = v_id; v_updated := v_updated + 1; else insert into public.tree_children ( branch_key, parent_name, parent, name, child_name, person_id, parent_person_id, birth_date_g, birth_date_h, birth_year, birth_order, death_date_g, death_date_h, city, area, is_deceased, deceased, created_at ) values ( v_branch, v_parent, v_parent, v_child, v_child, coalesce(v_person_id, gen_random_uuid()), v_parent_person_id, nullif(v_row->>'birth_date_g', '')::date, nullif(v_row->>'birth_date_h', ''), nullif(v_row->>'birth_year', '')::int, nullif(v_row->>'birth_order', '')::int, v_death_date_g, v_death_date_h, nullif(v_row->>'city', ''), nullif(v_row->>'area', ''), coalesce(v_deceased, false), coalesce(v_deceased, false), coalesce(nullif(v_row->>'created_at', '')::timestamptz, now()) ); v_inserted := v_inserted + 1; end if; end loop; return jsonb_build_object('inserted', v_inserted, 'updated', v_updated, 'skipped', v_skipped);
end;
$$; create or replace function public.admin_tree_child_upsert_v1( p_token text, p_row jsonb
) returns jsonb
language plpgsql
security definer
set search_path = public
as $$
declare v_id bigint; v_branch text; v_parent text; v_child text; v_old_parent text; v_old_child text; v_person_id uuid; v_parent_person_id uuid; v_deceased boolean; v_saved_id bigint;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; if to_regclass('public.tree_children') is null then raise exception 'tree_children table missing'; end if; v_id := nullif(p_row->>'id', '')::bigint; v_branch := nullif(btrim(coalesce(p_row->>'branch_key', '')), ''); v_parent := nullif(btrim(coalesce(p_row->>'parent_name', '')), ''); v_child := nullif(btrim(coalesce(p_row->>'child_name', '')), ''); v_person_id := nullif(p_row->>'person_id', '')::uuid; v_parent_person_id := nullif(p_row->>'parent_person_id', '')::uuid; v_deceased := case when p_row ? 'is_deceased' then (p_row->>'is_deceased')::boolean when p_row ? 'deceased' then (p_row->>'deceased')::boolean else false end; if v_branch is null or v_parent is null or v_child is null then raise exception 'missing tree row fields'; end if; if v_parent_person_id is null then select min(c.person_id::text)::uuid into v_parent_person_id from public.tree_children c where c.branch_key = v_branch and coalesce(c.child_name, c.name) = v_parent having count(distinct c.person_id) = 1; end if; if v_id is not null then select coalesce(c.parent_name, c.parent), coalesce(c.child_name, c.name), c.person_id into v_old_parent, v_old_child, v_person_id from public.tree_children c where c.id = v_id and c.branch_key = v_branch limit 1; if v_old_child is null then raise exception 'tree row not found'; end if; update public.tree_children c set parent_name = v_parent, parent = v_parent, child_name = v_child, name = v_child, person_id = coalesce(c.person_id, v_person_id, gen_random_uuid()), parent_person_id = coalesce(v_parent_person_id, c.parent_person_id), birth_date_g = nullif(p_row->>'birth_date_g', '')::date, birth_date_h = nullif(p_row->>'birth_date_h', ''), birth_year = nullif(p_row->>'birth_year', '')::int, birth_order = nullif(p_row->>'birth_order', '')::int, death_date_g = nullif(p_row->>'death_date_g', '')::date, death_date_h = nullif(p_row->>'death_date_h', ''), city = nullif(p_row->>'city', ''), area = nullif(p_row->>'area', ''), is_deceased = coalesce(v_deceased, false), deceased = coalesce(v_deceased, false) where c.id = v_id returning c.id into v_saved_id; if v_old_child<>v_child then update public.tree_children c set parent_name = case when coalesce(c.parent_name, c.parent, '') = v_old_child then v_child when coalesce(c.parent_name, c.parent, '') like v_old_child || '/%' then v_child || substr(coalesce(c.parent_name, c.parent), length(v_old_child) + 1) else c.parent_name end, parent = case when coalesce(c.parent, c.parent_name, '') = v_old_child then v_child when coalesce(c.parent, c.parent_name, '') like v_old_child || '/%' then v_child || substr(coalesce(c.parent, c.parent_name), length(v_old_child) + 1) else c.parent end, child_name = case when coalesce(c.child_name, c.name, '') like v_old_child || '/%' then v_child || substr(coalesce(c.child_name, c.name), length(v_old_child) + 1) else c.child_name end, name = case when coalesce(c.name, c.child_name, '') like v_old_child || '/%' then v_child || substr(coalesce(c.name, c.child_name), length(v_old_child) + 1) else c.name end where c.branch_key = v_branch and c.id<>v_id and ( coalesce(c.parent_name, c.parent, '') = v_old_child or coalesce(c.parent_name, c.parent, '') like v_old_child || '/%' or coalesce(c.child_name, c.name, '') like v_old_child || '/%' ); end if; else insert into public.tree_children ( branch_key, parent_name, parent, child_name, name, person_id, parent_person_id, birth_date_g, birth_date_h, birth_year, birth_order, death_date_g, death_date_h, city, area, is_deceased, deceased, created_at ) values ( v_branch, v_parent, v_parent, v_child, v_child, coalesce(v_person_id, gen_random_uuid()), v_parent_person_id, nullif(p_row->>'birth_date_g', '')::date, nullif(p_row->>'birth_date_h', ''), nullif(p_row->>'birth_year', '')::int, nullif(p_row->>'birth_order', '')::int, nullif(p_row->>'death_date_g', '')::date, nullif(p_row->>'death_date_h', ''), nullif(p_row->>'city', ''), nullif(p_row->>'area', ''), coalesce(v_deceased, false), coalesce(v_deceased, false), now() ) returning id into v_saved_id; end if; return jsonb_build_object('ok', true, 'id', v_saved_id);
end;
$$; create or replace function public.admin_tree_child_delete_subtree_v1( p_token text, p_branch_key text, p_id bigint
) returns bigint
language plpgsql
security definer
set search_path = public
as $$
declare v_target text; v_deleted bigint := 0;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; select coalesce(c.child_name, c.name) into v_target from public.tree_children c where c.branch_key = p_branch_key and c.id = p_id limit 1; if v_target is null then return 0; end if; delete from public.tree_children c where c.branch_key = p_branch_key and ( c.id = p_id or coalesce(c.parent_name, c.parent, '') = v_target or coalesce(c.parent_name, c.parent, '') like v_target || '/%' or coalesce(c.child_name, c.name, '') = v_target or coalesce(c.child_name, c.name, '') like v_target || '/%' ); get diagnostics v_deleted = row_count; return v_deleted;
end;
$$; grant execute on function public.events_delegate_allowed_v1(text, text, text, text) to anon, authenticated;
grant execute on function public.check_events_delegate_access(text, text, text, text) to anon, authenticated;
grant execute on function public.family_events_insert_v1(text, text, text, text, jsonb) to anon, authenticated;
grant execute on function public.family_events_update_v1(text, text, text, text, text, text, jsonb) to anon, authenticated;
grant execute on function public.family_events_delete_v1(text, text, text, text, text, text) to anon, authenticated;
grant execute on function public.admin_token_ok_v1(text) to anon, authenticated;
grant execute on function public.admin_create_delegate_request_v1(text, text, text, text, text, text, text) to anon, authenticated;
grant execute on function public.admin_delete_delegate_v1(text, text, text, text) to anon, authenticated; -- حذف طلب إداري : للإدارة فقط عبر التوكن
create or replace function public.admin_delete_request_v1( p_token text, p_id text
)
returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_id bigint;
begin if not public.admin_token_ok_v1(p_token) then return false; end if; v_id := nullif(regexp_replace(coalesce(p_id, ''), '\D', '', 'g'), '')::bigint; if v_id is null then return false; end if; delete from public.approval_requests where id = v_id; return found;
exception when others then return false;
end;
$$; grant execute on function public.admin_delete_request_v1(text, text) to anon, authenticated; grant execute on function public.admin_tree_children_import_v1(text, jsonb) to anon, authenticated;
grant execute on function public.admin_tree_child_upsert_v1(text, jsonb) to anon, authenticated;
grant execute on function public.admin_tree_child_delete_subtree_v1(text, text, bigint) to anon, authenticated; drop function if exists public.admin_publish_event_card_v1(text, text, jsonb);
create or replace function public.admin_publish_event_card_v1( p_token text, p_request_id text, p_row jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_request_id text;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; v_request_id := nullif(btrim(coalesce(p_request_id, '')), ''); if v_request_id is null or p_row is null or jsonb_typeof(p_row)<>'object' then return false; end if; if exists ( select 1 from public.family_events e where coalesce(e.details, '') like '%' || v_request_id || '%' ) then return true; end if; insert into public.family_events ( branch_key, type, person, date_label, event_date, details, hospital_name, hospital_dept, contact_method, contact_phone, visit_date_from, visit_date_to, visit_time_from, visit_time_to, created_at ) values ( nullif(p_row->>'branch_key', ''), nullif(p_row->>'type', ''), nullif(p_row->>'person', ''), nullif(p_row->>'date_label', ''), nullif(p_row->>'event_date', '')::date, nullif(p_row->>'details', ''), nullif(p_row->>'hospital_name', ''), nullif(p_row->>'hospital_dept', ''), nullif(p_row->>'contact_method', ''), nullif(p_row->>'contact_phone', ''), nullif(p_row->>'visit_date_from', '')::date, nullif(p_row->>'visit_date_to', '')::date, nullif(p_row->>'visit_time_from', ''), nullif(p_row->>'visit_time_to', ''), coalesce(nullif(p_row->>'created_at', '')::timestamptz, now()) ); return true;
end;
$$; revoke all on function public.admin_publish_event_card_v1(text, text, jsonb) from public;
grant execute on function public.admin_publish_event_card_v1(text, text, jsonb) to anon, authenticated; تحديث الخدمة, 'تحديث البيانات';
`.trim(); const REQUEST_EDIT_SETUP_SQL = `
drop function if exists public.admin_update_request_branch_v1(text, text, text, text);
drop function if exists public.admin_update_request_branch_v1(text, text, text, text, text, jsonb); create or replace function public.admin_update_request_branch_v1( p_token text, p_id text, p_old_branch_key text, p_branch_key text, p_name text, p_phone text, p_email text, p_message text, p_old_tree_rows jsonb, p_new_tree_rows jsonb
) returns boolean
language plpgsql
security definer
set search_path = public
as $$
declare v_branch text; v_old_branch text; v_kind text; v_status text; v_row jsonb; v_parent text; v_child text;
begin if not public.admin_token_ok_v1(p_token) then raise exception 'not allowed'; end if; v_branch := nullif(btrim(coalesce(p_branch_key, '')), ''); v_old_branch := nullif(btrim(coalesce(p_old_branch_key, '')), ''); if v_branch is null then raise exception 'branch required'; end if; select kind, status into v_kind, v_status from public.approval_requests where request_id = nullif(btrim(coalesce(p_id, '')), '') or id::text = nullif(btrim(coalesce(p_id, '')), '') limit 1 for update; if v_status is null or v_status not in ('pending', 'approved') then return false; end if; if v_status = 'approved' and v_kind = 'tree_card' and v_old_branch is not null and p_old_tree_rows is not null and jsonb_typeof(p_old_tree_rows) = 'array' then for v_row in select value from jsonb_array_elements(p_old_tree_rows) loop v_parent := nullif(btrim(coalesce(v_row->>'parent_name', '')), ''); v_child := nullif(btrim(coalesce(v_row->>'child_name', '')), ''); if v_parent is null or v_child is null then continue; end if; if v_old_branch<>v_branch or not exists ( select 1 from jsonb_array_elements(coalesce(p_new_tree_rows, '[]'::jsonb)) n where nullif(btrim(coalesce(n->>'parent_name', '')), '') = v_parent and nullif(btrim(coalesce(n->>'child_name', '')), '') = v_child ) then delete from public.tree_children c where c.branch_key = v_old_branch and c.parent_name = v_parent and coalesce(c.child_name, c.name) = v_child; end if; end loop; perform public.admin_tree_children_import_v1( p_token, coalesce(p_new_tree_rows, '[]'::jsonb) ); end if; update public.approval_requests set branch_key = v_branch, name = nullif(btrim(coalesce(p_name, '')), ''), phone = nullif(btrim(coalesce(p_phone, '')), ''), email = nullif(lower(btrim(coalesce(p_email, ''))), ''), message = coalesce(nullif(p_message, ''), message) where status in ('pending', 'approved') and ( request_id = nullif(btrim(coalesce(p_id, '')), '') or id::text = nullif(btrim(coalesce(p_id, '')), '') ); return found;
end;
$$; revoke all on function public.admin_update_request_branch_v1(text, text, text, text, text, text, text, text, jsonb, jsonb) from public;
grant execute on function public.admin_update_request_branch_v1(text, text, text, text, text, text, text, text, jsonb, jsonb) to anon, authenticated;
`.trim(); const LAHM_SALEH_FIX_SQL = `
begin; delete from public.tree_children c
where c.branch_key = 'لاحم' and ( regexp_replace(coalesce(c.child_name, c.name, ''), '^.*/', '') in ( 'صالح سليمان عواد', 'عواد سليمان صالح' ) or coalesce(c.child_name, c.name, '') in ( 'لاحم بن مطلق بن زيدان/صالح/عواد', 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان', 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف' ) ); update public.tree_children c
set parent_name = case when coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان', 'لاحم') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/سليمان') then 'لاحم بن مطلق بن زيدان/صالح' when coalesce(c.parent_name, c.parent, '') = 'سليمان' then 'لاحم بن مطلق بن زيدان/صالح/سليمان' else replace(coalesce(c.parent_name, c.parent), 'لاحم بن مطلق بن زيدان/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان') end, parent = case when coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان', 'لاحم') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/سليمان') then 'لاحم بن مطلق بن زيدان/صالح' when coalesce(c.parent, c.parent_name, '') = 'سليمان' then 'لاحم بن مطلق بن زيدان/صالح/سليمان' else replace(coalesce(c.parent, c.parent_name), 'لاحم بن مطلق بن زيدان/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان') end, child_name = case when coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان', 'لاحم') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/سليمان') then 'لاحم بن مطلق بن زيدان/صالح/سليمان' else replace(coalesce(c.child_name, c.name), 'لاحم بن مطلق بن زيدان/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان') end, name = case when coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان', 'لاحم') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/سليمان') then 'لاحم بن مطلق بن زيدان/صالح/سليمان' else replace(coalesce(c.name, c.child_name), 'لاحم بن مطلق بن زيدان/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان') end
where c.branch_key = 'لاحم' and ( ( coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان', 'لاحم') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/سليمان') ) or coalesce(c.parent_name, c.parent, '') = 'سليمان' or coalesce(c.parent_name, c.parent, '') like 'لاحم بن مطلق بن زيدان/سليمان/%' or coalesce(c.child_name, c.name, '') like 'لاحم بن مطلق بن زيدان/سليمان/%' ); update public.tree_children c
set parent_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان', parent = 'لاحم بن مطلق بن زيدان/صالح/سليمان', child_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد', name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد'
where c.branch_key = 'لاحم' and coalesce(c.parent_name, c.parent, '') in ('لاحم بن مطلق بن زيدان/صالح', 'صالح') and coalesce(c.child_name, c.name, '') in ('عواد', 'لاحم بن مطلق بن زيدان/صالح/عواد'); update public.tree_children c
set parent_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد', parent = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد', child_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان', name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان'
where c.branch_key = 'لاحم' and coalesce(c.parent_name, c.parent, '') in ('عواد', 'لاحم بن مطلق بن زيدان/صالح/عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد') and coalesce(c.child_name, c.name, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان'); update public.tree_children c
set parent_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان', parent = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان', child_name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف', name = 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'
where c.branch_key = 'لاحم' and coalesce(c.parent_name, c.parent, '') in ('سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان', 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان') and coalesce(c.child_name, c.name, '') in ('نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'); update public.tree_children c
set parent_name = replace( replace( replace(c.parent_name, 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'), 'لاحم بن مطلق بن زيدان/صالح سليمان عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), 'لاحم بن مطلق بن زيدان/عواد سليمان صالح', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), parent = replace( replace( replace(coalesce(c.parent, c.parent_name), 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'), 'لاحم بن مطلق بن زيدان/صالح سليمان عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), 'لاحم بن مطلق بن زيدان/عواد سليمان صالح', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), child_name = replace( replace( replace(coalesce(c.child_name, c.name), 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'), 'لاحم بن مطلق بن زيدان/صالح سليمان عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), 'لاحم بن مطلق بن زيدان/عواد سليمان صالح', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), name = replace( replace( replace(coalesce(c.name, c.child_name), 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف'), 'لاحم بن مطلق بن زيدان/صالح سليمان عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' ), 'لاحم بن مطلق بن زيدان/عواد سليمان صالح', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد' )
where c.branch_key = 'لاحم' and ( coalesce(c.parent_name, c.parent, '') like 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف/%' or coalesce(c.child_name, c.name, '') like 'لاحم بن مطلق بن زيدان/صالح/عواد/سليمان/نايف/%' or coalesce(c.parent_name, c.parent, '') like 'لاحم بن مطلق بن زيدان/صالح سليمان عواد/%' or coalesce(c.child_name, c.name, '') like 'لاحم بن مطلق بن زيدان/صالح سليمان عواد/%' or coalesce(c.parent_name, c.parent, '') like 'لاحم بن مطلق بن زيدان/عواد سليمان صالح/%' or coalesce(c.child_name, c.name, '') like 'لاحم بن مطلق بن زيدان/عواد سليمان صالح/%' ); insert into public.tree_children ( branch_key, parent_name, parent, name, child_name, created_at
)
select 'لاحم', x.parent_name, x.parent_name, x.child_name, x.child_name, now()
from ( values ('لاحم بن مطلق بن زيدان', 'صالح'), ('لاحم بن مطلق بن زيدان/صالح', 'لاحم بن مطلق بن زيدان/صالح/سليمان'), ('لاحم بن مطلق بن زيدان/صالح/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد'), ('لاحم بن مطلق بن زيدان/صالح/سليمان/عواد', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان'), ('لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان', 'لاحم بن مطلق بن زيدان/صالح/سليمان/عواد/سليمان/نايف')
) as x(parent_name, child_name)
where not exists ( select 1 from public.tree_children c where c.branch_key = 'لاحم' and c.parent_name = x.parent_name and ( coalesce(c.child_name, c.name) = x.child_name or regexp_replace(coalesce(c.child_name, c.name, ''), '^.*/', '') = regexp_replace(x.child_name, '^.*/', '') )
); update public.tree_children
set person_id = gen_random_uuid()
where branch_key = 'لاحم' and person_id is null; update public.tree_children c
set parent_person_id = matches.person_id
from ( select child.id, min(parent.person_id::text)::uuid as person_id from public.tree_children child join public.tree_children parent on parent.branch_key = child.branch_key and coalesce(parent.child_name, parent.name) = coalesce(child.parent_name, child.parent) and parent.person_id is not null where child.branch_key = 'لاحم' group by child.id having count(distinct parent.person_id) = 1
) matches
where c.id = matches.id; commit;
`.trim(); if (delegatesSqlEl) delegatesSqlEl.value = DELEGATES_SETUP_SQL; if (copyDelegatesSqlBtn) { copyDelegatesSqlBtn.addEventListener("click", async () =>{ const ok = await copyText(DELEGATES_SETUP_SQL); showAlert(ok ? "success" : "error", ok ? "تم نسخ أمر الصيانة." : "تعذر النسخ."); }); } if (requestEditSqlEl) requestEditSqlEl.value = REQUEST_EDIT_SETUP_SQL; if (copyRequestEditSqlBtn) { copyRequestEditSqlBtn.addEventListener("click", async () =>{ const ok = await copyText(REQUEST_EDIT_SETUP_SQL); showAlert(ok ? "success" : "error", ok ? "تم نسخ أمر الصيانة تعديل الفرع." : "تعذر النسخ."); }); } if (lahmSalehFixSqlEl) lahmSalehFixSqlEl.value = LAHM_SALEH_FIX_SQL; if (copyLahmSalehFixSqlBtn) { copyLahmSalehFixSqlBtn.addEventListener("click", async () =>{ const ok = await copyText(LAHM_SALEH_FIX_SQL); showAlert(ok ? "success" : "error", ok ? "تم نسخ أمر الصيانة إصلاح صالح." : "تعذر النسخ."); }); } try { lastNotifiedPendingKey = String(localStorage.getItem(ADMIN_NOTIF_LAST_KEY) || ""); } catch (e) { lastNotifiedPendingKey = ""; } try { lastEmailedAuditKey = String(localStorage.getItem(ADMIN_EMAIL_LAST_AUDIT_KEY) || ""); } catch (e) { lastEmailedAuditKey = ""; } function setEmailStatus(text) { if (!adminEmailStatus) return; adminEmailStatus.textContent = text || ""; } function loadAdminNotifyEmail() { try { return String(localStorage.getItem(ADMIN_EMAIL_KEY) || "").trim(); } catch (e) { return ""; } } function saveAdminNotifyEmail(v) { try { localStorage.setItem(ADMIN_EMAIL_KEY, String(v || "").trim()); } catch (e) {} } function loadBoolSetting(key, fallback) { try { const raw = localStorage.getItem(key); if (raw == null) return !!fallback; return raw === "1"; } catch (e) { return !!fallback; } } function saveBoolSetting(key, v) { try { localStorage.setItem(key, v ? "1" : "0"); } catch (e) {} } function canShowBrowserNotifications() { return typeof Notification !== "undefined" && typeof Notification.requestPermission === "function"; } async function ensureBrowserNotificationsEnabled() { if (!canShowBrowserNotifications()) return false; if (Notification.permission === "granted") return true; if (Notification.permission === "denied") return false; const res = await Notification.requestPermission(); return res === "granted"; } function updateNotifsButtonText() { if (!adminEnableNotifsBtn) return; if (!getAdminToken()) { adminEnableNotifsBtn.textContent = "تفعيل إشعارات الطلبات"; adminEnableNotifsBtn.disabled = true; return; } if (!canShowBrowserNotifications()) { adminEnableNotifsBtn.textContent = "الإشعارات غير مدعومة"; adminEnableNotifsBtn.disabled = true; return; } if (Notification.permission === "granted") { adminEnableNotifsBtn.textContent = "الإشعارات مفعلة"; adminEnableNotifsBtn.disabled = true; return; } if (Notification.permission === "denied") { adminEnableNotifsBtn.textContent = "الإشعارات مرفوضة"; adminEnableNotifsBtn.disabled = true; return; } adminEnableNotifsBtn.textContent = "تفعيل إشعارات الطلبات"; adminEnableNotifsBtn.disabled = false; } function saveLastNotifiedPendingKey(key) { const k = String(key || "").trim(); if (!k) return; lastNotifiedPendingKey = k; try { localStorage.setItem(ADMIN_NOTIF_LAST_KEY, k); } catch (e) {} } function kindLabel(kind) { const map = { org_role: "صندوق / جمعية", tree_delegate: "مندوب الشجرة", events_delegate: "مندوب المناسبات", member_registration: "تسجيل عضو", tree_card: "بطاقة الشجرة", tree_audit: "تعديل بيانات الشجرة", events_audit: "تعديل الأخبار والمناسبات", event_card: "بطاقة مناسبة", test_request: "طلب اختبار", unknown: "غير معروف" }; const key = String(kind || "").trim(); return map[key] || key || "غير معروف"; } function showPendingRequestNotification(row) { if (!canShowBrowserNotifications()) return; if (Notification.permission !== "granted") return; if (!row) return; const title = "طلب جديد: " + kindLabel(row.kind); const parts = []; if (row.branch_key) parts.push("الفرع: " + row.branch_key); if (row.name) parts.push("الاسم: " + row.name); if (row.phone) parts.push("الجوال: " + row.phone); if (row.email) parts.push("البريد: " + row.email); const body = parts.join("\n"); try { const n = new Notification(title, { body, tag: "alzidan-admin-req", renotify: true }); n.onclick = () =>{ try { window.focus(); } catch (e) {} }; } catch (e) {} } function showAlert(type, text) { if (!alertEl) return; alertEl.className = "alert " + (type === "success" ? "alert-success" : "alert-error"); alertEl.textContent = text || ""; alertEl.style.display = "block"; } function normalizeEmail(v) { return String(v || "") .trim() .toLowerCase(); } function isLikelyEmail(v) { const s = normalizeEmail(v); return !!(s && s.includes("@") && s.includes(".") && s.length >= 6); } function fallbackCopyText(text) { const el = document.createElement("textarea"); el.value = String(text || ""); el.setAttribute("readonly", ""); el.style.position = "fixed"; el.style.opacity = "0"; el.style.left = "-9999px"; document.body.appendChild(el); el.select(); try { document.execCommand("copy"); } catch (e) {} document.body.removeChild(el); } async function copyText(text) { const t = String(text || ""); if (!t) return false; try { if (navigator.clipboard && typeof navigator.clipboard.writeText === "function") { await navigator.clipboard.writeText(t); return true; } } catch (e) {} fallbackCopyText(t); return true; } function downloadTextFile(filename, text, mimeType) { const name = String(filename || "").trim() || "file.txt"; const content = String(text || ""); const type = String(mimeType || "").trim() || "text/plain;charset=utf-8"; const blob = new Blob([content], { type }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = name; document.body.appendChild(a); a.click(); a.remove(); setTimeout(() =>URL.revokeObjectURL(url), 1000); } function treeCsvTemplateText() { const bom = "\ufeff"; const header = ["الفرع", "اسم_الأب", "الاسم_كامل", "تاريخ_الميلاد_ميلادي", "تاريخ_الميلاد_هجري", "سنة_الميلاد", "المدينة", "الحي", "متوفى"].join(","); const example = ["زيدان", "خميس", "عبدالله خميس الزيدان", "١٩٩٠/٠١/٠١", "١٤١٠/٠١/٠١", "١٩٩٠", "حائل", "المدينة/الحي", "لا"].join(","); return bom + header + "\n" + example + "\n"; } function treeCardTemplateText() { const lines = []; lines.push("بطاقة إضافة بيانات للشجرة"); lines.push(""); lines.push("العائلة (إجباري): زيدان / مزيد / زايد / لاحم / ملحم"); lines.push("الجد (إجباري): "); lines.push("الأب (إجباري): "); lines.push("الاسم (إجباري): "); lines.push("تاريخ الميلاد (اختياري): "); lines.push("المدينة (اختياري): "); lines.push("الحي/القرية (اختياري): "); lines.push(""); lines.push("الأبناء (اختياري):"); lines.push("1- الاسم: — تاريخ الميلاد: "); lines.push("2- الاسم: — تاريخ الميلاد: "); lines.push("3- الاسم: — تاريخ الميلاد: "); lines.push("4- الاسم: — تاريخ الميلاد: "); lines.push("5- الاسم: — تاريخ الميلاد: "); lines.push("أضف سطوراً أخرى إذا كان عدد الأبناء أكثر."); return lines.join("\n"); } function openWhatsAppWithText(text) { const t = String(text || "").trim(); if (!t) return false; const url = "https://wa.me/?text=" + encodeURIComponent(t); try { window.open(url, "_blank", "noopener,noreferrer"); return true; } catch (e) { return false; } } function setWaFileStatus(text) { if (!waFileStatusEl) return; waFileStatusEl.textContent = String(text || ""); } function truncateText(t, max) { const s = String(t || ""); const n = Number(max || 0); if (!n || s.length<= n) return s; return s.slice(0, Math.max(0, n - 20)) + "\n...\n(تم الاختصار)"; } function takeLines(text, maxLines, maxChars) { const src = String(text || "").replace(/\r\n/g, "\n").replace(/\r/g, "\n"); const lines = src.split("\n"); const out = []; for (let i = 0; i< lines.length && out.length< maxLines; i += 1) { const v = String(lines[i] || ""); if (!v.trim()) continue; out.push(v); if (maxChars && out.join("\n").length >= maxChars) break; } return out.join("\n"); } async function readFileAsText(file) { if (!file) return ""; try { if (typeof file.text === "function") return await file.text(); } catch (e) {} return await new Promise((resolve) =>{ try { const reader = new FileReader(); reader.onload = () =>resolve(String(reader.result || "")); reader.onerror = () =>resolve(""); reader.readAsText(file); } catch (e) { resolve(""); } }); } function buildWhatsAppTextFromFile(file, text) { const f = file || null; const name = f && f.name ? String(f.name || "") : "file"; const size = f && f.size != null ? Number(f.size) : 0; const sizeKb = isFinite(size) ? Math.max(0, Math.round(size / 1024)) : 0; const ext = name.toLowerCase().split(".").pop(); const raw = String(text || ""); let snippet = ""; if (ext === "json") { try { const parsed = JSON.parse(raw); snippet = JSON.stringify(parsed, null, 2); } catch (e) { snippet = raw; } snippet = takeLines(snippet, 120, 2400); } else if (ext === "csv") { snippet = takeLines(raw, 35, 2400); } else { snippet = takeLines(raw, 60, 2400); } const lines = []; lines.push("ملف: " + name); if (sizeKb) lines.push("الحجم: " + String(sizeKb) + "KB"); lines.push(""); lines.push("محتوى مختصر:"); lines.push(snippet || "(فارغ)"); return truncateText(lines.join("\n"), 3600); } async function shareTreeCsvTemplateFile() { const csv = treeCsvTemplateText(); const file = new File([csv], "alzidan-tree-template.csv", { type: "text/csv;charset=utf-8" }); if (!navigator || typeof navigator.share !== "function") return { ok: false, reason: "no_share" }; try { if (typeof navigator.canShare === "function" && !navigator.canShare({ files: [file] })) { return { ok: false, reason: "no_file_share" }; } } catch (e) { return { ok: false, reason: "no_file_share" }; } try { await navigator.share({ title: "قالب شجرة العائلة (CSV)", text: "قالب CSV لتعبئة شجرة عائلة الزيدان", files: [file] }); return { ok: true }; } catch (e) { return { ok: false, reason: "share_failed" }; } } function detectCsvDelimiter(line) { const s = String(line || ""); const commas = (s.match(/,/g) || []).length; const semis = (s.match(/;/g) || []).length; return semis >commas ? ";" : ","; } function parseCsv(text) { const raw = String(text || "").replace(/\r\n/g, "\n").replace(/\r/g, "\n"); const lines = raw .split("\n") .map((l) =>l.replace(/^\ufeff/, "")) .filter((l) =>l.trim().length); if (!lines.length) return []; const delimiter = detectCsvDelimiter(lines[0]); const parseLine = (line) =>{ const out = []; let cur = ""; let inQuotes = false; for (let i = 0; i< line.length; i++) { const ch = line[i]; if (inQuotes) { if (ch === '"') { if (line[i + 1] === '"') { cur += '"'; i++; } else { inQuotes = false; } } else { cur += ch; } continue; } if (ch === '"') { inQuotes = true; continue; } if (ch === delimiter) { out.push(cur.trim()); cur = ""; continue; } cur += ch; } out.push(cur.trim()); return out; }; const header = parseLine(lines[0]).map((h) =>String(h || "").trim().replace(/^\ufeff/, "")); const rows = []; for (let i = 1; i< lines.length; i++) { const parts = parseLine(lines[i]); if (!parts.some((p) =>String(p || "").trim())) continue; const obj = {}; header.forEach((k, idx) =>{ if (!k) return; obj[k] = parts[idx] != null ? String(parts[idx]) : ""; }); rows.push(obj); } return rows; } function coerceBool(v) { if (v == null) return null; const s = String(v || "").trim().toLowerCase(); if (!s) return null; if (s === "1" || s === "true" || s === "yes" || s === "y" || s === "نعم" || s === "صح") return true; if (s === "0" || s === "false" || s === "no" || s === "n" || s === "لا" || s === "خطأ") return false; return null; } function normalizeArabicDigitsToLatin(v) { const s = String(v || ""); const map = { "٠": "0", "١": "1", "٢": "2", "٣": "3", "٤": "4", "٥": "5", "٦": "6", "٧": "7", "٨": "8", "٩": "9", "۰": "0", "۱": "1", "۲": "2", "۳": "3", "۴": "4", "۵": "5", "۶": "6", "۷": "7", "۸": "8", "۹": "9" }; return s.replace(/[٠-٩۰-۹]/g, (ch) =>map[ch] || ch); } function pickRowValue(row, keys) { const r = row || {}; for (let i = 0; i< keys.length; i += 1) { const k = keys[i]; if (!k) continue; const v = r[k]; const s = v != null ? String(v).trim() : ""; if (s) return s; } return ""; } function toIntOrNull(v) { const s = normalizeArabicDigitsToLatin(String(v || "").trim()); if (!s) return null; const n = parseInt(s, 10); return Number.isFinite(n) ? n : null; } function toIsoDateOrEmpty(v) { const raw = normalizeArabicDigitsToLatin(String(v || "").trim()); if (!raw) return ""; const cleaned = raw.replace(/[.\s]+/g, "/"); let y = null; let m = null; let d = null; let mm = cleaned.match(/^(\d{4})[-/](\d{1,2})[-/](\d{1,2})$/); if (mm) { y = toIntOrNull(mm[1]); m = toIntOrNull(mm[2]); d = toIntOrNull(mm[3]); } else { mm = cleaned.match(/^(\d{1,2})[-/](\d{1,2})[-/](\d{4})$/); if (mm) { d = toIntOrNull(mm[1]); m = toIntOrNull(mm[2]); y = toIntOrNull(mm[3]); } } if (!y || !m || !d) return ""; if (y< 1800 || y >2100) return ""; if (m< 1 || m >12) return ""; if (d< 1 || d >31) return ""; const dt = new Date(Date.UTC(y, m - 1, d)); if (dt.getUTCFullYear() !== y || dt.getUTCMonth() !== m - 1 || dt.getUTCDate() !== d) return ""; const yy = String(y).padStart(4, "0"); const mm2 = String(m).padStart(2, "0"); const dd2 = String(d).padStart(2, "0"); return `${yy}-${mm2}-${dd2}`; } function chunkArray(arr, size) { const n = Math.max(1, Number(size || 1)); const out = []; for (let i = 0; i< arr.length; i += n) out.push(arr.slice(i, i + n)); return out; } function setTreeImportStatus(text) { if (!treeImportStatusEl) return; treeImportStatusEl.textContent = String(text || ""); } function setBatchTreeStatus(text) { if (!batchTreeStatus) return; batchTreeStatus.textContent = String(text || ""); } function normalizeBatchText(v) { return normalizeArabicDigitsToLatin(String(v || "")) .replace(/[\u064B-\u065F\u0670]/g, "") .replace(/[إأآ]/g, "أ") .replace(/\s+/g, " ") .trim(); } function cleanBatchLine(v) { let s = normalizeBatchText(v); s = s.replace(/^(?:أرسلت من الـ iPhone|الاسم)\s*/i, "").trim(); s = s.replace(/^[\d\s/\\.\-:،,؛\])(\[]+/, "").trim(); s = s.replace(/\b(?:الزيدان|الشريهي|الشريهى|الشريه)\b/g, "").trim(); s = s.replace(/\s+/g, " ").trim(); return s; } function batchJoinAbd(words, start) { const list = Array.isArray(words) ? words : []; const first = normalizeBatchText(list[start] || ""); if (!first) return { value: "", next: start }; if (first === "عبد" && list[start + 1]) { return { value: normalizeBatchText("عبد" + String(list[start + 1] || "")), next: start + 2 }; } return { value: first, next: start + 1 }; } function batchLeaf(path) { const s = normalizeBatchText(path); if (!s) return ""; if (s.includes("/")) return s.split("/").filter(Boolean).slice(-1)[0] || s; const rootMatch = s.match(/^(.+?)\s+بن\s+مطلق\s+بن\s+زيدان$/); return rootMatch ? normalizeBatchText(rootMatch[1]) : s; } function batchParentPath(path) { const s = normalizeBatchText(path); if (!s || !s.includes("/")) return ""; const parts = s.split("/").filter(Boolean); parts.pop(); return parts.join("/"); } function batchSqlLiteral(v) { return "'" + String(v || "").replace(/'/g, "''") + "'"; } function parseBatchFullLineage(line, branch) { const raw = cleanBatchLine(line); if (!raw || !/\s+بن\s+/.test(raw) || !raw.includes("مطلق بن زيدان")) return null; const parts = raw .split(/\s+بن\s+/g) .map((p) =>cleanBatchLine(p).replace(/\b(?:ناصر|مفلح|مطلق|زيدان)\b\s*$/g, "").trim()) .map((p) =>normalizeBatchText(p)) .filter(Boolean); const branchKey = normalizeBatchText(branch); const branchIndex = parts.findIndex((p) =>p === branchKey); if (branchIndex< 0) return null; const lineage = parts.slice(0, branchIndex + 1).reverse(); if (lineage[0] !== branchKey || lineage.length< 2) return null; let parent = branchKey + " بن مطلق بن زيدان"; const relations = []; for (let i = 1; i< lineage.length; i += 1) { const child = parent + "/" + lineage[i]; relations.push({ parent, child, source: raw }); parent = child; } return { path: parent, relations, lineage, source: raw }; } function parseBatchShortLine(line) { const raw = cleanBatchLine(line); if (!raw) return null; if (raw === "الاسم") return null; const words = raw.split(/\s+/g).map((w) =>normalizeBatchText(w)).filter(Boolean); if (words.length< 2) return null; let pos = 0; const namePart = batchJoinAbd(words, pos); const name = namePart.value; pos = namePart.next; const fatherPart = batchJoinAbd(words, pos); const father = fatherPart.value; pos = fatherPart.next; const grandfatherPart = batchJoinAbd(words, pos); const grandfather = grandfatherPart.value; if (!name || !father) return null; return { raw, name, father, grandfather }; } function renderBatchSummary(result) { if (!batchTreeSummary) return; const r = result || {}; const items = [ ["الأسطر", r.lineCount || 0], ["العلاقات", (r.rows || []).length], ["مكرر داخل النص", (r.duplicates || []).length] ]; batchTreeSummary.innerHTML = items.map(([label, value]) =>( '<div class="batch-stat"><strong>' + escapeHtml(value) + '</strong><span>' + escapeHtml(label) + '</span></div>' )).join(""); } function renderBatchNotes(result) { if (!batchTreeNotes) return; const r = result || {}; const notes = []; (r.unresolved || []).forEach((x) =>notes.push("لم أتعرف على الأب: " + x)); (r.duplicates || []).slice(0, 25).forEach((x) =>notes.push("تكرار داخل النص: " + x)); (r.suspicious || []).slice(0, 25).forEach((x) =>notes.push("اسم مشتبه: " + x)); batchTreeNotes.innerHTML = notes.length ? notes.map((n) =>'<div class="batch-list-item">' + escapeHtml(n) + '</div>').join("") : '<div class="batch-list-item">لا توجد ملاحظات واضحة داخل النص.</div>'; } function buildBatchTreeSql() { const branch = normalizeBatchText(batchTreeBranch ? batchTreeBranch.value : ""); const raw = String(batchTreeInput ? batchTreeInput.value : ""); const lines = raw.split(/\r?\n/g).map(cleanBatchLine).filter(Boolean).filter((l) =>l !== "الاسم"); const root = branch + " بن مطلق بن زيدان"; const rows = []; const duplicates = []; const unresolved = []; const suspicious = []; const seenRelations = new Set(); const knownByLeaf = new Map(); let seq = 0; function remember(path) { const p = normalizeBatchText(path); const leaf = batchLeaf(p); if (!leaf) return; if (!knownByLeaf.has(leaf)) knownByLeaf.set(leaf, []); knownByLeaf.get(leaf).push({ path: p, seq: seq += 1 }); } function addRelation(parent, child, source) { const p = normalizeBatchText(parent); const c = normalizeBatchText(child); if (!p || !c || p === c) return; const key = p + "||" + c; if (seenRelations.has(key)) { duplicates.push(source || c); return; } seenRelations.add(key); rows.push({ branch, parent: p, child: c }); remember(p); remember(c); } function resolveParent(father, grandfather) { let candidates = (knownByLeaf.get(normalizeBatchText(father)) || []).slice(); const gf = normalizeBatchText(grandfather); if (gf) { const filtered = candidates.filter((c) =>batchLeaf(batchParentPath(c.path)) === gf); if (filtered.length) candidates = filtered; } candidates.sort((a, b) =>b.seq - a.seq); return candidates.length ? candidates[0].path : ""; } function addFullLineageRelations(full) { const lineage = full && Array.isArray(full.lineage) ? full.lineage : []; if (lineage.length< 2) return false; let parent = root; for (let i = 1; i< lineage.length; i += 1) { const leaf = normalizeBatchText(lineage[i]); if (!leaf) continue; const child = parent + "/" + leaf; addRelation(parent, child, full.source || child); parent = child; } return true; } remember(root); lines.forEach((line) =>{ if (/[^\u0600-\u06FF\s\d/\\.\-:،,؛\])(\[]/.test(line)) suspicious.push(line); const full = parseBatchFullLineage(line, branch); if (full && full.relations && full.relations.length) { addFullLineageRelations(full); return; } const short = parseBatchShortLine(line); if (!short) { suspicious.push(line); return; } const parent = resolveParent(short.father, short.grandfather); if (!parent) { unresolved.push(short.raw); return; } addRelation(parent, parent + "/" + short.name, short.raw); }); const values = rows.map((r) =>" (" + [r.branch, r.parent, r.child].map(batchSqlLiteral).join(", ") + ")").join(",\n"); const sql = rows.length ? `-- Generated by admin batch tree tool
-- يضيف العلاقات الناقصة فقط ولا يكرر الموجود.
do $$
declare r record; v_parent_person_id uuid;
begin create temporary table tmp_batch_tree_rows ( branch_key text, parent_name text, child_name text ) on commit drop; insert into tmp_batch_tree_rows (branch_key, parent_name, child_name) values
${values}; for r in select * from tmp_batch_tree_rows loop select min(c.person_id::text)::uuid into v_parent_person_id from public.tree_children c where c.branch_key = r.branch_key and coalesce(c.child_name, c.name) = r.parent_name and c.person_id is not null; insert into public.tree_children ( branch_key, parent_name, parent, child_name, name, person_id, parent_person_id, is_deceased, deceased, created_at ) select r.branch_key, r.parent_name, r.parent_name, r.child_name, r.child_name, gen_random_uuid(), v_parent_person_id, false, false, now() where not exists ( select 1 from public.tree_children c where c.branch_key = r.branch_key and coalesce(c.parent_name, c.parent) = r.parent_name and coalesce(c.child_name, c.name) = r.child_name ); end loop;
end $$; تحديث الخدمة, 'تحديث البيانات';
` : ""; return { branch, lineCount: lines.length, rows, duplicates, unresolved, suspicious, sql }; } function runBatchTreeBuild() { const result = buildBatchTreeSql(); if (batchTreeSql) batchTreeSql.value = result.sql || ""; renderBatchSummary(result); renderBatchNotes(result); if (!result.lineCount) { setBatchTreeStatus("الصق الأسماء أولاً."); } else if (!result.rows.length) { setBatchTreeStatus("لم يتم تجهيز علاقات. تأكد من وجود سطر كامل يحتوي: بن مطلق بن زيدان."); } else { setBatchTreeStatus("تم تجهيز أمر الصيانة بعدد " + result.rows.length + " علاقة. راجع الملاحظات قبل التنفيذ."); } return result; } async function runTreeImportFromFile(file) { const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } if (!file) { showAlert("error", "اختر ملف CSV أولاً."); return; } setTreeImportStatus("جاري قراءة الملف..."); const text = await file.text(); const rawRows = parseCsv(text); const rows = rawRows .map((r) =>{ const row = r || {}; const branch_key = pickRowValue(row, ["branch_key", "branch", "الفرع", "فرع", "العائلة", "العائله", "اسم_العائلة", "اسم_العائله"]); const parent_name = pickRowValue(row, ["parent_name", "parent", "father", "الأب", "الاب", "اسم_الأب", "اسم_الاب", "والد", "الوالد"]); const child_name = pickRowValue(row, ["child_name", "child", "name", "الاسم", "الاسم_كامل", "الاسم_الكامل", "الاسم كامل", "اسم"]); const birth_date_g = toIsoDateOrEmpty( pickRowValue(row, ["birth_date_g", "birth_date", "dob", "تاريخ_الميلاد", "تاريخ الميلاد", "تاريخ_الميلاد_ميلادي", "الميلاد"]) ); const birth_date_h = pickRowValue(row, ["birth_date_h", "hijri", "تاريخ_الميلاد_هجري", "تاريخ_الميلاد_الهجري", "هجري"]); const city = pickRowValue(row, ["city", "المدينة", "المدينه"]); const area = pickRowValue(row, ["area", "الحي", "المنطقة", "المنطقه"]); const birthYearRaw = pickRowValue(row, ["birth_year", "year", "سنة_الميلاد", "سنه_الميلاد", "سنة الميلاد", "سنه الميلاد"]); const ageRaw = pickRowValue(row, ["age", "العمر", "عمر"]); let birth_year = normalizeArabicDigitsToLatin(String(birthYearRaw || "").trim()); if (!birth_year) { const age = toIntOrNull(ageRaw); if (age != null && age >= 1 && age<= 120) { const y = new Date().getFullYear() - age; birth_year = String(y); } } const deceasedRaw = pickRowValue(row, ["is_deceased", "deceased", "متوفى", "متوفي", "وفاة", "وفاه"]); const deceasedBool = coerceBool(deceasedRaw); const out = { branch_key: String(branch_key || "").trim(), parent_name: String(parent_name || "").trim(), child_name: String(child_name || "").trim(), birth_date_g: String(birth_date_g || "").trim(), birth_date_h: String(birth_date_h || "").trim(), birth_year: String(birth_year || "").trim(), city: String(city || "").trim(), area: String(area || "").trim() }; if (deceasedBool != null) out.is_deceased = deceasedBool; return out; }) .filter((r) =>r.branch_key && r.parent_name && r.child_name); if (!rows.length) { showAlert("error", "لم يتم العثور على صفوف صالحة داخل الملف."); setTreeImportStatus(""); return; } const chunks = chunkArray(rows, 200); let inserted = 0; let updated = 0; let skipped = 0; for (let i = 0; i< chunks.length; i++) { setTreeImportStatus(`جاري الاستيراد... (${i + 1}/${chunks.length})`); const { data, error } = await sb.rpc("admin_tree_children_import_v1", { p_token: token, p_rows: chunks[i] }); if (error) { showAlert("error", "تعذر الاستيراد حالياً، حاول لاحقاً أو تواصل مع الإدارة."); setTreeImportStatus(""); return; } inserted += Number(data && data.inserted != null ? data.inserted : 0) || 0; updated += Number(data && data.updated != null ? data.updated : 0) || 0; skipped += Number(data && data.skipped != null ? data.skipped : 0) || 0; } showAlert("success", "تم الاستيراد بنجاح."); setTreeImportStatus(`إضافة: ${inserted} — تحديث: ${updated} — تجاهل: ${skipped}`); } function maybeOpenEmailDraft(to, subject, body) {
  return false;
} function getAdminNotificationEmail() { const email = normalizeEmail((adminNotifyEmail && adminNotifyEmail.value != null ? adminNotifyEmail.value : loadAdminNotifyEmail()) || ""); return isLikelyEmail(email) ? email : ""; } function initEmailSettings() { if (adminNotifyEmail) { adminNotifyEmail.value = loadAdminNotifyEmail(); adminNotifyEmail.addEventListener("input", () =>{ saveAdminNotifyEmail(adminNotifyEmail.value); setEmailStatus(""); }); } if (adminEmailNotifPending) { adminEmailNotifPending.checked = loadBoolSetting(ADMIN_EMAIL_PENDING_KEY, false); adminEmailNotifPending.addEventListener("change", () =>{ saveBoolSetting(ADMIN_EMAIL_PENDING_KEY, !!adminEmailNotifPending.checked); setEmailStatus(""); }); } if (adminEmailNotifAudit) { adminEmailNotifAudit.checked = loadBoolSetting(ADMIN_EMAIL_AUDIT_KEY, false); adminEmailNotifAudit.addEventListener("change", () =>{ saveBoolSetting(ADMIN_EMAIL_AUDIT_KEY, !!adminEmailNotifAudit.checked); setEmailStatus(""); if (adminEmailNotifAudit.checked) { seedAuditEmailKey().catch(() =>{}); } }); } if (adminEmailAutoOpen) { adminEmailAutoOpen.checked = loadBoolSetting(ADMIN_EMAIL_AUTO_OPEN_KEY, false); adminEmailAutoOpen.addEventListener("change", () =>{ saveBoolSetting(ADMIN_EMAIL_AUTO_OPEN_KEY, !!adminEmailAutoOpen.checked); setEmailStatus(""); }); } if (adminEmailTestBtn) { adminEmailTestBtn.addEventListener("click", async () =>{ const subject = "اختبار تنبيهات بريد الإدارة"; const body = [ "هذه رسالة اختبار من صفحة الإدارة.", "", "إذا وصلتك المسودة/تم نسخ النص فالإعدادات تعمل.", "التاريخ: " + formatDateTimeArSaVerbose(new Date()) ].join("\n"); await sendAdminEmail(subject, body); }); } } function formatDateTimeArSaVerbose(v) { if (!v) return ""; let date; if (v instanceof Date) date = v; else date = new Date(v); if (!date || !Number.isFinite(date.getTime())) return String(v || ""); let s = ""; try { s = date.toLocaleString("ar-SA"); } catch (e) { s = String(v || ""); } s = String(s || "").trim(); if (!s) return ""; s = s.replace(/\s*(AM|am)\s*$/, " صباحاً"); s = s.replace(/\s*(PM|pm)\s*$/, " مساءً"); s = s.replace(/\s*ص\s*$/, " صباحاً"); s = s.replace(/\s*م\s*$/, " مساءً"); return s; } async function sendAdminEmail(subject, body) { const to = getAdminNotificationEmail(); if (!to) { setEmailStatus("اكتب بريد الإدارة لتفعيل التنبيه."); return { ok: false, reason: "missing_email" }; } await copyText(body || ""); const autoOpen = !!(adminEmailAutoOpen && adminEmailAutoOpen.checked); if (autoOpen) { const opened = maybeOpenEmailDraft(to, subject, body); setEmailStatus(opened ? "تم فتح مسودة بريد (وتم نسخ النص)." : "تم نسخ نص البريد. المتصفح منع فتح المسودة."); return { ok: opened, reason: opened ? "opened" : "blocked" }; } setEmailStatus("تم نسخ نص البريد."); return { ok: true, reason: "copied" }; } async function seedAuditEmailKey() { const sb = getClient(); if (!sb) return; const token = getAdminToken(); if (!token) return; const fetchOne = async (kind) =>{ const { data, error } = await sb.rpc("admin_list_requests", { p_token: token, p_status: "approved", p_kind: kind, p_limit: 1 }); if (error) return null; const list = Array.isArray(data) ? data : []; return list && list[0] ? list[0] : null; }; const a = await fetchOne("tree_audit"); const b = await fetchOne("events_audit"); const pickLatest = (x, y) =>{ if (!x) return y || null; if (!y) return x || null; const ax = String(x.created_at || ""); const ay = String(y.created_at || ""); if (ay >ax) return y; if (ay< ax) return x; const ix = Number(x.id || 0); const iy = Number(y.id || 0); return iy >ix ? y : x; }; const latest = pickLatest(a, b); if (!latest) return; const key = String(latest.kind || "") + "|" + String(latest.request_id || latest.id || latest.created_at || ""); if (!key) return; lastEmailedAuditKey = key; didInitialAuditSync = true; try { localStorage.setItem(ADMIN_EMAIL_LAST_AUDIT_KEY, key); } catch (e) {} } function hideAlert() { if (!alertEl) return; alertEl.className = "alert"; alertEl.textContent = ""; alertEl.style.display = "none"; } function setStatus(text) { if (!sbStatus) return; sbStatus.textContent = text || ""; } function setProtectedVisibility(isAuthed) { const ok = !!isAuthed; document.body.classList.toggle("admin-authenticated", ok); if (adminCurrentUser) { const currentName = String(adminUsername && adminUsername.value ? adminUsername.value : "").trim(); adminCurrentUser.textContent = ok ? ("مسجل الدخول: " + (currentName || "الإدارة")) : ""; } if (adminProtectedInline) adminProtectedInline.style.display = ok ? "block" : "none"; if (adminProtectedSections) adminProtectedSections.style.display = ok ? "block" : "none"; if (adminLockedHint) adminLockedHint.style.display = ok ? "none" : "block"; if (adminLogoutBtn) adminLogoutBtn.disabled = !ok; if (adminRefreshBtn) adminRefreshBtn.disabled = !ok; if (adminEnableNotifsBtn) adminEnableNotifsBtn.disabled = !ok; if (refreshViewsStatsBtn) refreshViewsStatsBtn.disabled = !ok; if (refreshDelegateAuditBtn) refreshDelegateAuditBtn.disabled = !ok; if (sourceTreeLoad) sourceTreeLoad.disabled = !ok; if (sourceTreeNew) sourceTreeNew.disabled = !ok; if (sourceTreeForm) Array.from(sourceTreeForm.elements || []).forEach((el) =>{ el.disabled = !ok; }); if (sourceTreeDelete && ok && !(sourceTreeId && sourceTreeId.value)) sourceTreeDelete.disabled = true; } function updateEmailSettingsUi() { const authed = !!getAdminToken(); if (adminNotifyEmail) adminNotifyEmail.disabled = !authed; if (adminEmailNotifPending) adminEmailNotifPending.disabled = !authed; if (adminEmailNotifAudit) adminEmailNotifAudit.disabled = !authed; if (adminEmailAutoOpen) adminEmailAutoOpen.disabled = !authed; if (adminEmailTestBtn) adminEmailTestBtn.disabled = !authed; if (!authed) setEmailStatus(""); } function getClient() {
  if (sbClient) return sbClient;

  if (window.__alzidanConfig && typeof window.__alzidanConfig.getClient === "function") {
    const shared = window.__alzidanConfig.getClient();
    if (shared) {
      sbClient = shared;
      window.__alzidanSupabaseClient = shared;
      window.__alzidanالخدمةClient = shared;
      return sbClient;
    }
  }

  if (window.__alzidanSupabaseClient) {
    sbClient = window.__alzidanSupabaseClient;
    window.__alzidanالخدمةClient = sbClient;
    return sbClient;
  }

  if (window.__alzidanالخدمةClient) {
    sbClient = window.__alzidanالخدمةClient;
    window.__alzidanSupabaseClient = sbClient;
    return sbClient;
  }

  const url = String(SUPABASE_URL || "").trim();
  const anonKey = String(SUPABASE_ANON_KEY || "").trim();
  if (!url || !anonKey) return null;
  if (!window.supabase || typeof window.supabase.createClient !== "function") return null;

  sbClient = window.supabase.createClient(url, anonKey);
  window.__alzidanSupabaseClient = sbClient;
  window.__alzidanالخدمةClient = sbClient;
  return sbClient;
} function getAdminToken() { return String(adminToken || "").trim(); } function setSourceTreeStatus(text) { if (sourceTreeStatus) sourceTreeStatus.textContent = String(text || ""); } function escapeHtml(value) { return String(value == null ? "" : value) .replace(/&/g, "&amp;") .replace(/</g, "&lt;") .replace(/>/g, "&gt;") .replace(/"/g, "&quot;") .replace(/'/g, "&#039;"); } function getSourceTreeBranch() { return normalizeTreeCardText(sourceTreeBranch && sourceTreeBranch.value ? sourceTreeBranch.value : "لاحم") || "لاحم"; } function sourceTreeRoot(branch) { const b = normalizeTreeCardText(branch); return b ? b + " بن مطلق بن زيدان" : ""; } function sourceTreeRowPath(row) { const rawChild = normalizeTreeCardText(row && (row.child_name || row.name) ? (row.child_name || row.name) : ""); if (!rawChild) return ""; if (rawChild.includes("/")) return rawChild; const parent = sourceTreeParentPath(row); return parent ? parent + "/" + rawChild : rawChild; } function sourceTreeParentPath(row) { return normalizeTreeCardText(row && (row.parent_name || row.parent) ? (row.parent_name || row.parent) : ""); } function sourceTreeLeaf(path) { return relationLeafName(path); } function sourceTreeSortRows(rows) { return (Array.isArray(rows) ? rows.slice() : []).sort((a, b) =>{ const ap = sourceTreeParentPath(a); const bp = sourceTreeParentPath(b); if (ap !== bp) return ap.localeCompare(bp, "ar"); const ao = Number(a.birth_order || 0); const bo = Number(b.birth_order || 0); if (ao && bo && ao !== bo) return ao - bo; if (ao && !bo) return -1; if (!ao && bo) return 1; const ag = String(a.birth_date_g || ""); const bg = String(b.birth_date_g || ""); if (ag && bg && ag !== bg) return ag.localeCompare(bg); return sourceTreeLeaf(sourceTreeRowPath(a)).localeCompare(sourceTreeLeaf(sourceTreeRowPath(b)), "ar"); }); } function sourceTreeParentOfPath(path) { const p = normalizeTreeCardText(path || ""); const idx = p.lastIndexOf("/"); return idx >0 ? p.slice(0, idx) : ""; } function sourceTreeDisplayRows() { const branch = getSourceTreeBranch(); const root = sourceTreeRoot(branch); const byChildPath = new Map(); sourceTreeRows.forEach((row) =>{ const child = sourceTreeRowPath(row); if (!child) return; const existing = byChildPath.get(child); if (!existing || (row.id && !existing.id)) byChildPath.set(child, row); }); sourceTreeRows.forEach((row) =>{ let parent = sourceTreeParentPath(row); while (parent && parent !== root) { if (!byChildPath.has(parent)) { const grandParent = sourceTreeParentOfPath(parent) || root; byChildPath.set(parent, { _virtual: true, branch_key: branch, parent_name: grandParent, parent: grandParent, child_name: parent, name: parent, birth_order: null }); } parent = sourceTreeParentOfPath(parent); } }); return sourceTreeSortRows(Array.from(byChildPath.values())); } function sourceTreeChildrenCount(row) { const childPath = sourceTreeRowPath(row); const personId = normalizeTreeCardText(row && row.person_id ? row.person_id : ""); return sourceTreeRows.filter((x) =>{ const parentId = normalizeTreeCardText(x && x.parent_person_id ? x.parent_person_id : ""); if (personId && parentId && parentId === personId) return true; return sourceTreeParentPath(x) === childPath; }).length; } function refreshSourceTreeParentOptions(selectedValue) { if (!sourceTreeParent) return; const branch = getSourceTreeBranch(); const root = sourceTreeRoot(branch); const current = normalizeTreeCardText(selectedValue || sourceTreeParent.value || root); const paths = new Map(); if (root) paths.set(root, root); sourceTreeDisplayRows().forEach((row) =>{ const child = sourceTreeRowPath(row); if (child) paths.set(child, child); }); sourceTreeParent.innerHTML = ""; paths.forEach((path) =>{ const option = document.createElement("option"); option.value = path; option.textContent = relationPathLabel(path); sourceTreeParent.appendChild(option); }); if (current && !paths.has(current)) { const option = document.createElement("option"); option.value = current; option.textContent = relationPathLabel(current); sourceTreeParent.appendChild(option); } sourceTreeParent.value = current || root || ""; } function resetSourceTreeForm(parentValue) { if (sourceTreeId) sourceTreeId.value = ""; if (sourceTreePersonId) sourceTreePersonId.value = ""; refreshSourceTreeParentOptions(parentValue || sourceTreeRoot(getSourceTreeBranch())); if (sourceTreeName) sourceTreeName.value = ""; if (sourceTreeOrder) sourceTreeOrder.value = ""; if (sourceTreeBirthG) sourceTreeBirthG.value = ""; if (sourceTreeBirthH) sourceTreeBirthH.value = ""; if (sourceTreeAge) sourceTreeAge.value = ""; if (sourceTreeDeathG) sourceTreeDeathG.value = ""; if (sourceTreeDeathH) sourceTreeDeathH.value = ""; if (sourceTreeCity) sourceTreeCity.value = ""; if (sourceTreeArea) sourceTreeArea.value = ""; if (sourceTreeDeceased) sourceTreeDeceased.checked = false; if (sourceTreeDelete) sourceTreeDelete.disabled = true; setSourceTreeStatus("وضع إضافة شخص جديد."); } function fillSourceTreeForm(row) { clearSourceTreeExtraChildren(); if (!row) return resetSourceTreeForm(); const parent = sourceTreeParentPath(row); const child = sourceTreeRowPath(row); if (sourceTreeId) sourceTreeId.value = String(row.id || ""); if (sourceTreePersonId) sourceTreePersonId.value = String(row.person_id || ""); refreshSourceTreeParentOptions(parent); if (sourceTreeName) sourceTreeName.value = sourceTreeLeaf(child); if (sourceTreeOrder) sourceTreeOrder.value = row.birth_order == null ? "" : String(row.birth_order || ""); if (sourceTreeBirthG) sourceTreeBirthG.value = String(row.birth_date_g || "").slice(0, 10); if (sourceTreeBirthH) sourceTreeBirthH.value = String(row.birth_date_h || ""); if (sourceTreeDeathG) sourceTreeDeathG.value = String(row.death_date_g || "").slice(0, 10); if (sourceTreeDeathH) sourceTreeDeathH.value = String(row.death_date_h || ""); if (sourceTreeCity) sourceTreeCity.value = String(row.city || ""); if (sourceTreeArea) sourceTreeArea.value = String(row.area || ""); if (sourceTreeDeceased) sourceTreeDeceased.checked = !!(row.is_deceased || row.deceased); if (sourceTreeDelete) sourceTreeDelete.disabled = !row.id; setSourceTreeStatus("تعديل: " + relationPathLabel(child)); } function selectSourceTreeRow(row, itemEl) { if (sourceTreeList) sourceTreeList.querySelectorAll(".source-tree-item").forEach((x) =>x.classList.remove("active")); if (itemEl) itemEl.classList.add("active"); fillSourceTreeForm(row); } function startAddSourceTreeChild(row) { const child = sourceTreeRowPath(row); resetSourceTreeForm(child || sourceTreeRoot(getSourceTreeBranch())); setSourceTreeStatus("إضافة ابن تحت: " + relationPathLabel(child)); if (sourceTreeName) sourceTreeName.focus(); } function renderSourceTreeList() { if (!sourceTreeList) return; sourceTreeList.innerHTML = ""; const rows = sourceTreeDisplayRows(); if (!rows.length) { sourceTreeList.innerHTML = '<div class="hint">لا توجد بيانات لهذا الفرع بعد.</div>'; return; } rows.forEach((row) =>{ const item = document.createElement("div"); item.className = "source-tree-item"; item.dataset.id = String(row.id || ""); const child = sourceTreeRowPath(row); const parent = sourceTreeParentPath(row); const childrenCount = sourceTreeChildrenCount(row); const isVirtual = !!row._virtual; item.innerHTML = `<div class="source-tree-item-title">${escapeHtml(sourceTreeLeaf(child) || child)}</div><div class="source-tree-item-meta">الأب: ${escapeHtml(sourceTreeLeaf(parent) || parent || "-")} · الأبناء: ${childrenCount}${row.birth_order ? " · الترتيب: " + escapeHtml(row.birth_order) : ""}${isVirtual ? " · مستنتج من المسار" : ""}</div><div class="source-tree-item-actions"><button class="btn btn-outline btn-sm" type="button" data-source-tree-edit>تعديل</button><button class="btn btn-primary btn-sm" type="button" data-source-tree-add-child>إضافة ابن</button></div>`; const editBtn = item.querySelector("[data-source-tree-edit]"); const addChildBtn = item.querySelector("[data-source-tree-add-child]"); if (editBtn) editBtn.addEventListener("click", () =>selectSourceTreeRow(row, item)); if (addChildBtn) addChildBtn.addEventListener("click", () =>startAddSourceTreeChild(row)); item.addEventListener("dblclick", () =>selectSourceTreeRow(row, item)); sourceTreeList.appendChild(item); }); } async function loadSourceTreeRows() { const sb = getClient(); const token = getAdminToken(); if (!sb || !token) { setSourceTreeStatus("سجل الدخول أولًا."); return; } const branch = getSourceTreeBranch(); setSourceTreeStatus("جاري تحميل الشجرة..."); const fields = [ "id,person_id,parent_person_id,branch_key,parent_name,parent,child_name,name,birth_date_g,birth_date_h,birth_year,birth_order,death_date_g,death_date_h,city,area,is_deceased,deceased,created_at", "id,person_id,parent_person_id,branch_key,parent_name,parent,child_name,name,birth_date_g,birth_date_h,birth_year,birth_order,city,area,is_deceased,deceased,created_at", "id,branch_key,parent_name,parent,child_name,name,birth_date_g,birth_date_h,birth_year,birth_order,city,area,is_deceased,deceased,created_at" ]; let lastError = null; for (const f of fields) { const { data, error } = await sb.from("tree_children").select(f).eq("branch_key", branch).limit(3000); if (!error) { sourceTreeRows = Array.isArray(data) ? data : []; renderSourceTreeList(); resetSourceTreeForm(sourceTreeRoot(branch)); setSourceTreeStatus("تم تحميل " + sourceTreeRows.length + " علاقة."); return; } lastError = error; const msg = String(error.message || "").toLowerCase(); if (!(msg.includes("column") && msg.includes("does not exist"))) break; } sourceTreeRows = []; renderSourceTreeList(); setSourceTreeStatus("تعذر تحميل الشجرة حالياً، حاول لاحقاً أو تواصل مع الإدارة."); } function sourceTreeCurrentYear() { return new Date().getFullYear(); } function normalizeSourceTreeNumber(value) { return String(value || "") .replace(/[٠-٩۰-۹]/g, (digit) =>{ const code = digit.charCodeAt(0); const arabicZero = "٠".charCodeAt(0); const persianZero = "۰".charCodeAt(0); return String(code >= persianZero ? code - persianZero : code - arabicZero); }) .replace(/[^0-9]/g, ""); } function hijriYearToApproxGregorian(hijriYear) { const y = Number(hijriYear); if (!Number.isFinite(y) || y< 1200 || y >1700) return null; return Math.round((y * 0.970224) + 621.5774); } function getYearFromDateValue(value) { const raw = String(value || "").trim(); if (!raw) return null; const normalized = normalizeSourceTreeNumber(raw); const match = normalized.match(/(13|14|15|19|20)\d{2}/); if (!match) return null; return Number(match[0]); } function getReferenceYearForApproxAge() { const deathGregorianYear = getYearFromDateValue(sourceTreeDeathG && sourceTreeDeathG.value ? sourceTreeDeathG.value : ""); if (deathGregorianYear) return deathGregorianYear; const deathHijriYear = getYearFromDateValue(sourceTreeDeathH && sourceTreeDeathH.value ? sourceTreeDeathH.value : ""); const approxGregorian = deathHijriYear ? hijriYearToApproxGregorian(deathHijriYear) : null; if (approxGregorian) return approxGregorian; return sourceTreeCurrentYear(); } function birthYearFromApproxAge() { const raw = normalizeSourceTreeNumber(sourceTreeAge && sourceTreeAge.value ? sourceTreeAge.value : ""); if (!raw) return ""; const age = Number(raw); if (!Number.isFinite(age) || age< 0 || age >130) return ""; const deathHijriYear = getYearFromDateValue(sourceTreeDeathH && sourceTreeDeathH.value ? sourceTreeDeathH.value : ""); if (deathHijriYear) return String(deathHijriYear - age); return String(getReferenceYearForApproxAge() - age); }  function approxAgeFromBirthYear(yearValue) { const year = Number(normalizeSourceTreeNumber(yearValue)); if (!Number.isFinite(year) || year< 1800) return ""; const age = sourceTreeCurrentYear() - year; if (!Number.isFinite(age) || age< 0 || age >130) return ""; return String(age); } 
function clearSourceTreeExtraChildren() {
  if (sourceTreeExtraChildren) sourceTreeExtraChildren.innerHTML = "";
}
function nextSourceTreeOrderValue() {
  const first = Number(sourceTreeOrder && sourceTreeOrder.value ? sourceTreeOrder.value : 1);
  const base = Number.isFinite(first) && first > 0 ? first : 1;
  const count = sourceTreeExtraChildren ? sourceTreeExtraChildren.querySelectorAll("[data-extra-child-row]").length : 0;
  return base + count + 1;
}
function addSourceTreeExtraChildField(value) {
  if (!sourceTreeExtraChildren) return;
  const row = document.createElement("div");
  row.setAttribute("data-extra-child-row", "1");
  row.style.display = "grid";
  row.style.gridTemplateColumns = "minmax(0, 1fr) 120px auto";
  row.style.gap = "8px";
  row.style.alignItems = "end";
  row.innerHTML =
    '<div class="field"><label>اسم ابن إضافي</label><input data-extra-child-name type="text" placeholder="اسم الابن" value="' + escapeHtml(value || "") + '" /></div>' +
    '<div class="field"><label>الترتيب</label><input data-extra-child-order type="number" min="1" inputmode="numeric" value="' + String(nextSourceTreeOrderValue()) + '" /></div>' +
    '<button class="btn btn-outline btn-sm" type="button" data-extra-child-remove>حذف الخانة</button>';
  const remove = row.querySelector("[data-extra-child-remove]");
  if (remove) remove.addEventListener("click", () => row.remove());
  sourceTreeExtraChildren.appendChild(row);
}
function getSourceTreeExtraChildrenPayloads(basePayload) {
  if (!sourceTreeExtraChildren || !basePayload) return [];
  const rows = Array.from(sourceTreeExtraChildren.querySelectorAll("[data-extra-child-row]"));
  return rows.map((row) => {
    const nameEl = row.querySelector("[data-extra-child-name]");
    const orderEl = row.querySelector("[data-extra-child-order]");
    const name = normalizeTreeCardText(nameEl && nameEl.value ? nameEl.value : "");
    if (!name) return null;
    const child = name.includes("/") ? name : basePayload.parent_name + "/" + name;
    return Object.assign({}, basePayload, {
      id: "",
      person_id: "",
      child_name: child,
      birth_order: orderEl && orderEl.value ? String(orderEl.value) : ""
    });
  }).filter(Boolean);
}

/* BIRTH_DEATH_NORMALIZE_ENGINE_V1 */
function birthPad2(n) {
  return String(n).padStart(2, "0");
}

function normalizeDateDigitsOnly(value) {
  return String(value || "")
    .replace(/[٠-٩]/g, d => String("٠١٢٣٤٥٦٧٨٩".indexOf(d)))
    .replace(/[۰-۹]/g, d => String("۰۱۲۳۴۵۶۷۸۹".indexOf(d)))
    .trim();
}

function parseGregorianISO(value) {
  if (value instanceof Date && Number.isFinite(value.getTime())) return value;

  const raw = normalizeDateDigitsOnly(value);
  const m = raw.match(/^(\d{4})[-\/](\d{1,2})[-\/](\d{1,2})$/);
  if (!m) return null;

  const y = Number(m[1]);
  const mo = Number(m[2]);
  const d = Number(m[3]);

  if (y < 1800 || y > 2200 || mo < 1 || mo > 12 || d < 1 || d > 31) return null;

  const date = new Date(y, mo - 1, d);
  if (!Number.isFinite(date.getTime())) return null;

  if (date.getFullYear() !== y || date.getMonth() !== mo - 1 || date.getDate() !== d) return null;

  return date;
}

function gregorianDateToISO(date) {
  if (!(date instanceof Date) || !Number.isFinite(date.getTime())) return "";
  return date.getFullYear() + "-" + birthPad2(date.getMonth() + 1) + "-" + birthPad2(date.getDate());
}

function parseHijriParts(value) {
  const raw = normalizeDateDigitsOnly(value);
  const parts = raw.split(/\D+/).filter(Boolean).map(Number);
  if (!parts.length) return null;

  let y = null, m = 1, d = 1, full = false;

  if (parts[0] >= 1200 && parts[0] <= 1700) {
    y = parts[0];
    m = parts[1] || 1;
    d = parts[2] || 1;
    full = parts.length >= 3;
  } else {
    const yi = parts.findIndex(x => x >= 1200 && x <= 1700);
    if (yi === -1) return null;
    y = parts[yi];
    const before = parts.slice(0, yi);
    if (before.length >= 2) {
      d = before[0];
      m = before[1];
      full = true;
    } else if (before.length === 1) {
      m = before[0];
    }
  }

  if (y < 1200 || y > 1700 || m < 1 || m > 12 || d < 1 || d > 30) return null;
  return { y, m, d, full };
}

function hijriToISO(h) {
  if (!h || !h.y) return "";
  return String(h.y) + "-" + birthPad2(h.m || 1) + "-" + birthPad2(h.d || 1);
}

function hijriToGregorianISO(value) {
  const h = typeof value === "object" ? value : parseHijriParts(value);
  if (!h || !h.full) return "";

  const jd =
    Math.floor((11 * h.y + 3) / 30) +
    354 * h.y +
    30 * h.m -
    Math.floor((h.m - 1) / 2) +
    h.d +
    1948440 -
    385;

  let l = jd + 68569;
  const n = Math.floor((4 * l) / 146097);
  l = l - Math.floor((146097 * n + 3) / 4);
  const i = Math.floor((4000 * (l + 1)) / 1461001);
  l = l - Math.floor((1461 * i) / 4) + 31;
  const j = Math.floor((80 * l) / 2447);
  const day = l - Math.floor((2447 * j) / 80);
  l = Math.floor(j / 11);
  const month = j + 2 - 12 * l;
  const year = 100 * (n - 49) + i + l;

  return year + "-" + birthPad2(month) + "-" + birthPad2(day);
}

function gregorianToHijriISO(value) {
  const date = value instanceof Date ? value : parseGregorianISO(value);
  if (!(date instanceof Date) || !Number.isFinite(date.getTime())) return "";

  try {
    const parts = new Intl.DateTimeFormat("en-u-ca-islamic-umalqura", {
      year: "numeric",
      month: "2-digit",
      day: "2-digit"
    }).formatToParts(date);

    const y = parts.find(x => x.type === "year")?.value;
    const m = parts.find(x => x.type === "month")?.value;
    const d = parts.find(x => x.type === "day")?.value;

    if (y && m && d) return y + "-" + m + "-" + d;
  } catch (e) {}

  return "";
}

function shiftGregorianYears(date, yearsBack) {
  const d = new Date(date.getTime());
  d.setFullYear(d.getFullYear() - yearsBack);
  return d;
}

function normalizeBirthPayload(rawPayload) {
  const payload = Object.assign({}, rawPayload || {});

  function clean(v) {
    return String(v == null ? "" : v).trim();
  }

  let birthG = clean(payload.birth_date_g);
  let birthH = clean(payload.birth_date_h);
  let deathG = clean(payload.death_date_g);
  let deathH = clean(payload.death_date_h);
  const age = Number(normalizeDateDigitsOnly(payload._age || "").replace(/[^0-9]/g, ""));
  const isDead = !!(payload.is_deceased || payload.deceased);

  if (deathH && !deathG) {
    const h = parseHijriParts(deathH);
    if (h) {
      deathH = hijriToISO(h);
      deathG = h.full ? hijriToGregorianISO(h) : "";
    }
  }

  if (deathG && !deathH) {
    const d = parseGregorianISO(deathG);
    if (d) {
      deathG = gregorianDateToISO(d);
      deathH = gregorianToHijriISO(d) || "";
    }
  }

  if (birthH && !birthG) {
    const h = parseHijriParts(birthH);
    if (h) {
      birthH = h.full ? hijriToISO(h) : "تقريباً " + String(h.y);
      birthG = h.full ? hijriToGregorianISO(h) : "";
    }
  }

  if (birthG && !birthH) {
    const d = parseGregorianISO(birthG);
    if (d) {
      birthG = gregorianDateToISO(d);
      birthH = gregorianToHijriISO(d) || "";
    }
  }

  if (!birthG && !birthH && Number.isFinite(age) && age > 0 && age <= 130) {
    if (isDead && deathH) {
      const dh = parseHijriParts(deathH);
      if (dh) {
        const bh = { y: dh.y - age, m: dh.m || 1, d: dh.d || 1, full: !!dh.full };
        birthH = dh.full ? hijriToISO(bh) : "تقريباً " + String(bh.y);
        birthG = dh.full ? hijriToGregorianISO(bh) : "";
      }
    } else {
      const ref = isDead && deathG ? parseGregorianISO(deathG) : new Date();
      if (ref) {
        const bd = shiftGregorianYears(ref, age);
        birthG = gregorianDateToISO(bd);
        birthH = gregorianToHijriISO(bd) || "";
      }
    }
  }

  payload.birth_date_g = birthG || "";
  payload.birth_date_h = birthH || "";
  payload.death_date_g = deathG || "";
  payload.death_date_h = deathH || "";

  if (birthH) {
    const h = parseHijriParts(birthH);
    payload.birth_year = h && h.y ? String(h.y) : "";
  } else if (birthG) {
    const hText = gregorianToHijriISO(birthG);
    const h = parseHijriParts(hText);
    payload.birth_year = h && h.y ? String(h.y) : "";
  } else {
    payload.birth_year = clean(payload.birth_year);
  }

  delete payload._age;
  return payload;
}

function buildSourceTreePayload() {
  const branch = getSourceTreeBranch();
  const parent = normalizeTreeCardText(sourceTreeParent && sourceTreeParent.value ? sourceTreeParent.value : "");
  const name = normalizeTreeCardText(sourceTreeName && sourceTreeName.value ? sourceTreeName.value : "");

  if (!branch || !parent || !name) return null;

  const child = name.includes("/") ? name : parent + "/" + name;

  const rawPayload = {
    id: sourceTreeId && sourceTreeId.value ? sourceTreeId.value : "",
    person_id: sourceTreePersonId && sourceTreePersonId.value ? sourceTreePersonId.value : "",
    branch_key: branch,
    parent_name: parent,
    child_name: child,
    birth_date_g: sourceTreeBirthG && sourceTreeBirthG.value ? sourceTreeBirthG.value : "",
    birth_date_h: sourceTreeBirthH && sourceTreeBirthH.value ? normalizeTreeCardText(sourceTreeBirthH.value) : "",
    birth_year: "",
    birth_order: sourceTreeOrder && sourceTreeOrder.value ? String(sourceTreeOrder.value) : "",
    death_date_g: sourceTreeDeathG && sourceTreeDeathG.value ? sourceTreeDeathG.value : "",
    death_date_h: sourceTreeDeathH && sourceTreeDeathH.value ? normalizeTreeCardText(sourceTreeDeathH.value) : "",
    city: sourceTreeCity && sourceTreeCity.value ? normalizeTreeCardText(sourceTreeCity.value) : "",
    area: sourceTreeArea && sourceTreeArea.value ? normalizeTreeCardText(sourceTreeArea.value) : "",
    is_deceased: !!(sourceTreeDeceased && sourceTreeDeceased.checked),
    deceased: !!(sourceTreeDeceased && sourceTreeDeceased.checked),
    _age: sourceTreeAge && sourceTreeAge.value ? sourceTreeAge.value : ""
  };

  return normalizeBirthPayload(rawPayload);
} async function saveSourceTreeRow(event) { if (event) event.preventDefault(); const sb = getClient(); const token = getAdminToken(); const payload = buildSourceTreePayload(); if (!sb || !token) return setSourceTreeStatus("سجل الدخول أولًا."); if (!payload) return setSourceTreeStatus("أكمل الأب والاسم."); const extraPayloads = getSourceTreeExtraChildrenPayloads(payload); const payloads = [payload].concat(extraPayloads); setSourceTreeStatus("جاري الحفظ..."); let saved = 0; for (const rowPayload of payloads) { const { data, error } = await sb.rpc("admin_tree_child_upsert_v1", { p_token: token, p_row: rowPayload }); if (error) {
        const rawMsg = JSON.stringify({
          code: error.code || "",
          message: error.message || "",
          details: error.details || "",
          hint: error.hint || ""
        }, null, 2);
        try { console.error("SOURCE_TREE_SAVE_ERROR", error, rowPayload); } catch (_) {}
        setSourceTreeStatus("تعذر الحفظ: " + String(rowPayload.child_name || rowPayload.name || "") + " — تفاصيل الخطأ: " + rawMsg);
        return;
      } saved += 1; } clearSourceTreeExtraChildren(); setSourceTreeStatus("تم الحفظ. عدد السجلات: " + String(saved)); await loadSourceTreeRows(); } async function deleteSourceTreeSubtree() { const sb = getClient(); const token = getAdminToken(); const branch = getSourceTreeBranch(); const id = Number(sourceTreeId && sourceTreeId.value ? sourceTreeId.value : 0); const name = normalizeTreeCardText(sourceTreeName && sourceTreeName.value ? sourceTreeName.value : ""); if (!sb || !token) return setSourceTreeStatus("سجل الدخول أولًا."); if (!id) return setSourceTreeStatus("اختر شخصًا من القائمة أولًا."); const ok = window.confirm("سيتم حذف «" + name + "» وكل من تحته من الشجرة. هل أنت متأكد؟"); if (!ok) return; setSourceTreeStatus("جاري الحذف..."); const { data, error } = await sb.rpc("admin_tree_child_delete_subtree_v1", { p_token: token, p_branch_key: branch, p_id: id }); if (error) { const msg = formatRpcError(error); const missing = msg.toLowerCase().includes("could not find the function") || msg.toLowerCase().includes("does not exist") || String(error.code || "").toLowerCase() === "pgrst202"; setSourceTreeStatus(missing ? "الدالة غير ظاهرة لـ الخدمة بعد التهيئة. حاول تحديث الصفحة، أو تواصل مع الإدارة." + msg : "تعذر الحذف: " + msg); return; } setSourceTreeStatus("تم حذف " + String(data || 0) + " سجل."); await loadSourceTreeRows(); } function setEventsSourceStatus(message) { if (eventsSourceStatus) eventsSourceStatus.textContent = message || ""; } function eventTypeLabel(type) { const map = { happy: "فرح", death: "وفاة", sick: "مريض", birth: "مولود", graduation: "تخرج", promotion: "ترقية", meeting: "اجتماع", other: "أخرى" }; return map[type] || type || "مناسبة"; } function renderEventsSourceList() { if (!eventsSourceList) return; eventsSourceList.innerHTML = ""; if (!eventsSourceRows.length) { eventsSourceList.innerHTML = '<div class="hint">لا توجد أخبار أو مناسبات محملة.</div>'; return; } eventsSourceRows.forEach((row) =>{ const card = document.createElement("div"); card.className = "source-tree-item"; card.innerHTML = '<strong>' + escapeHtml(getEventCleanTitle(row)) + '</strong>' + '<div class="hint">' + escapeHtml(eventTypeLabel(row.type)) + ' · ' + escapeHtml(row.branch_key || "") + ' · ' + escapeHtml(row.created_at ? String(row.created_at).slice(0, 10) : "") + '</div>' + '<div style="margin-top:8px; display:flex; gap:8px; flex-wrap:wrap;">' + '<button class="btn btn-outline btn-sm" type="button">تعديل</button>' + '</div>'; const btn = card.querySelector("button"); if (btn) btn.addEventListener("click", () =>fillEventsSourceForm(row)); eventsSourceList.appendChild(card); }); } function parseEventDetailsClean(value) { if (!value) return {}; if (typeof value === "object") return value; try { const parsed = JSON.parse(String(value)); return parsed && typeof parsed === "object" ? parsed : { text: String(value || "") }; } catch (e) { return { text: String(value || "") }; } } function getEventCleanText(row) { const details = parseEventDetailsClean(row && row.details); return String(details.text || details.extra || details.notes || ""); } function getEventCleanTitle(row) { const details = parseEventDetailsClean(row && row.details); return String(details.title || row.person || getEventCleanText(row) || "بدون عنوان"); } function eventSourceTypeLabel(type) { const map = { birth: "عقيقة مولود", marriage: "زواج", graduation: "حفل تخرج", promotion: "حفل ترقية", new_house: "منزل جديد", gathering: "اجتماع عائلي", general: "مناسبة عامة", sick: "مريض", operation: "عملية", death: "وفاة", happy: "فرح", meeting: "اجتماع", other: "أخرى" }; return map[type] || type || "أخرى"; } function setBannerGeneralStatus(message) { if (bannerGeneralStatus) bannerGeneralStatus.textContent = message || ""; } function clampShowDays(value) { const n = Number(value); if (!Number.isFinite(n)) return 7; if (n< 1) return 1; if (n >7) return 7; return Math.round(n); } async function publishBannerGeneralNews(event) { if (event) event.preventDefault(); const sb = getClient(); const token = getAdminToken(); if (!sb || !token) return setBannerGeneralStatus("سجل الدخول أولاً."); const text = bannerGeneralText && bannerGeneralText.value ? bannerGeneralText.value.trim() : ""; const branch = bannerGeneralBranch && bannerGeneralBranch.value ? bannerGeneralBranch.value : "زيدان"; const showDays = clampShowDays(bannerGeneralShowDays && bannerGeneralShowDays.value ? bannerGeneralShowDays.value : 7); if (!text) return setBannerGeneralStatus("اكتب نص الخبر العام أولاً."); const details = { v: 1, kind: "general_notice", text, showDays }; setBannerGeneralStatus("جاري نشر الخبر العام..."); const { data, error } = await sb.rpc("admin_banner_message_create_v1", { p_token: token, p_branch_key: branch, p_message: text, p_show_days: showDays }); if (error) { setBannerGeneralStatus("تعذر نشر الخبر العام، حاول لاحقاً أو تواصل مع الإدارة."); return; } const verify = await sb .from("banner_messages") .select("id,message,created_at") .order("created_at", { ascending: false }) .limit(1); if (bannerGeneralText) bannerGeneralText.value = ""; if (verify && verify.data && verify.data[0]) { setBannerGeneralStatus("تم نشر الخبر العام. آخر سجل: #" + verify.data[0].id + " — " + verify.data[0].message); } else { setBannerGeneralStatus("تم تنفيذ الطلب، لكن لم أجد سجلاً جديداً في banner_messages."); } if (typeof loadBannerMessagesRows === "function") await loadBannerMessagesRows(); if (typeof loadEventsSourceRows === "function") await loadEventsSourceRows(); } function clearBannerGeneralForm() { if (bannerGeneralText) bannerGeneralText.value = ""; if (bannerGeneralShowDays) bannerGeneralShowDays.value = "7"; setBannerGeneralStatus(""); } function setBannerMessagesStatus(message) { if (bannerMessagesStatus) bannerMessagesStatus.textContent = message || ""; } function resetBannerMessagesForm() { if (bannerMessagesId) bannerMessagesId.value = ""; if (bannerMessagesBranch) bannerMessagesBranch.value = "زيدان"; if (bannerMessagesShowDays) bannerMessagesShowDays.value = "7"; if (bannerMessagesActive) bannerMessagesActive.checked = true; if (bannerMessagesText) bannerMessagesText.value = ""; setBannerMessagesStatus(""); } function renderBannerMessagesList() { if (!bannerMessagesList) return; bannerMessagesList.innerHTML = ""; if (!bannerMessagesRows.length) { bannerMessagesList.innerHTML = '<div class="hint">لا توجد أخبار عامة محملة.</div>'; return; } bannerMessagesRows.forEach((row) =>{ const card = document.createElement("div"); card.className = "source-tree-item"; const title = String(row.message || "بدون نص"); const shortTitle = title.length >80 ? title.slice(0, 80) + "..." : title; card.innerHTML = '<strong>#' + escapeHtml(row.id) + ' — ' + escapeHtml(shortTitle) + '</strong>' + '<div class="hint">' + escapeHtml(row.branch_key || "") + ' · ' + escapeHtml(row.is_active === false ? "غير مفعل" : "مفعل") + ' · ' + escapeHtml((row.show_days || 7) + " أيام") + ' · ' + escapeHtml(row.created_at ? String(row.created_at).slice(0, 10) : "") + '</div>' + '<div style="margin-top:8px; display:flex; gap:8px; flex-wrap:wrap;">' + '<button class="btn btn-outline btn-sm" type="button">تعديل</button>' + '</div>'; const btn = card.querySelector("button"); if (btn) btn.addEventListener("click", () =>fillBannerMessagesForm(row)); bannerMessagesList.appendChild(card); }); } async function loadBannerMessagesRows() { const sb = getClient(); if (!sb) return setBannerMessagesStatus("تعذر الاتصال، حاول لاحقاً أو تواصل مع الإدارة.."); setBannerMessagesStatus("جاري تحميل الأخبار العامة..."); const { data, error } = await sb .from("banner_messages") .select("id,branch_key,message,show_days,is_active,created_by,created_at") .order("created_at", { ascending: false }) .limit(300); if (error) { setBannerMessagesStatus("تعذر التحميل، حاول لاحقاً أو تواصل مع الإدارة."); return; } bannerMessagesRows = Array.isArray(data) ? data : []; renderBannerMessagesList(); setBannerMessagesStatus("تم تحميل " + bannerMessagesRows.length + " خبر عام."); } function fillBannerMessagesForm(row) { if (!row) return; if (bannerMessagesId) bannerMessagesId.value = row.id || ""; if (bannerMessagesBranch) bannerMessagesBranch.value = row.branch_key || "زيدان"; if (bannerMessagesShowDays) bannerMessagesShowDays.value = String(clampShowDays(row.show_days || 7)); if (bannerMessagesActive) bannerMessagesActive.checked = row.is_active !== false; if (bannerMessagesText) bannerMessagesText.value = row.message || ""; setBannerMessagesStatus("تعديل الخبر العام رقم #" + (row.id || "")); } async function saveBannerMessageRow(event) { if (event) event.preventDefault(); const sb = getClient(); const token = getAdminToken(); const id = Number(bannerMessagesId && bannerMessagesId.value ? bannerMessagesId.value : 0); const branch = bannerMessagesBranch && bannerMessagesBranch.value ? bannerMessagesBranch.value : "زيدان"; const showDays = clampShowDays(bannerMessagesShowDays && bannerMessagesShowDays.value ? bannerMessagesShowDays.value : 7); const isActive = bannerMessagesActive ? !!bannerMessagesActive.checked : true; const text = bannerMessagesText && bannerMessagesText.value ? bannerMessagesText.value.trim() : ""; if (!sb || !token) return setBannerMessagesStatus("سجل الدخول أولاً."); if (!id) return setBannerMessagesStatus("اختر خبراً عاماً من القائمة أولاً."); if (!text) return setBannerMessagesStatus("اكتب نص الخبر العام."); setBannerMessagesStatus("جاري حفظ الخبر العام..."); const { data, error } = await sb.rpc("admin_banner_message_update_v1", { p_token: token, p_id: id, p_branch_key: branch, p_message: text, p_show_days: showDays, p_is_active: isActive }); if (error) { setBannerMessagesStatus("تعذر الحفظ، حاول لاحقاً أو تواصل مع الإدارة."); return; } setBannerMessagesStatus("تم حفظ الخبر العام."); await loadBannerMessagesRows(); } async function deleteBannerMessageRow() { const sb = getClient(); const token = getAdminToken(); const id = Number(bannerMessagesId && bannerMessagesId.value ? bannerMessagesId.value : 0); if (!sb || !token) return setBannerMessagesStatus("سجل الدخول أولاً."); if (!id) return setBannerMessagesStatus("اختر خبراً عاماً أولاً."); const ok = window.confirm("سيتم حذف هذا الخبر العام نهائياً. هل أنت متأكد؟"); if (!ok) return; setBannerMessagesStatus("جاري حذف الخبر العام..."); const { data, error } = await sb.rpc("admin_banner_message_delete_v1", { p_token: token, p_id: id }); if (error) { setBannerMessagesStatus("تعذر الحذف، حاول لاحقاً أو تواصل مع الإدارة."); return; } resetBannerMessagesForm(); setBannerMessagesStatus("تم حذف الخبر العام."); await loadBannerMessagesRows(); } async function loadTickerSpeedSetting() { const sb = getClient(); if (!sb || !adminTickerSpeed) return; const { data, error } = await sb .from("site_settings") .select("value") .eq("key", "ticker_speed_web_seconds") .limit(1); if (!error && data && data[0] && data[0].value) { adminTickerSpeed.value = String(data[0].value); } const mobileRes = await sb .from("site_settings") .select("value") .eq("key", "ticker_speed_mobile_seconds") .limit(1); if (!mobileRes.error && mobileRes.data && mobileRes.data[0] && mobileRes.data[0].value && adminTickerMobileSpeed) { adminTickerMobileSpeed.value = String(mobileRes.data[0].value); } } async function saveTickerSpeedSetting() { const sb = getClient(); const token = getAdminToken(); const value = adminTickerSpeed && adminTickerSpeed.value ? adminTickerSpeed.value : "50"; const mobileValue = adminTickerMobileSpeed && adminTickerMobileSpeed.value ? adminTickerMobileSpeed.value : "3"; if (!sb || !token) return setBannerMessagesStatus("سجل الدخول أولاً."); setBannerMessagesStatus("جاري حفظ سرعة الشريط..."); const { error } = await sb.rpc("admin_site_setting_set_v1", { p_token: token, p_key: "ticker_speed_web_seconds", p_value: value }); if (error) { setBannerMessagesStatus("تعذر حفظ سرعة الويب، حاول لاحقاً أو تواصل مع الإدارة."); return; } const mobileSave = await sb.rpc("admin_site_setting_set_v1", { p_token: token, p_key: "ticker_speed_mobile_seconds", p_value: mobileValue }); if (mobileSave.error) { setBannerMessagesStatus("تعذر حفظ سرعة التطبيق، حاول لاحقاً أو تواصل مع الإدارة."); return; } setBannerMessagesStatus("تم حفظ السرعة: الويب " + value + " ثانية، التطبيق " + mobileValue + " ثانية."); } async function loadEventsSourceRows() { const sb = getClient(); if (!sb) return setEventsSourceStatus("تعذر الاتصال، حاول لاحقاً أو تواصل مع الإدارة.."); setEventsSourceStatus("جاري تحميل الأخبار والمناسبات..."); const { data, error } = await sb .from("family_events") .select("id,branch_key,type,person,date_label,event_date,details,hospital_name,hospital_dept,contact_method,contact_phone,visit_date_from,visit_date_to,visit_time_from,visit_time_to,created_at") .order("created_at", { ascending: false }) .limit(300); if (error) { setEventsSourceStatus("تعذر التحميل، حاول لاحقاً أو تواصل مع الإدارة."); return; } eventsSourceRows = Array.isArray(data) ? data : []; renderEventsSourceList(); setEventsSourceStatus("تم تحميل " + eventsSourceRows.length + " خبر/مناسبة."); } function fillEventsSourceForm(row) { if (!row) return; const details = parseEventDetailsClean(row.details); if (eventsSourceId) eventsSourceId.value = row.id || ""; if (eventsSourceBranch) eventsSourceBranch.value = row.branch_key || "زيدان"; if (eventsSourceType) eventsSourceType.value = row.type || "general"; if (eventsSourcePerson) eventsSourcePerson.value = row.person || ""; if (eventsSourceTitle) eventsSourceTitle.value = row.date_label || ""; if (eventsSourceGregorian) eventsSourceGregorian.value = row.event_date || ""; if (eventsSourceText) eventsSourceText.value = details.text || details.extra || details.notes || ""; if (eventsSourceImage) eventsSourceImage.value = details.imageUrl || details.image_url || details.photoUrl || details.photo_url || ""; if (eventsSourceVideo) eventsSourceVideo.value = details.videoUrl || details.video_url || ""; const adminImageFile = document.getElementById("admin-event-image-file"); const adminVideoFile = document.getElementById("admin-event-video-file"); if (adminImageFile) adminImageFile.value = ""; if (adminVideoFile) adminVideoFile.value = ""; setEventsSourceStatus("تعديل الخبر: " + (row.person || getEventCleanTitle(row) || row.id)); } function adminEventFileExtFromName(name, fallback) {
  const raw = String(name || "").split("?")[0].trim();
  const m = /\.([a-z0-9]{1,8})$/i.exec(raw);
  return (m ? m[1] : fallback || "bin").toLowerCase();
}
function adminEventPublicStorageUrl(path) {
  return String(SUPABASE_URL || "").replace(/\/+$/, "") + "/storage/v1/object/public/event-media/" + String(path || "").split("/").map(encodeURIComponent).join("/");
}
async function uploadAdminEventMedia(sb, file, kind) {
  if (!file) return "";
  const isImage = kind === "image";
  const fallback = isImage ? "jpg" : "mp4";
  const path = "admin_" + Date.now() + "_" + Math.random().toString(36).slice(2) + "_" + kind + "." + adminEventFileExtFromName(file.name, fallback);
  const { error } = await sb.storage.from("event-media").upload(path, file, { contentType: file.type || (isImage ? "image/jpeg" : "video/mp4"), upsert: false });
  if (error) throw new Error("تعذر رفع " + (isImage ? "الصورة" : "الفيديو") + ": " + (error.message || error.error || JSON.stringify(error)));
  return adminEventPublicStorageUrl(path);
}
async function enrichAdminEventPayloadWithUploadedMedia(sb, payload) {
  const imageFile = document.getElementById("admin-event-image-file");
  const videoFile = document.getElementById("admin-event-video-file");
  if (!payload || typeof payload !== "object") return payload;
  let details = {};
  try { details = payload.details ? JSON.parse(String(payload.details)) : {}; } catch (e) { details = {}; }
  if (imageFile && imageFile.files && imageFile.files[0]) details.imageUrl = await uploadAdminEventMedia(sb, imageFile.files[0], "image");
  if (videoFile && videoFile.files && videoFile.files[0]) details.videoUrl = await uploadAdminEventMedia(sb, videoFile.files[0], "video");
  payload.details = JSON.stringify(details);
  return payload;
}
 function buildEventsSourcePayload() { const id = Number(eventsSourceId && eventsSourceId.value ? eventsSourceId.value : 0); const selected = eventsSourceRows.find((item) =>Number(item.id) === id) || {}; const oldDetails = parseEventDetailsClean(selected.details); const text = eventsSourceText && eventsSourceText.value ? eventsSourceText.value.trim() : ""; const imageUrl = eventsSourceImage && eventsSourceImage.value ? eventsSourceImage.value.trim() : ""; const videoUrl = eventsSourceVideo && eventsSourceVideo.value ? eventsSourceVideo.value.trim() : ""; const details = { ...oldDetails, v: oldDetails.v || 1, kind: oldDetails.kind || "happy_notice", text, imageUrl, videoUrl, showDays: Number(oldDetails.showDays || 7) }; return { id, branch_key: eventsSourceBranch && eventsSourceBranch.value ? eventsSourceBranch.value : "", type: eventsSourceType && eventsSourceType.value ? eventsSourceType.value : "general", person: eventsSourcePerson && eventsSourcePerson.value ? eventsSourcePerson.value.trim() : "", date_label: eventsSourceTitle && eventsSourceTitle.value ? eventsSourceTitle.value.trim() : "", event_date: eventsSourceGregorian && eventsSourceGregorian.value ? eventsSourceGregorian.value.trim() : "", details: JSON.stringify(details), contact_phone: "", contact_method: "" }; } async function saveEventsSourceRow(event) { if (event) event.preventDefault(); const sb = getClient(); const token = getAdminToken(); let payload = buildEventsSourcePayload(); if (!sb || !token) return setEventsSourceStatus("سجل الدخول أولاً."); if (!payload) return setEventsSourceStatus("اختر خبراً أو مناسبة أولاً."); if (!payload.branch_key || !payload.type || !payload.person) return setEventsSourceStatus("اختر الفرع والنوع واكتب اسم الشخص أولاً."); setEventsSourceStatus("جاري رفع/تعديل الخبر/المناسبة..."); try { payload = await enrichAdminEventPayloadWithUploadedMedia(sb, payload); } catch (e) { setEventsSourceStatus(e && e.message ? e.message : "تعذر رفع الوسائط."); return; } let data = null;
let error = null;
if (payload && Number(payload.id || 0) > 0) {
  const res = await sb.rpc("admin_family_event_save_v1", { p_token: token, p_row: payload });
  data = res.data || null;
  error = res.error || null;
} else {
  const res = await sb.rpc("admin_family_event_insert_v1", { p_token: token, p_row: payload });
  data = res.data || null;
  error = res.error || null;
  if (!error && data && data.id && eventsSourceId) eventsSourceId.value = String(data.id);
}
if (error) { const msg = formatRpcError(error); try { console.error("ADMIN_EVENT_SAVE_ERROR", error); } catch (_) {} setEventsSourceStatus("تعذر الحفظ: " + msg); return; } try {
  const d = payload && payload.details ? JSON.parse(String(payload.details)) : {};
  const parts = [];
  if (d.imageUrl) parts.push("الصورة مرفوعة");
  if (d.videoUrl) parts.push("الفيديو مرفوع");
  setEventsSourceStatus("تم حفظ الخبر/المناسبة" + (parts.length ? " — " + parts.join(" و ") : "") + ".");
} catch (e) {
  setEventsSourceStatus("تم حفظ الخبر/المناسبة.");
}
await loadEventsSourceRows();
try {
  const d = payload && payload.details ? JSON.parse(String(payload.details)) : {};
  const parts = [];
  if (d.imageUrl) parts.push("الصورة مرفوعة");
  if (d.videoUrl) parts.push("الفيديو مرفوع");
  if (parts.length) setEventsSourceStatus("تم حفظ الخبر/المناسبة — " + parts.join(" و ") + ".");
} catch (e) {} } async function deleteEventsSourceRow() { const sb = getClient(); const token = getAdminToken(); const id = Number(eventsSourceId && eventsSourceId.value ? eventsSourceId.value : 0); if (!sb || !token) return setEventsSourceStatus("سجل الدخول أولاً."); if (!id) return setEventsSourceStatus("اختر خبراً أو مناسبة أولاً."); const ok = window.confirm("سيتم حذف هذا الخبر/المناسبة نهائياً . هل أنت متأكد؟"); if (!ok) return; setEventsSourceStatus("جاري الحذف..."); const { data, error } = await sb.rpc("admin_family_event_delete_v1", { p_token: token, p_id: id }); if (error) { const msg = formatRpcError(error); setEventsSourceStatus("تعذر الحذف، حاول لاحقاً أو تواصل مع الإدارة."); return; } if (eventsSourceId) eventsSourceId.value = ""; setEventsSourceStatus("تم حذف الخبر/المناسبة ."); await loadEventsSourceRows(); } function formatRpcError(error) {
                if (!error) return "تعذر تنفيذ العملية، حاول لاحقاً أو تواصل مع الإدارة.";
                console.warn("Admin operation error:", error);
                return "تعذر تنفيذ العملية، حاول لاحقاً أو تواصل مع الإدارة.";
            }

            function tokenFromRpcResult(data) { if (!data) return ""; if (typeof data === "string") return data.trim(); if (typeof data === "object") return String(data.token || data.session_token || "").trim(); return ""; } async function refreshAuthStatus() { const sb = getClient(); if (!sb) { setStatus("الخدمة غير جاهزة حالياً."); setProtectedVisibility(false); updateEmailSettingsUi(); return null; } const token = getAdminToken(); setStatus(token ? "" : "غير مسجل الدخول."); setProtectedVisibility(!!token); updateEmailSettingsUi(); return token ? { token } : null; } async function pollPendingRequestsForNotifications() { const sb = getClient(); if (!sb) return; const token = getAdminToken(); if (!token) return; const { data, error } = await sb.rpc("admin_list_requests", { p_token: token, p_status: "pending", p_kind: null, p_limit: 20 }); if (error) return; const list = Array.isArray(data) ? data : []; const newest = list && list[0] ? list[0] : null; if (!newest) { if (!didInitialPendingSync) didInitialPendingSync = true; return; } const key = String(newest.request_id || newest.id || newest.created_at || "").trim(); if (!key) return; if (!didInitialPendingSync) { didInitialPendingSync = true; saveLastNotifiedPendingKey(key); return; } if (key === lastNotifiedPendingKey) return; saveLastNotifiedPendingKey(key); showPendingRequestNotification(newest); if (adminEmailNotifPending && adminEmailNotifPending.checked) { const subject = "طلب جديد: " + kindLabel(newest.kind); const body = [ "طلب جديد في الإدارة", newest.request_id ? "رقم الطلب: " + newest.request_id : "", newest.kind ? "النوع: " + kindLabel(newest.kind) : "", newest.branch_key ? "الفرع: " + newest.branch_key : "", newest.name ? "الاسم: " + newest.name : "", newest.phone ? "الجوال: " + newest.phone : "", newest.email ? "البريد: " + newest.email : "", newest.created_at ? "التاريخ: " + formatDateTimeArSaVerbose(newest.created_at) : "" ] .filter(Boolean) .join("\n"); await sendAdminEmail(subject, body); } } async function pollAuditForEmailNotifications() { const sb = getClient(); if (!sb) return; const token = getAdminToken(); if (!token) return; if (!(adminEmailNotifAudit && adminEmailNotifAudit.checked)) return; const fetchOne = async (kind) =>{ const { data, error } = await sb.rpc("admin_list_requests", { p_token: token, p_status: "approved", p_kind: kind, p_limit: 1 }); if (error) return null; const list = Array.isArray(data) ? data : []; return list && list[0] ? list[0] : null; }; const a = await fetchOne("tree_audit"); const b = await fetchOne("events_audit"); const pickLatest = (x, y) =>{ if (!x) return y || null; if (!y) return x || null; const ax = String(x.created_at || ""); const ay = String(y.created_at || ""); if (ay >ax) return y; if (ay< ax) return x; const ix = Number(x.id || 0); const iy = Number(y.id || 0); return iy >ix ? y : x; }; const latest = pickLatest(a, b); if (!latest) return; const key = String(latest.kind || "") + "|" + String(latest.request_id || latest.id || latest.created_at || ""); if (!key) return; if (!didInitialAuditSync) { didInitialAuditSync = true; if (!lastEmailedAuditKey) { lastEmailedAuditKey = key; try { localStorage.setItem(ADMIN_EMAIL_LAST_AUDIT_KEY, key); } catch (e) {} return; } } if (key === lastEmailedAuditKey) return; lastEmailedAuditKey = key; try { localStorage.setItem(ADMIN_EMAIL_LAST_AUDIT_KEY, key); } catch (e) {} const subject = "تعديل مندوب: " + kindLabel(latest.kind); const body = [ "سجل تعديل جديد من المندوب", latest.kind ? "النوع: " + kindLabel(latest.kind) : "", latest.branch_key ? "الفرع: " + latest.branch_key : "", latest.name ? "المندوب: " + latest.name : "", latest.phone ? "الجوال: " + latest.phone : "", latest.email ? "البريد: " + latest.email : "", latest.created_at ? "التاريخ: " + formatDateTimeArSaVerbose(latest.created_at) : "", "", latest.message ? String(latest.message) : "" ] .filter(Boolean) .join("\n"); await sendAdminEmail(subject, body); } function startPendingPolling() { if (pendingPollTimer) return; pendingPollTimer = setInterval(() =>{ if (document.visibilityState !== "visible") return; pollPendingRequestsForNotifications().catch(() =>{}); pollAuditForEmailNotifications().catch(() =>{}); }, 20000); } function stopPendingPolling() { if (!pendingPollTimer) return; clearInterval(pendingPollTimer); pendingPollTimer = null; } function renderEmpty(text) { if (!requestsBody) return; requestsBody.innerHTML = ""; const tr = document.createElement("tr"); const td = document.createElement("td"); td.colSpan = 10; td.className = "hint"; td.textContent = text; tr.appendChild(td); requestsBody.appendChild(tr); } function statusLabel(status) { if (status === "pending") return "انتظار"; if (status === "approved") return "قبول"; if (status === "rejected") return "رفض"; return status || ""; } function coerceRpcId(v) { if (v == null) return ""; if (typeof v === "number" && Number.isFinite(v)) return v; const s = String(v || "").trim(); if (!s) return ""; if (/^-?\d+$/.test(s)) return s; return s; } function normalizeTreeCardText(v) { return String(v || "") .replace(/\s+/g, " ") .trim(); } function safeParseJsonTextLoose(v) { try { if (v == null) return null; const s = String(v || "").trim(); if (!s) return null; return JSON.parse(s); } catch (e) { return null; } } function extractTreeCardPayloadFromMessage(msg) { const text = String(msg || ""); const marker = "__JSON__:"; const idx = text.indexOf(marker); if (idx< 0) return null; const jsonText = text.slice(idx + marker.length).trim(); if (!jsonText) return null; const parsed = safeParseJsonTextLoose(jsonText); return parsed && typeof parsed === "object" ? parsed : null; } function updateBranchInRequestMessage(message, branchKey, kind) { const text = String(message || ""); const branch = normalizeTreeCardText(branchKey); if (!text || !branch) return text; if (kind === "tree_card") { const marker = "__JSON__:"; const idx = text.indexOf(marker); const visiblePart = idx >= 0 ? text.slice(0, idx).trimEnd() : text; const payload = extractTreeCardPayloadFromMessage(text); if (payload) { payload.branch_key = branch; const updatedVisible = /^العائلة \(إجباري\):.*$/m.test(visiblePart) ? visiblePart.replace(/^العائلة \(إجباري\):.*$/m, "العائلة (إجباري): " + branch) : visiblePart; return updatedVisible + "\n\n" + marker + "\n" + JSON.stringify(payload, null, 2); } } if (/^الفرع:.*$/m.test(text)) { return text.replace(/^الفرع:.*$/m, "الفرع: " + branch); } return text; } function normalizeAdminPhone(v) { return String(v || "") .replace(/[٠-٩]/g, (digit) =>String(digit.charCodeAt(0) - 1632)) .replace(/[۰-۹]/g, (digit) =>String(digit.charCodeAt(0) - 1776)) .replace(/[^\d+]/g, "") .trim(); } function extractRequestMediaLinks(message) { const media = { image: "", video: "" }; String(message || "").split(/\r?\n/).forEach((rawLine) =>{ const line = rawLine.trim(); const imageMatch = line.match(/^رابط الصورة\s*:\s*(https?:\/\/\S+)/i); const videoMatch = line.match(/^رابط الفيديو\s*:\s*(https?:\/\/\S+)/i); if (imageMatch && !media.image) media.image = imageMatch[1]; if (videoMatch && !media.video) media.video = videoMatch[1]; }); return media; } function requestMessageWithoutMediaLinks(message) { return String(message || "") .split(/\r?\n/) .filter((rawLine) =>{ const line = String(rawLine || "").trim(); if (/^رابط الصورة\s*:/i.test(line)) return false; if (/^رابط الفيديو\s*:/i.test(line)) return false; return true; }) .join("\n") .trim(); } function appendRequestMediaPreview(parent, message) { const media = extractRequestMediaLinks(message); if (!media.image && !media.video) return; const wrap = document.createElement("div"); wrap.className = "request-media-preview"; if (media.image) { const item = document.createElement("div"); item.className = "request-media-item"; const title = document.createElement("div"); title.className = "request-media-title"; title.textContent = "الصورة المرفقة"; const img = document.createElement("img"); img.alt = "الصورة المرفقة مع الطلب"; img.loading = "lazy"; img.src = media.image; const note = document.createElement("div"); note.className = "request-media-note"; note.textContent = "الصورة المرفقة مع الطلب."; item.appendChild(title); item.appendChild(img); item.appendChild(note); wrap.appendChild(item); } if (media.video) { const item = document.createElement("div"); item.className = "request-media-item"; const title = document.createElement("div"); title.className = "request-media-title"; title.textContent = "الفيديو المرفق"; const video = document.createElement("video"); video.controls = true; video.preload = "metadata"; video.src = media.video; item.appendChild(title); item.appendChild(video); wrap.appendChild(item); } parent.appendChild(wrap); } function requestLineValue(message, labels) { const wanted = (Array.isArray(labels) ? labels : [labels]).map((label) =>String(label || "").trim()); const lines = String(message || "").split(/\r?\n/); for (const rawLine of lines) { const line = String(rawLine || "").trim(); for (const label of wanted) { const prefix = label + ":"; if (line.startsWith(prefix)) return line.slice(prefix.length).trim(); } } return ""; } function parseJsonEnvelopeFromRequestMessage(message) { const marker = "__JSON__:"; const text = String(message || ""); const idx = text.indexOf(marker); if (idx< 0) return null; const raw = text.slice(idx + marker.length).trim(); if (!raw) return null; try { return JSON.parse(raw); } catch (e) { return null; } } function eventTypeFromLabel(label) { const s = normalizeTreeCardText(label || ""); if (s === "مولود") return "birth"; if (s === "زواج") return "marriage"; if (s === "تخرج") return "graduation"; if (s === "ترقية") return "promotion"; if (s === "اجتماع") return "gathering"; if (s === "مريض") return "sick"; if (s === "وفاة") return "death"; return "gathering"; } function buildEventCardRow(row) { const requestId = String(row && row.request_id ? row.request_id : "").trim(); const msg = String(row && row.message ? row.message : ""); const envelope = parseJsonEnvelopeFromRequestMessage(msg); if (envelope && envelope.event && typeof envelope.event === "object") { const event = envelope.event; let details = {}; if (typeof event.details === "string") { try { details = JSON.parse(event.details); } catch (e) { details = { text: event.details }; } } else if (event.details && typeof event.details === "object") { details = event.details; } details.requestId = requestId; return { branch_key: normalizeTreeCardText(row.branch_key || event.branch_key || ""), type: String(event.type || "gathering"), person: String(event.person || row.name || ""), date_label: String(event.date_label || ""), event_date: String(event.event_date || ""), details: JSON.stringify(details), hospital_name: String(event.hospital_name || ""), hospital_dept: String(event.hospital_dept || ""), contact_method: String(event.contact_method || ""), contact_phone: String(event.contact_phone || ""), visit_date_from: String(event.visit_date_from || ""), visit_date_to: String(event.visit_date_to || ""), visit_time_from: String(event.visit_time_from || ""), visit_time_to: String(event.visit_time_to || ""), created_at: String(event.created_at || row.created_at || new Date().toISOString()) }; } const media = extractRequestMediaLinks(msg); const typeLabel = requestLineValue(msg, ["نوع المناسبة", "النوع"]); const text = requestLineValue(msg, ["النص"]); const details = { v: 1, kind: "happy_notice", requestId, text, imageUrl: media.image, videoUrl: media.video, showDays: 7 }; return { branch_key: normalizeTreeCardText(row.branch_key || requestLineValue(msg, "الفرع")), type: eventTypeFromLabel(typeLabel), person: requestLineValue(msg, ["اسم صاحب المناسبة", "صاحب المناسبة"]) || String(row.name || ""), date_label: requestLineValue(msg, "التاريخ"), event_date: "", details: JSON.stringify(details), hospital_name: "", hospital_dept: "", contact_method: "", contact_phone: String(row.phone || ""), visit_date_from: "", visit_date_to: "", visit_time_from: "", visit_time_to: "", created_at: String(row.created_at || new Date().toISOString()) }; } async function publishEventCardRequest(sb, token, row) { const requestId = String(row && row.request_id ? row.request_id : "").trim(); if (!requestId) return { ok: false, message: "رقم الطلب ناقص." }; const eventRow = buildEventCardRow(row); if (!eventRow.branch_key || !eventRow.type || !eventRow.person) { return { ok: false, message: "بيانات المناسبة ناقصة، افتح عرض الطلب وتأكد من الفرع والنوع والاسم." }; } const { data, error } = await sb.rpc("admin_publish_event_card_v1", { p_token: token, p_request_id: requestId, p_row: eventRow }); if (error) { const msg = String(error.message || ""); if (msg.includes("تعذر تنفيذ العملية") || msg.includes("تحديث الخدمة")) { return { ok: false, needsSql: true, message: "تعذر نشر المناسبة حالياً، حاول لاحقاً أو تواصل مع الإدارة." }; } return { ok: false, message: "تعذر نشر المناسبة حالياً، حاول لاحقاً أو تواصل مع الإدارة." }; } if (data !== true) return { ok: false, message: "تعذر نشر المناسبة. تحقق من صلاحية الإدارة." }; return { ok: true }; } function buildTreeCardMessageFromPayload(payload, reqRow) { const ancestors = Array.isArray(payload.ancestors) ? payload.ancestors : []; const children = Array.isArray(payload.children) ? payload.children : []; const submitter = payload.submitter || {}; const lines = [ "بطاقة إضافة بيانات للشجرة", "", "رقم الطلب: " + String(reqRow.request_id || ""), "العائلة (إجباري): " + String(payload.branch_key || "") ]; const lineagePath = Array.isArray(payload.lineage_path) ? payload.lineage_path : []; const treeRows = Array.isArray(payload.tree_rows) ? payload.tree_rows : []; if (treeRows.length) { lines.push("العلاقات العائلية:"); treeRows.forEach((relation, idx) =>{ lines.push( String(idx + 1) + "- " + relationPathLabel(relation.parent_name) + " ← " + relationLeafName(relation.child_name) + (relation.birth_date_g ? " — " + relation.birth_date_g : "") ); }); } else if (lineagePath.length) { lines.push("مسار النسب من الأكبر إلى الأصغر:"); lineagePath.forEach((name, idx) =>lines.push(String(idx + 1) + "- " + name)); } else { lines.push("سلسلة الأجداد:"); ancestors.forEach((name, idx) =>lines.push("الجد " + String(idx + 1) + ": " + name)); } lines.push("الأب (إجباري): " + String(payload.father || "")); lines.push("الاسم (إجباري): " + String(payload.name || "")); lines.push("تاريخ الميلاد (اختياري): " + String(payload.birth_date_g || "")); lines.push("المدينة (اختياري): " + String(payload.city || "")); lines.push("الحي/القرية (اختياري): " + String(payload.area || "")); lines.push("", "الأبناء (اختياري):"); if (children.length) { children.forEach((child, idx) =>{ lines.push(String(idx + 1) + "- الاسم: " + child.name + " — تاريخ الميلاد: " + String(child.dob || "")); }); } else { lines.push("(لا يوجد)"); } lines.push("", "بيانات المرسل (إجباري):"); lines.push("الاسم: " + String(submitter.name || "")); lines.push("الجوال: " + String(submitter.phone || "")); lines.push("البريد (اختياري): " + String(submitter.email || "")); lines.push("التاريخ: " + formatDateTimeArSaVerbose(payload.created_at || reqRow.created_at || new Date().toISOString())); lines.push("", "__JSON__:", JSON.stringify(payload, null, 2)); return lines.join("\n"); } function parseEditedChildren(text) { const children = []; const lines = String(text || "").split(/\r?\n/); for (const raw of lines) { const line = normalizeTreeCardText(raw); if (!line) continue; const parts = line.split("|"); const name = normalizeTreeCardText(parts[0] || ""); const dob = normalizeTreeCardText(parts.slice(1).join("|") || ""); if (!name) continue; children.push({ name, dob }); } return children; } function showTreeCardEditError(text) { if (!treeCardEditError) return; treeCardEditError.textContent = String(text || ""); treeCardEditError.style.display = text ? "block" : "none"; } function relationLeafName(path) { const parts = String(path || "").split("/").map(normalizeTreeCardText).filter(Boolean); return parts.length ? parts[parts.length - 1] : ""; } function relationPathLabel(path) { return String(path || "") .split("/") .map(normalizeTreeCardText) .filter(Boolean) .join(" ← "); } function getRelationCards() { return treeCardRelations ? Array.from(treeCardRelations.querySelectorAll(".relation-card")) : []; } function getRelationParentPaths() { const branch = normalizeTreeCardText(treeCardEditForm && treeCardEditForm.elements.branch ? treeCardEditForm.elements.branch.value : ""); const root = branch ? branch + " بن مطلق بن زيدان" : ""; const paths = new Set(root ? [root] : []); getRelationCards().forEach((card) =>{ const parent = normalizeTreeCardText(card.querySelector('[name="relationParent"]')?.value || ""); const childName = normalizeTreeCardText(card.querySelector('[name="relationChild"]')?.value || ""); if (parent) paths.add(parent); if (parent && childName) paths.add(parent + "/" + childName); }); return Array.from(paths); } function refreshRelationParentOptions() { const paths = getRelationParentPaths(); getRelationCards().forEach((card) =>{ const select = card.querySelector('[name="relationParent"]'); if (!select) return; const current = normalizeTreeCardText(select.dataset.value || select.value || ""); select.innerHTML = ""; paths.forEach((path) =>{ const option = document.createElement("option"); option.value = path; option.textContent = relationPathLabel(path); select.appendChild(option); }); if (current && !paths.includes(current)) { const option = document.createElement("option"); option.value = current; option.textContent = relationPathLabel(current); select.appendChild(option); } select.value = current || paths[0] || ""; delete select.dataset.value; }); } function addRelationCard(relation) { if (!treeCardRelations) return; const card = document.createElement("div"); card.className = "relation-card"; card.innerHTML = `<div class="relation-fields"><div class="field"><label>الأب</label><select name="relationParent"></select></div><div class="field"><label>اسم الابن/الابنة</label><input name="relationChild" type="text" readonly /></div><div class="field"><label>تاريخ الميلاد</label><input name="relationDob" type="date" /></div></div><div class="relation-actions"><button class="btn btn-primary btn-sm" type="button" data-add-child-relation>إضافة أبناء</button><button class="btn btn-outline btn-sm" type="button" data-edit-relation>تعديل الاسم</button><button class="btn btn-outline btn-sm" type="button" data-remove-relation>حذف الشخص</button></div>`; const select = card.querySelector('[name="relationParent"]'); const child = card.querySelector('[name="relationChild"]'); const dob = card.querySelector('[name="relationDob"]'); const initialChildName = normalizeTreeCardText(relation && relation.child_name ? relationLeafName(relation.child_name) : ""); if (select) select.dataset.value = normalizeTreeCardText(relation && relation.parent_name ? relation.parent_name : ""); if (child) { child.value = initialChildName; child.readOnly = !!initialChildName; if (!initialChildName) { child.placeholder = "اكتب اسم الابن/الابنة"; child.addEventListener("input", refreshRelationParentOptions); } } if (dob) dob.value = normalizeTreeCardText(relation && relation.birth_date_g ? relation.birth_date_g : ""); const addChild = card.querySelector("[data-add-child-relation]"); if (addChild) { addChild.addEventListener("click", () =>{ const parent = normalizeTreeCardText(select ? select.value : ""); const childName = normalizeTreeCardText(child ? child.value : ""); if (!parent || !childName) { showTreeCardEditError("اكتب اسم الشخص أولًا ثم أضف أبناءه."); return; } const newChildName = normalizeTreeCardText(window.prompt("اكتب اسم الابن/الابنة:", "") || ""); if (!newChildName) return; showTreeCardEditError(""); addRelationCard({ parent_name: parent + "/" + childName, child_name: parent + "/" + childName + "/" + newChildName }); }); } const edit = card.querySelector("[data-edit-relation]"); if (edit) { edit.addEventListener("click", () =>{ const oldName = normalizeTreeCardText(child ? child.value : ""); const newName = normalizeTreeCardText(window.prompt("اكتب الاسم الصحيح:", oldName) || ""); if (!newName || newName === oldName) return; const parent = normalizeTreeCardText(select ? select.value : ""); const oldPath = parent && oldName ? parent + "/" + oldName : ""; const newPath = parent + "/" + newName; if (child) child.value = newName; getRelationCards().forEach((otherCard) =>{ const otherSelect = otherCard.querySelector('[name="relationParent"]'); if (!otherSelect || !oldPath) return; const current = normalizeTreeCardText(otherSelect.value || otherSelect.dataset.value || ""); if (current === oldPath || current.startsWith(oldPath + "/")) { otherSelect.dataset.value = newPath + current.slice(oldPath.length); } }); refreshRelationParentOptions(); }); } const remove = card.querySelector("[data-remove-relation]"); if (remove) { remove.addEventListener("click", () =>{ const parent = normalizeTreeCardText(select ? select.value : ""); const childName = normalizeTreeCardText(child ? child.value : ""); const removedPath = parent && childName ? parent + "/" + childName : ""; if (removedPath) { getRelationCards().forEach((otherCard) =>{ if (otherCard === card) return; const otherParent = normalizeTreeCardText(otherCard.querySelector('[name="relationParent"]')?.value || ""); if (otherParent === removedPath || otherParent.startsWith(removedPath + "/")) otherCard.remove(); }); } card.remove(); refreshRelationParentOptions(); }); } treeCardRelations.appendChild(card); refreshRelationParentOptions(); } function collectRelationRows(branch) { const rows = []; const seen = new Set(); for (const card of getRelationCards()) { const parent = normalizeTreeCardText(card.querySelector('[name="relationParent"]')?.value || ""); const childName = normalizeTreeCardText(card.querySelector('[name="relationChild"]')?.value || ""); const dob = normalizeTreeCardText(card.querySelector('[name="relationDob"]')?.value || ""); if (!parent && !childName) continue; if (!parent || !childName) return { ok: false, message: "كل علاقة تحتاج اختيار الأب وكتابة اسم الابن/الابنة.", rows: [] }; const child = parent + "/" + childName; const key = parent + "|" + child; if (seen.has(key)) continue; seen.add(key); rows.push({ branch_key: branch, parent_name: parent, child_name: child, birth_date_g: dob || "" }); } if (!rows.length) return { ok: false, message: "أضف علاقة عائلية واحدة على الأقل.", rows: [] }; return { ok: true, rows }; } function normalizeKnownLahmSalehRows(rows, branch) { const source = Array.isArray(rows) ? rows : []; if (normalizeTreeCardText(branch) !== "لاحم") return source; const root = "لاحم بن مطلق بن زيدان"; const badPrefixes = [ root + "/صالح سليمان عواد", root + "/عواد سليمان صالح", root + "/صالح/عواد" ]; const hasBadPath = source.some((item) =>{ const parent = normalizeTreeCardText(item && item.parent_name ? item.parent_name : ""); const child = normalizeTreeCardText(item && item.child_name ? item.child_name : ""); const leaf = relationLeafName(child); const isDirectBadAwwad = parent === root + "/صالح" && leaf === "عواد"; const isBadAwwadSon = (parent === "عواد" || parent === root + "/صالح/عواد") && leaf === "سليمان"; const isBadNaif = (parent === "سليمان" || parent === root + "/صالح/سليمان") && leaf === "نايف"; return isDirectBadAwwad || isBadAwwadSon || isBadNaif || badPrefixes.some((prefix) =>parent === prefix || parent.startsWith(prefix + "/") || child === prefix || child.startsWith(prefix + "/")); }); if (!hasBadPath) return source; const canonicalAwwad = root + "/صالح/سليمان/عواد"; const fixed = source .filter((item) =>{ const child = normalizeTreeCardText(item && item.child_name ? item.child_name : ""); return !badPrefixes.includes(child); }) .map((item) =>{ let parent = normalizeTreeCardText(item && item.parent_name ? item.parent_name : ""); let child = normalizeTreeCardText(item && item.child_name ? item.child_name : ""); const leaf = relationLeafName(child); if (parent === root + "/صالح" && leaf === "عواد") { parent = root + "/صالح/سليمان"; child = canonicalAwwad; } else if ((parent === "عواد" || parent === root + "/صالح/عواد") && leaf === "سليمان") { parent = canonicalAwwad; child = canonicalAwwad + "/سليمان"; } else if ((parent === "سليمان" || parent === root + "/صالح/سليمان") && leaf === "نايف") { parent = canonicalAwwad + "/سليمان"; child = canonicalAwwad + "/سليمان/نايف"; } badPrefixes.forEach((prefix) =>{ if (parent === prefix || parent.startsWith(prefix + "/")) parent = canonicalAwwad + parent.slice(prefix.length); if (child === prefix || child.startsWith(prefix + "/")) child = canonicalAwwad + child.slice(prefix.length); }); return { ...(item || {}), branch_key: "لاحم", parent_name: parent, child_name: child }; }); const required = [ { branch_key: "لاحم", parent_name: root, child_name: root + "/صالح" }, { branch_key: "لاحم", parent_name: root + "/صالح", child_name: root + "/صالح/سليمان" }, { branch_key: "لاحم", parent_name: root + "/صالح/سليمان", child_name: canonicalAwwad }, { branch_key: "لاحم", parent_name: canonicalAwwad, child_name: canonicalAwwad + "/سليمان" }, { branch_key: "لاحم", parent_name: canonicalAwwad + "/سليمان", child_name: canonicalAwwad + "/سليمان/نايف" } ]; const seen = new Set(); return required.concat(fixed).filter((item) =>{ const parent = normalizeTreeCardText(item && item.parent_name ? item.parent_name : ""); const child = normalizeTreeCardText(item && item.child_name ? item.child_name : ""); const key = parent + "|" + child; if (!parent || !child || seen.has(key)) return false; seen.add(key); return true; }); } function openTreeCardEditor(row) { const payload = extractTreeCardPayloadFromMessage(row && row.message ? row.message : ""); if (!payload || !treeCardEditForm || !treeCardEditDialog) { showAlert("error", "تعذر قراءة تفاصيل بطاقة الشجرة."); return; } treeCardEditRow = row; const submitter = payload.submitter || {}; treeCardEditForm.elements.branch.value = normalizeTreeCardText(payload.branch_key || row.branch_key || ""); if (treeCardRelations) treeCardRelations.innerHTML = ""; const built = buildTreeCardRows(row, treeCardEditForm.elements.branch.value); const rawInitialRows = Array.isArray(payload.tree_rows) && payload.tree_rows.length ? payload.tree_rows : (built.ok ? built.rows : []); const initialRows = normalizeKnownLahmSalehRows( rawInitialRows, treeCardEditForm.elements.branch.value ); initialRows.forEach((relation) =>addRelationCard(relation)); if (!initialRows.length) addRelationCard(null); treeCardEditForm.elements.submitterName.value = normalizeTreeCardText(submitter.name || row.name || ""); treeCardEditForm.elements.submitterPhone.value = normalizeAdminPhone(submitter.phone || row.phone || ""); treeCardEditForm.elements.submitterEmail.value = normalizeEmail(submitter.email || row.email || ""); showTreeCardEditError(""); treeCardEditDialog.showModal(); } function buildTreeCardRows(reqRow, branchOverride) { const payload = extractTreeCardPayloadFromMessage(reqRow && reqRow.message ? reqRow.message : ""); if (!payload) return { ok: false, message: "تعذر قراءة بيانات البطاقة (JSON غير موجود).", rows: [] }; const branchKey = normalizeTreeCardText(branchOverride || payload.branch_key || reqRow.branch_key || ""); const father = normalizeTreeCardText(payload.father || ""); const personName = normalizeTreeCardText(payload.name || ""); const personDob = normalizeTreeCardText(payload.birth_date_g || ""); const city = normalizeTreeCardText(payload.city || ""); const area = normalizeTreeCardText(payload.area || ""); if (!branchKey) { return { ok: false, message: "بيانات البطاقة ناقصة (العائلة).", rows: [] }; } const createdAt = normalizeTreeCardText(payload.created_at || reqRow.created_at || new Date().toISOString()); const rows = []; const seen = new Set(); function pushEdge(parent, child, extra) { const p = normalizeTreeCardText(parent || ""); const c = normalizeTreeCardText(child || ""); if (!p || !c) return; const key = branchKey + "|" + p + "|" + c; if (seen.has(key)) return; seen.add(key); const row = { branch_key: branchKey, parent_name: p, child_name: c, created_at: createdAt }; if (extra && typeof extra === "object") Object.assign(row, extra); rows.push(row); } const customRows = Array.isArray(payload.tree_rows) ? payload.tree_rows : []; if (customRows.length) { customRows.forEach((item) =>{ const parent = normalizeTreeCardText(item && item.parent_name ? item.parent_name : ""); const child = normalizeTreeCardText(item && item.child_name ? item.child_name : ""); if (!parent || !child) return; pushEdge(parent, child, { birth_date_g: normalizeTreeCardText(item.birth_date_g || ""), city: normalizeTreeCardText(item.city || ""), area: normalizeTreeCardText(item.area || "") }); }); return { ok: true, rows }; } if (!father || !personName) { return { ok: false, message: "بيانات البطاقة ناقصة (الأب/الاسم).", rows: [] }; } const lineagePath = Array.isArray(payload.lineage_path) ? payload.lineage_path.map((v) =>normalizeTreeCardText(v)).filter(Boolean) : []; if (lineagePath.length) { const branchRoot = branchKey + " بن مطلق بن زيدان"; let parentId = branchRoot; lineagePath.forEach((baseName, idx) =>{ const childId = parentId + "/" + baseName; const isLeaf = idx === lineagePath.length - 1; pushEdge( parentId, childId, isLeaf ? { birth_date_g: personDob || "", city: city || "", area: area || "" } : null ); parentId = childId; }); const kids = Array.isArray(payload.children) ? payload.children : []; kids.forEach((c) =>{ const childName = normalizeTreeCardText(c && c.name ? c.name : ""); const childDob = normalizeTreeCardText(c && c.dob ? c.dob : ""); if (!childName) return; pushEdge(parentId, parentId + "/" + childName, { birth_date_g: childDob || "" }); }); return { ok: true, rows }; } const ancestorsFromArray = Array.isArray(payload.ancestors) ? payload.ancestors : []; const ancestorsFromFields = [payload.grandfather, payload.grandfather2, payload.grandfather3, payload.grandfather4].filter(Boolean); const ancestorsClosestFirst = (ancestorsFromArray.length ? ancestorsFromArray : ancestorsFromFields) .map((v) =>normalizeTreeCardText(v)) .filter(Boolean); const farthestFirst = ancestorsClosestFirst.slice().reverse(); for (let i = 0; i + 1< farthestFirst.length; i += 1) { pushEdge(farthestFirst[i], farthestFirst[i + 1]); } if (ancestorsClosestFirst.length) { pushEdge(ancestorsClosestFirst[0], father); } pushEdge(father, personName, { birth_date_g: personDob || "", city: city || "", area: area || "" }); const kids = Array.isArray(payload.children) ? payload.children : []; kids.forEach((c) =>{ const childName = normalizeTreeCardText(c && c.name ? c.name : ""); const childDob = normalizeTreeCardText(c && c.dob ? c.dob : ""); if (!childName) return; pushEdge(personName, childName, { birth_date_g: childDob || "" }); }); return { ok: true, rows }; } if (treeCardAddRelation) { treeCardAddRelation.addEventListener("click", () =>addRelationCard(null)); } if (treeCardEditForm && treeCardEditForm.elements.branch) { treeCardEditForm.elements.branch.addEventListener("change", () =>{ const branch = normalizeTreeCardText(treeCardEditForm.elements.branch.value); const root = branch ? branch + " بن مطلق بن زيدان" : ""; getRelationCards().forEach((card) =>{ const select = card.querySelector('[name="relationParent"]'); if (select && select.value.includes(" بن مطلق بن زيدان") && !select.value.includes("/")) { select.dataset.value = root; } }); refreshRelationParentOptions(); }); } if (treeCardEditCancel) { treeCardEditCancel.addEventListener("click", () =>{ treeCardEditRow = null; showTreeCardEditError(""); if (treeCardEditDialog && treeCardEditDialog.open) treeCardEditDialog.close(); }); } if (treeCardEditForm) { treeCardEditForm.addEventListener("submit", async (event) =>{ event.preventDefault(); showTreeCardEditError(""); const row = treeCardEditRow; if (!row) { showTreeCardEditError("تعذر تحديد الطلب."); return; } const branch = normalizeTreeCardText(treeCardEditForm.elements.branch.value); const relations = collectRelationRows(branch); const submitterName = normalizeTreeCardText(treeCardEditForm.elements.submitterName.value); const submitterPhone = normalizeAdminPhone(treeCardEditForm.elements.submitterPhone.value); const submitterEmail = normalizeEmail(treeCardEditForm.elements.submitterEmail.value); if (!branch || !relations.ok || !submitterName || submitterPhone.length< 9) { showTreeCardEditError(relations.ok ? "أكمل الفرع وبيانات المرسل." : relations.message); return; } if (submitterEmail && !isLikelyEmail(submitterEmail)) { showTreeCardEditError("البريد الإلكتروني غير صحيح."); return; } const oldPayload = extractTreeCardPayloadFromMessage(row.message) || {}; const lastRelation = relations.rows[relations.rows.length - 1]; const personName = relationLeafName(lastRelation.child_name); const father = relationLeafName(lastRelation.parent_name); const payload = { ...oldPayload, v: 1, kind: "tree_card", branch_key: branch, grandfather: "", ancestors: [], lineage_path: [], tree_rows: relations.rows, father, name: personName, birth_date_g: lastRelation.birth_date_g || "", city: "", area: "", children: [], submitter: { name: submitterName, phone: submitterPhone, email: submitterEmail }, created_at: oldPayload.created_at || row.created_at || new Date().toISOString() }; const message = buildTreeCardMessageFromPayload(payload, row); const oldBuilt = buildTreeCardRows(row, row.branch_key || oldPayload.branch_key || ""); if (!oldBuilt.ok) { showTreeCardEditError(oldBuilt.message || "تعذر تجهيز بيانات الشجرة."); return; } const sb = getClient(); const token = getAdminToken(); const id = coerceRpcId(row.id != null ? row.id : row.request_id); if (!sb || !token || !id) { showTreeCardEditError("يلزم تسجيل الدخول والاتصال بقاعدة البيانات."); return; } const submitBtn = treeCardEditForm.querySelector('button[type="submit"]'); if (submitBtn) submitBtn.disabled = true; const { data, error } = await sb.rpc("admin_update_request_branch_v1", { p_token: token, p_id: String(id), p_old_branch_key: normalizeTreeCardText(row.branch_key || oldPayload.branch_key || "") || null, p_branch_key: branch, p_name: submitterName, p_phone: submitterPhone, p_email: submitterEmail || null, p_message: message, p_old_tree_rows: oldBuilt.rows, p_new_tree_rows: relations.rows }); if (submitBtn) submitBtn.disabled = false; if (error) { const errorText = String(error.message || ""); const missingRpc = errorText.toLowerCase().includes("could not find the function") || errorText.toLowerCase().includes("does not exist") || String(error.code || "").toLowerCase() === "pgrst202"; showTreeCardEditError( missingRpc ? "تعذر حفظ التعديلات حالياً، حاول لاحقاً أو تواصل مع الإدارة." : "تعذر حفظ التعديلات حالياً، حاول لاحقاً أو تواصل مع الإدارة." ); return; } if (data !== true) { showTreeCardEditError("لا يمكن تعديل هذا الطلب في حالته الحالية."); return; } treeCardEditRow = null; treeCardEditDialog.close(); showAlert("success", row.status === "approved" ? "تم تعديل الطلب وتصحيح بيانات الشجرة." : "تم تعديل الطلب كاملًا."); await loadRequests(); }); } async function importTreeCardToTree(sb, token, reqRow) { const built = buildTreeCardRows(reqRow); if (!built.ok) return built; const rows = built.rows; const { data, error } = await sb.rpc("admin_tree_children_import_v1", { p_token: token, p_rows: rows }); if (error) { const msg = String(error.message || ""); const low = msg.toLowerCase(); const isMissing = low.includes("could not find the function") || low.includes("does not exist") || String(error.code || "").toLowerCase() === "pgrst202"; if (isMissing) { return { ok: false, message: "تعذر إضافة البيانات للشجرة حالياً، حاول لاحقاً أو تواصل مع الإدارة." }; } return { ok: false, message: "تعذر إضافة البيانات للشجرة حالياً، حاول لاحقاً أو تواصل مع الإدارة." }; } const inserted = data && data.inserted != null ? Number(data.inserted) : null; const updated = data && data.updated != null ? Number(data.updated) : null; const skipped = data && data.skipped != null ? Number(data.skipped) : null; const parts = []; if (Number.isFinite(inserted)) parts.push("جديد: " + String(inserted)); if (Number.isFinite(updated)) parts.push("تحديث: " + String(updated)); if (Number.isFinite(skipped)) parts.push("تخطي: " + String(skipped)); return { ok: true, message: parts.length ? parts.join("، ") : "" }; } function buildRequestDetailsText(row) { const lines = [ row.request_id ? "رقم الطلب: " + row.request_id : "", row.branch_key ? "الفرع: " + row.branch_key : "", row.phone ? "الجوال: " + row.phone : "", row.email ? "البريد: " + row.email : "", row.created_at ? "التاريخ الكامل: " + formatDateTimeArSaVerbose(row.created_at) : "", "", requestMessageWithoutMediaLinks(row.message || "") ].filter((line, index, arr) =>line || (index >0 && index< arr.length - 1)); return lines.join("\n").trim() || "لا توجد تفاصيل إضافية."; } function formatDateShortForRequests(value) { if (!value) return ""; try { const d = new Date(value); if (isNaN(d.getTime())) return String(value || ""); return d.toLocaleDateString("ar-SA", { year: "numeric", month: "2-digit", day: "2-digit" }); } catch (e) { return String(value || ""); } } function renderRequestRow(row) { if (!requestsBody) return; const tr = document.createElement("tr"); function tdText(text) { const td = document.createElement("td"); td.textContent = text || ""; return td; } tr.appendChild(tdText(row.request_id || "")); tr.appendChild(tdText(kindLabel(row.kind))); tr.appendChild(tdText(row.branch_key || "")); tr.appendChild(tdText(row.name || "")); tr.appendChild(tdText(row.phone || "")); tr.appendChild(tdText(row.email || "")); const tdStatus = document.createElement("td"); const pill = document.createElement("span"); pill.className = "status-pill " + (row.status === "approved" ? "status-approved" : row.status === "rejected" ? "status-rejected" : "status-pending"); pill.textContent = statusLabel(row.status); tdStatus.appendChild(pill); tr.appendChild(tdStatus); const tdDate = document.createElement("td"); if (row.created_at) { try { tdDate.textContent = formatDateShortForRequests(row.created_at); } catch (e) { tdDate.textContent = String(row.created_at || ""); } } else { tdDate.textContent = ""; } tr.appendChild(tdDate); const tdMsg = document.createElement("td"); const det = document.createElement("details"); det.className = "msg"; const sum = document.createElement("summary"); sum.textContent = "عرض"; const body = document.createElement("div"); body.textContent = buildRequestDetailsText(row); det.appendChild(sum); det.appendChild(body); appendRequestMediaPreview(body, row.message || ""); tdMsg.appendChild(det); tr.appendChild(tdMsg); const tdActions = document.createElement("td"); const actions = document.createElement("div"); actions.className = "cell-actions"; const approveBtn = document.createElement("button"); approveBtn.type = "button"; approveBtn.className = "btn btn-primary btn-sm"; approveBtn.textContent = "قبول"; const publishEventBtn = document.createElement("button"); publishEventBtn.type = "button"; publishEventBtn.className = "btn btn-primary btn-sm"; publishEventBtn.textContent = "نشر"; publishEventBtn.title = "نشر المناسبة في الويب والتطبيق"; const rejectBtn = document.createElement("button"); rejectBtn.type = "button"; rejectBtn.className = "btn btn-outline btn-sm"; rejectBtn.textContent = "رفض"; const editBranchBtn = document.createElement("button"); editBranchBtn.type = "button"; editBranchBtn.className = "btn btn-outline btn-sm"; editBranchBtn.textContent = row.kind === "tree_card" ? "تعديل كامل" : "تعديل الفرع"; const deleteBtn = document.createElement("button"); deleteBtn.type = "button"; deleteBtn.className = "btn btn-outline btn-sm btn-danger"; deleteBtn.textContent = "حذف"; const canApprove = row.status !== "approved"; const canReject = row.status !== "rejected"; approveBtn.disabled = !canApprove; rejectBtn.disabled = !canReject; editBranchBtn.disabled = row.status !== "pending" && row.status !== "approved"; actions.appendChild(approveBtn); actions.appendChild(rejectBtn); if (row.kind === "event_card") actions.appendChild(publishEventBtn); actions.appendChild(editBranchBtn); actions.appendChild(deleteBtn); tdActions.appendChild(actions); tr.appendChild(tdActions); editBranchBtn.addEventListener("click", async () =>{ hideAlert(); if (row.kind === "tree_card") { openTreeCardEditor(row); return; } const branches = ["زيدان", "مزيد", "زايد", "لاحم", "ملحم"]; const currentBranch = normalizeTreeCardText(row.branch_key || ""); const entered = window.prompt( "اكتب الفرع الصحيح:\n" + branches.join(" / "), currentBranch ); if (entered == null) return; const branchKey = normalizeTreeCardText(entered); if (!branches.includes(branchKey)) { showAlert("error", "الفرع غير صحيح. اختر: " + branches.join("، ")); return; } if (branchKey === currentBranch) { showAlert("error", "لم يتغير الفرع."); return; } const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } const id = coerceRpcId(row.id != null ? row.id : row.request_id); if (!id) { showAlert("error", "بيانات الطلب ناقصة."); return; } const message = updateBranchInRequestMessage(row.message, branchKey, row.kind); let treeRows = []; if (row.status === "approved" && row.kind === "tree_card") { const built = buildTreeCardRows(row, currentBranch); if (!built.ok) { showAlert("error", built.message || "تعذر قراءة بيانات بطاقة الشجرة."); return; } treeRows = built.rows.map((item) =>({ parent_name: item.parent_name, child_name: item.child_name })); } editBranchBtn.disabled = true; const { data, error } = await sb.rpc("admin_update_request_branch_v1", { p_token: token, p_id: String(id), p_old_branch_key: currentBranch || null, p_branch_key: branchKey, p_name: row.name || null, p_phone: row.phone || null, p_email: row.email || null, p_message: message || null, p_old_tree_rows: treeRows, p_new_tree_rows: treeRows }); if (error) { const errorText = String(error.message || ""); const missingRpc = errorText.toLowerCase().includes("could not find the function") || errorText.toLowerCase().includes("does not exist") || String(error.code || "").toLowerCase() === "pgrst202"; showAlert( "error", missingRpc ? "تعذر تعديل الفرع حالياً، حاول لاحقاً أو تواصل مع الإدارة." : "تعذر تعديل الفرع حالياً، حاول لاحقاً أو تواصل مع الإدارة." ); editBranchBtn.disabled = false; return; } if (data !== true) { showAlert("error", "لم يتم تعديل الطلب. يمكن تعديل الطلبات المنتظرة أو المقبولة فقط."); editBranchBtn.disabled = false; return; } const movedText = row.status === "approved" && row.kind === "tree_card" ? " ونقل بيانات البطاقة إلى الفرع الصحيح" : ""; showAlert("success", "تم تعديل الفرع من «" + (currentBranch || "غير محدد") + "» إلى «" + branchKey + "»" + movedText + "."); await loadRequests(); }); approveBtn.addEventListener("click", async () =>{ hideAlert(); const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } const id = coerceRpcId(row.id != null ? row.id : row.request_id); if (!id) { showAlert("error", "بيانات الطلب ناقصة."); return; } if (row.kind === "tree_card") { const imported = await importTreeCardToTree(sb, token, row); if (!imported.ok) { showAlert("error", imported.message || "تعذر إضافة البطاقة للشجرة."); return; } } else if (row.kind === "event_card") { const published = await publishEventCardRequest(sb, token, row); if (!published.ok) { showAlert("error", published.message || "تعذر نشر المناسبة."); window.alert(published.message || "تعذر نشر المناسبة."); return; } } const { data, error } = await sb.rpc("admin_set_request_status_v2", { p_token: token, p_id: id, p_status: "approved" }); if (error) { showAlert("error", "تعذر اعتماد الطلب حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } if (data === false) { showAlert("error", "تعذر اعتماد الطلب. انتهت الجلسة أو لا توجد صلاحية."); return; } if (isLikelyEmail(row.email)) { const subject = "تم قبول الطلب (" + String(row.request_id || "").trim() + ")"; const body = [ "تم قبول طلبك.", row.request_id ? "رقم الطلب: " + row.request_id : "", row.kind ? "نوع الطلب: " + kindLabel(row.kind) : "", row.branch_key ? "الفرع: " + row.branch_key : "", row.name ? "الاسم: " + row.name : "", "", "يمكنك الآن المتابعة حسب تعليمات الإدارة." ] .filter(Boolean) .join("\n"); await copyText(body); maybeOpenEmailDraft(row.email, subject, body); } if (row.kind === "tree_card") { showAlert("success", `تم قبول الطلب وإضافة البيانات للشجرة: ${row.request_id}`); } else if (row.kind === "event_card") { showAlert("success", `تم قبول الطلب ونشر المناسبة في الويب والتطبيق: ${row.request_id}`); } else { showAlert("success", `تم قبول الطلب: ${row.request_id}`); } await loadRequests(); }); publishEventBtn.addEventListener("click", async () =>{ hideAlert(); const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } publishEventBtn.disabled = true; const published = await publishEventCardRequest(sb, token, row); publishEventBtn.disabled = false; if (!published.ok) { showAlert("error", published.message || "تعذر نشر المناسبة."); window.alert(published.message || "تعذر نشر المناسبة."); return; } showAlert("success", `تم نشر المناسبة في الويب والتطبيق: ${row.request_id}`); window.alert("تم نشر المناسبة في الويب والتطبيق."); await loadRequests(); }); deleteBtn.addEventListener("click", async () =>{ hideAlert(); const id = coerceRpcId(row.id != null ? row.id : row.request_id); if (!id) { showAlert("error", "بيانات الطلب ناقصة."); return; } const confirmed = window.confirm("تأكيد حذف الطلب نهائياً ؟ لا يمكن التراجع."); if (!confirmed) return; const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } deleteBtn.disabled = true; const { data, error } = await sb.rpc("admin_delete_request_v1", { p_token: token, p_id: String(id) }); deleteBtn.disabled = false; if (error) { showAlert("error", "تعذر حذف الطلب، حاول لاحقاً أو تواصل مع الإدارة."); return; } if (data !== true) { showAlert("error", "لم يتم حذف الطلب. انتهت الجلسة أو لا توجد صلاحية."); return; } showAlert("success", "تم حذف الطلب : " + String(row.request_id || id)); await loadRequests(); loadRequestsStats().catch(() =>{}); }); rejectBtn.addEventListener("click", async () =>{ hideAlert(); const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const token = getAdminToken(); if (!token) { showAlert("error", "يلزم تسجيل الدخول أولاً."); return; } const id = coerceRpcId(row.id != null ? row.id : row.request_id); if (!id) { showAlert("error", "بيانات الطلب ناقصة."); return; } const { data, error } = await sb.rpc("admin_set_request_status_v2", { p_token: token, p_id: id, p_status: "rejected" }); if (error) { showAlert("error", "تعذر رفض الطلب حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } if (data === false) { showAlert("error", "تعذر رفض الطلب. انتهت الجلسة أو لا توجد صلاحية."); return; } if (isLikelyEmail(row.email)) { const subject = "تم رفض الطلب (" + String(row.request_id || "").trim() + ")"; const body = [ "تم رفض طلبك.", row.request_id ? "رقم الطلب: " + row.request_id : "", row.kind ? "نوع الطلب: " + kindLabel(row.kind) : "", row.branch_key ? "الفرع: " + row.branch_key : "", row.name ? "الاسم: " + row.name : "", "", "للاستفسار: تواصل مع إدارة الموقع." ] .filter(Boolean) .join("\n"); await copyText(body); maybeOpenEmailDraft(row.email, subject, body); } showAlert("success", `تم رفض الطلب: ${row.request_id}`); await loadRequests(); }); requestsBody.appendChild(tr); } function renderRequestsStatsLoading() { const el = document.getElementById("requests-stats"); if (!el) return; el.textContent = "جاري تحميل الإحصاء..."; } function renderRequestsStatsError(text) { const el = document.getElementById("requests-stats"); if (!el) return; el.textContent = String(text || "تعذر عرض الإحصاء."); } function renderRequestsStats(list, limit) { const el = document.getElementById("requests-stats"); if (!el) return; const rows = Array.isArray(list) ? list : []; const total = rows.length; const byKind = new Map(); const byStatus = new Map(); rows.forEach((row) =>{ const kind = String(row.kind || "unknown"); const status = String(row.status || "unknown"); byKind.set(kind, (byKind.get(kind) || 0) + 1); byStatus.set(status, (byStatus.get(status) || 0) + 1); }); function card(label, value, extraClass) { return '<div class="requests-stat-card ' + (extraClass || '') + '">' + '<strong>' + String(value || 0) + '</strong>' + '<span>' + label + '</span>' + '</div>'; } const kindOrder = [ "tree_card", "tree_audit", "event_card", "events_audit" ]; let html = '<div class="requests-stats-cards">'; html += card("إجمالي الطلبات", total, "stat-total"); html += card("انتظار", byStatus.get("pending") || 0, "stat-pending"); html += card("قبول", byStatus.get("approved") || 0, "stat-approved"); html += card("رفض", byStatus.get("rejected") || 0, "stat-rejected"); const hiddenKinds = new Set([ "tree_delegate", "events_delegate", "test_request" ]); const customLabels = { tree_audit: "تعديل الشجرة", events_audit: "تعديل المناسبات" }; kindOrder.forEach((kind) =>{ if (hiddenKinds.has(kind)) return; const count = byKind.get(kind) || 0; if (count<= 0) return; const label = customLabels[kind] || kindLabel(kind); html += card(label, count, "stat-kind"); }); html += '</div>'; if (limit && total >= limit) { html += '<div class="hint" style="margin-top:8px;">تم عرض أول ' + String(limit) + ' طلب فقط.</div>'; } el.innerHTML = html; } async function loadRequestsStats() { const sb = getClient(); if (!sb) { renderRequestsStatsError("الخدمة غير جاهزة حالياً."); return; } const token = getAdminToken(); if (!token) { renderRequestsStatsError("سجل الدخول للإدارة لعرض الإحصاء."); return; } const limit = 5000; renderRequestsStatsLoading(); const { data, error } = await sb.rpc("admin_list_requests", { p_token: token, p_status: null, p_kind: null, p_limit: limit }); if (error) { renderRequestsStatsError("تعذر تحميل الإحصاء، حاول لاحقاً أو تواصل مع الإدارة."); return; } const list = Array.isArray(data) ? data : []; renderRequestsStats(list, limit); } function safeParseJsonText(v) { try { if (v == null) return null; const s = String(v || "").trim(); if (!s) return null; if (!(s.startsWith("{") || s.startsWith("["))) return null; return JSON.parse(s); } catch (e) { return null; } } function setDelegateAuditStatus(text) { if (!delegateAuditStatus) return; delegateAuditStatus.textContent = text || ""; } function clearDelegateAuditTable(text) { if (!delegateAuditBody) return; delegateAuditBody.innerHTML = ""; if (!text) return; const tr = document.createElement("tr"); const td = document.createElement("td"); td.colSpan = 5; td.className = "hint"; td.textContent = text; tr.appendChild(td); delegateAuditBody.appendChild(tr); } function formatAuditOp(op) { const o = String(op || "").trim(); if (o === "insert") return "إضافة"; if (o === "update") return "تعديل"; if (o === "delete") return "حذف"; if (o === "upsert_update") return "تعديل (موجود)"; return o || "عملية"; } function formatAuditTarget(env, row) { if (env && (env.kind === "events_audit" || env.kind === "event_audit")) { const parts = []; const type = env.type != null ? String(env.type || "").trim() : ""; const person = env.person != null ? String(env.person || "").trim() : ""; const date = env.event_date != null ? String(env.event_date || "").trim() : ""; if (type) parts.push("النوع: " + type); if (person) parts.push("الاسم: " + person); if (date) parts.push("التاريخ: " + date); return parts.join(" — "); } const parent = env && env.parent_name != null ? String(env.parent_name) : String(row.parent_name || row.parent || ""); const child = env && env.child_name != null ? String(env.child_name) : String(row.child_name || row.name || ""); const parts = []; if (parent) parts.push("الأب: " + parent); if (child) parts.push("الابن: " + child); return parts.join(" — "); } function renderDelegatesList(entries, selectedKey, onPick) { if (!delegatesListEl) return; delegatesListEl.innerHTML = ""; if (!entries.length) { const empty = document.createElement("div"); empty.className = "hint"; empty.textContent = "لا توجد بيانات مناديب حالياً."; delegatesListEl.appendChild(empty); return; } entries.forEach((e) =>{ const btn = document.createElement("button"); btn.type = "button"; btn.className = "btn btn-outline btn-sm"; btn.textContent = e.label; if (e.key === selectedKey) { btn.style.borderColor = "rgba(4,120,87,0.55)"; btn.style.color = "#065f46"; } btn.addEventListener("click", () =>onPick(e.key)); delegatesListEl.appendChild(btn); }); } function renderDelegateAuditSelect(entries, selectedKey, onPick) { if (!delegateAuditSelect) return; delegateAuditSelect.innerHTML = ""; const opt0 = document.createElement("option"); opt0.value = ""; opt0.textContent = "اختر مندوباً"; delegateAuditSelect.appendChild(opt0); entries.forEach((e) =>{ const opt = document.createElement("option"); opt.value = e.key; opt.textContent = e.label; delegateAuditSelect.appendChild(opt); }); delegateAuditSelect.value = selectedKey || ""; delegateAuditSelect.onchange = () =>onPick(String(delegateAuditSelect.value || "")); } function renderDelegateAuditRows(rows) { if (!delegateAuditBody) return; delegateAuditBody.innerHTML = ""; if (!rows.length) { clearDelegateAuditTable("لا توجد تعديلات لهذا المندوب بعد."); return; } rows.forEach((row) =>{ const env = safeParseJsonText(row.message); const tr = document.createElement("tr"); const tdTime = document.createElement("td"); try { tdTime.textContent = row.created_at ? formatDateTimeArSaVerbose(row.created_at) : ""; } catch (e) { tdTime.textContent = String(row.created_at || ""); } tr.appendChild(tdTime); const tdOp = document.createElement("td"); tdOp.textContent = formatAuditOp(env && env.op ? env.op : ""); tr.appendChild(tdOp); const tdBranch = document.createElement("td"); tdBranch.textContent = String(row.branch_key || (env && env.branch_key ? env.branch_key : "") || ""); tr.appendChild(tdBranch); const tdTarget = document.createElement("td"); tdTarget.textContent = formatAuditTarget(env, row); tr.appendChild(tdTarget); const tdDetails = document.createElement("td"); if (env) { const det = document.createElement("details"); det.className = "msg"; const sum = document.createElement("summary"); sum.textContent = "عرض"; const body = document.createElement("div"); try { body.textContent = JSON.stringify(env, null, 2); } catch (e) { body.textContent = String(row.message || ""); } det.appendChild(sum); det.appendChild(body); tdDetails.appendChild(det); } else if (row.message) { const det = document.createElement("details"); det.className = "msg"; const sum = document.createElement("summary"); sum.textContent = "عرض"; const body = document.createElement("div"); body.textContent = String(row.message || ""); det.appendChild(sum); det.appendChild(body); tdDetails.appendChild(det); } else { tdDetails.textContent = ""; } tr.appendChild(tdDetails); delegateAuditBody.appendChild(tr); }); } let delegateAuditState = { selectedKey: "", delegates: [], auditRows: [], accessByKey: new Map() }; function buildDelegateKey(branchKey, phone, email) { const b = String(branchKey || "").trim(); const p = String(phone || "").trim(); const e = String(email || "").trim(); return [b, p, e].join("|"); } function labelForDelegate(row, countsByKey) { const b = String(row.branch_key || "").trim(); const p = String(row.phone || "").trim(); const e = String(row.email || "").trim(); const n = String(row.name || "").trim(); const key = buildDelegateKey(b, p, e); const c = countsByKey.get(key) || 0; const who = n ? n : (p || e || "مندوب"); const parts = []; parts.push(who); if (b) parts.push("— " + b); parts.push("— " + String(c)); return parts.join(" "); } function applyDelegateAuditSelection(key) { const k = String(key || "").trim(); delegateAuditState.selectedKey = k; if (!k) { clearDelegateAuditTable("اختر مندوباً لعرض السجل."); setDelegateAuditStatus(""); if (delegatePermsStatus) delegatePermsStatus.textContent = ""; return; } const filtered = delegateAuditState.auditRows .filter((r) =>buildDelegateKey(r.branch_key, r.phone, r.email) === k) .sort((a, b) =>String(b.created_at || "").localeCompare(String(a.created_at || ""))); renderDelegateAuditRows(filtered.slice(0, 300)); const total = filtered.length; setDelegateAuditStatus(total ? "عدد السجلات: " + String(total) : "لا توجد سجلات."); const caps = delegateAuditState.accessByKey.get(k) || null; const treeStatus = caps && caps.tree ? String(caps.tree.status || "") : ""; const eventsStatus = caps && caps.events ? String(caps.events.status || "") : ""; const parts = []; parts.push("الشجرة: " + (treeStatus || "غير موجود")); parts.push("المناسبات: " + (eventsStatus || "غير موجود")); if (delegatePermsStatus) delegatePermsStatus.textContent = parts.join(" — "); } async function loadDelegateAudit() { const sb = getClient(); if (!sb) { clearDelegateAuditTable("الخدمة غير جاهزة حالياً."); return; } const token = getAdminToken(); if (!token) { clearDelegateAuditTable("سجل الدخول للإدارة لعرض سجل التعديلات."); return; } clearDelegateAuditTable("جاري تحميل سجل التعديلات..."); setDelegateAuditStatus(""); if (delegatePermsStatus) delegatePermsStatus.textContent = ""; const treeDelegatesRes = await sb.rpc("admin_list_requests", { p_token: token, p_status: null, p_kind: "tree_delegate", p_limit: 2000 }); if (treeDelegatesRes.error) { clearDelegateAuditTable("تعذر تحميل بيانات المناديب، حاول لاحقاً أو تواصل مع الإدارة."); return; } const treeDelegates = Array.isArray(treeDelegatesRes.data) ? treeDelegatesRes.data : []; const eventsDelegatesRes = await sb.rpc("admin_list_requests", { p_token: token, p_status: null, p_kind: "events_delegate", p_limit: 2000 }); if (eventsDelegatesRes.error) { clearDelegateAuditTable("تعذر تحميل بيانات المناديب، حاول لاحقاً أو تواصل مع الإدارة."); return; } const eventsDelegates = Array.isArray(eventsDelegatesRes.data) ? eventsDelegatesRes.data : []; const auditRes = await sb.rpc("admin_list_requests", { p_token: token, p_status: "approved", p_kind: "tree_audit", p_limit: 2000 }); if (auditRes.error) { clearDelegateAuditTable("تعذر تحميل سجل التعديلات، حاول لاحقاً أو تواصل مع الإدارة."); return; } const treeAudits = Array.isArray(auditRes.data) ? auditRes.data : []; const eventsAuditRes = await sb.rpc("admin_list_requests", { p_token: token, p_status: "approved", p_kind: "events_audit", p_limit: 2000 }); if (eventsAuditRes.error) { clearDelegateAuditTable("تعذر تحميل سجل المناسبات، حاول لاحقاً أو تواصل مع الإدارة."); return; } const eventsAudits = Array.isArray(eventsAuditRes.data) ? eventsAuditRes.data : []; const audits = treeAudits.concat(eventsAudits); const countsByKey = new Map(); audits.forEach((r) =>{ const key = buildDelegateKey(r.branch_key, r.phone, r.email); countsByKey.set(key, (countsByKey.get(key) || 0) + 1); }); const accessByKey = new Map(); const pickLatest = (a, b) =>{ if (!a) return b || null; if (!b) return a || null; return String(b.created_at || "").localeCompare(String(a.created_at || "")) >0 ? b : a; }; treeDelegates.forEach((r) =>{ const key = buildDelegateKey(r.branch_key, r.phone, r.email); if (!key || key === "||") return; const cur = accessByKey.get(key) || { tree: null, events: null }; cur.tree = pickLatest(cur.tree, r); accessByKey.set(key, cur); }); eventsDelegates.forEach((r) =>{ const key = buildDelegateKey(r.branch_key, r.phone, r.email); if (!key || key === "||") return; const cur = accessByKey.get(key) || { tree: null, events: null }; cur.events = pickLatest(cur.events, r); accessByKey.set(key, cur); }); const delegateRows = Array.from(accessByKey.entries()) .map(([key, caps]) =>{ const row = caps.tree || caps.events; const count = countsByKey.get(key) || 0; const approvedCount = (caps.tree && caps.tree.status === "approved" ? 1 : 0) + (caps.events && caps.events.status === "approved" ? 1 : 0); return { key, row, count, approvedCount }; }) .sort((a, b) =>(b.approvedCount || 0) - (a.approvedCount || 0) || (b.count || 0) - (a.count || 0)); const listEntries = delegateRows.map((d) =>({ key: d.key, label: labelForDelegate(d.row, countsByKey) })); delegateAuditState.delegates = listEntries; delegateAuditState.auditRows = audits; delegateAuditState.accessByKey = new Map(Array.from(accessByKey.entries()).map(([k, v]) =>({ key: k, tree: v.tree, events: v.events })).map((x) =>[x.key, { tree: x.tree, events: x.events }])); const selected = delegateAuditState.selectedKey && listEntries.some((e) =>e.key === delegateAuditState.selectedKey) ? delegateAuditState.selectedKey : (listEntries[0] ? listEntries[0].key : ""); const pickDelegateAuditKey = (k) =>{ const next = String(k || "").trim(); delegateAuditState.selectedKey = next; if (delegateAuditSelect) delegateAuditSelect.value = next; renderDelegatesList(listEntries, next, pickDelegateAuditKey); renderDelegateAuditSelect(listEntries, next, pickDelegateAuditKey); applyDelegateAuditSelection(next); }; pickDelegateAuditKey(selected); } function setDelegatePermsUiBusy(busy) { const isBusy = !!busy; if (delegatePermsTreeBtn) delegatePermsTreeBtn.disabled = isBusy; if (delegatePermsEventsBtn) delegatePermsEventsBtn.disabled = isBusy; if (delegatePermsBothBtn) delegatePermsBothBtn.disabled = isBusy; if (delegatePermsDisableBtn) delegatePermsDisableBtn.disabled = isBusy; } function makeAdminDelegateRequestId(prefix) { const p = String(prefix || "ADM").trim().toUpperCase() || "ADM"; const chunk = () =>Math.random().toString(16).slice(2, 6).toUpperCase().padEnd(4, "0").slice(0, 4); return p + "-" + chunk() + "-" + chunk(); } async function createDelegatePermissionRow(sb, kind, sourceRow) { const src = sourceRow || {}; const token = getAdminToken(); if (!token) return { ok: false, error: { message: "not_authed" } }; const { data, error } = await sb.rpc("admin_create_delegate_request_v1", { p_token: token, p_kind: kind, p_branch_key: src.branch_key || null, p_name: src.name || null, p_phone: src.phone || null, p_email: src.email || null, p_secret_hash: src.secret_hash || null }); if (error) return { ok: false, error }; const requestId = typeof data === "string" ? data : (data && typeof data === "object" ? String(data.request_id || "") : ""); return { ok: true, requestId }; } async function applyDelegatePermissions(mode) { const sb = getClient(); if (!sb) return; const token = getAdminToken(); if (!token) return; const key = String(delegateAuditState.selectedKey || "").trim(); if (!key) { showAlert("error", "اختر مندوباً أولاً."); return; } const caps = delegateAuditState.accessByKey.get(key) || { tree: null, events: null }; const sourceRow = caps.tree || caps.events; const treeWant = mode === "tree" || mode === "both"; const eventsWant = mode === "events" || mode === "both"; const treeStatus = treeWant ? "approved" : "rejected"; const eventsStatus = eventsWant ? "approved" : "rejected"; hideAlert(); setDelegatePermsUiBusy(true); if (delegatePermsStatus) delegatePermsStatus.textContent = "جاري تطبيق الصلاحيات..."; try { if (caps.tree && caps.tree.id != null) { const id = coerceRpcId(caps.tree.id); if (!id) { showAlert("error", "تعذر تحديث صلاحية الشجرة: بيانات غير مكتملة."); return; } const { error } = await sb.rpc("admin_set_request_status_v2", { p_token: token, p_id: id, p_status: treeStatus }); if (error) { showAlert("error", "تعذر تحديث صلاحية الشجرة حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } } else if (treeWant) { if (!sourceRow) { showAlert("error", "لا يمكن إنشاء صلاحية الشجرة لأن بيانات المندوب غير مكتملة."); return; } const created = await createDelegatePermissionRow(sb, "tree_delegate", sourceRow); if (!created.ok) { showAlert("error", "تعذر إنشاء صلاحية الشجرة حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } } if (caps.events && caps.events.id != null) { const id = coerceRpcId(caps.events.id); if (!id) { showAlert("error", "تعذر تحديث صلاحية المناسبات: بيانات غير مكتملة."); return; } const { error } = await sb.rpc("admin_set_request_status_v2", { p_token: token, p_id: id, p_status: eventsStatus }); if (error) { showAlert("error", "تعذر تحديث صلاحية المناسبات حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } } else if (eventsWant) { if (!sourceRow) { showAlert("error", "لا يمكن إنشاء صلاحية المناسبات لأن بيانات المندوب غير مكتملة."); return; } const created = await createDelegatePermissionRow(sb, "events_delegate", sourceRow); if (!created.ok) { showAlert("error", "تعذر إنشاء صلاحية المناسبات حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } } showAlert("success", "تم تحديث صلاحيات المندوب."); await loadDelegateAudit(); } finally { setDelegatePermsUiBusy(false); } } async function deleteDelegatePermanently() { const sb = getClient(); if (!sb) return; const token = getAdminToken(); if (!token) return; const key = String(delegateAuditState.selectedKey || "").trim(); if (!key) { showAlert("error", "اختر مندوباً أولاً."); return; } const parts = key.split("|"); const branchKey = String(parts[0] || "").trim(); const phone = String(parts[1] || "").trim(); const email = String(parts[2] || "").trim(); const confirmed = window.confirm("تأكيد حذف المندوب نهائياً؟ سيتم حذف صلاحياته وسجل التعديلات."); if (!confirmed) return; hideAlert(); setDelegatePermsUiBusy(true); if (delegatePermsStatus) delegatePermsStatus.textContent = "جاري حذف المندوب..."; try { const { data, error } = await sb.rpc("admin_delete_delegate_v1", { p_token: token, p_branch_key: branchKey || null, p_phone: phone || null, p_email: email || null }); if (error) { showAlert("error", "تعذر حذف المندوب حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } showAlert("success", "تم حذف المندوب. عدد السجلات: " + String(Number(data || 0))); await loadDelegateAudit(); } finally { setDelegatePermsUiBusy(false); } } function normalizeRequestSearchText(value) { return String(value == null ? "" : value) .toLowerCase() .replace(/[٠-٩]/g, (d) =>"٠١٢٣٤٥٦٧٨٩".indexOf(d)) .replace(/[۰-۹]/g, (d) =>"۰۱۲۳۴۵۶۷۸۹".indexOf(d)) .trim(); } function requestRowMatchesSearch(row, query) { const q = normalizeRequestSearchText(query); if (!q) return true; const text = [ row.request_id, row.id, kindLabel(row.kind), row.kind, row.branch_key, row.name, row.phone, row.email, statusLabel(row.status), row.created_at ].map(normalizeRequestSearchText).join(" | "); return text.includes(q); } function getRequestsPageSize() { const n = Number(requestsPageSizeSelect && requestsPageSizeSelect.value ? requestsPageSizeSelect.value : 50); return Number.isFinite(n) && n >0 ? n : 50; } function renderRequestsPage() { if (!requestsBody) return; requestsBody.innerHTML = ""; const query = requestSearchInput ? requestSearchInput.value : ""; const filtered = requestsAllRows.filter((row) =>requestRowMatchesSearch(row, query)); if (!filtered.length) { renderEmpty("لا توجد طلبات مطابقة للبحث والفلاتر الحالية."); } else { filtered.forEach((row) =>renderRequestRow(row)); } if (requestsPageInfo) { requestsPageInfo.textContent = "عدد النتائج: " + String(filtered.length); } if (requestsPrevPageBtn) requestsPrevPageBtn.disabled = true; if (requestsNextPageBtn) requestsNextPageBtn.disabled = true; } async function loadRequests() { if (!requestsBody) return; requestsBody.innerHTML = ""; const sb = getClient(); if (!sb) { renderEmpty("الخدمة غير جاهزة حالياً."); return; } const token = getAdminToken(); if (!token) { renderEmpty("سجل الدخول للإدارة لعرض الطلبات."); return; } const statusValue = String(filterStatus?.value || "pending"); const kindValue = String(filterKind?.value || "all"); const { data, error } = await sb.rpc("admin_list_requests", { p_token: token, p_status: statusValue === "all" ? null : statusValue, p_kind: kindValue === "all" ? null : kindValue, p_limit: 5000 }); if (error) { renderEmpty("تعذر جلب الطلبات حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } requestsAllRows = Array.isArray(data) ? data : []; requestsCurrentPage = 1; renderRequestsPage(); } if (adminLoginBtn) { adminLoginBtn.addEventListener("click", async () =>{ hideAlert(); const sb = getClient(); if (!sb) { showAlert("error", "تعذر الاتصال."); return; } const username = String(adminUsername?.value || "").trim(); const password = String(adminPassword?.value || "").trim(); if (!username || !password) { showAlert("error", "يرجى إدخال اسم المستخدم وكلمة المرور."); return; } const { data, error } = await sb.rpc("admin_login", { p_username: username, p_password: password }); if (error) { showAlert("error", "تعذر تسجيل الدخول حالياً، حاول لاحقاً أو تواصل مع الإدارة."); return; } const token = tokenFromRpcResult(data); if (!token) { showAlert("error", "اسم المستخدم أو كلمة المرور غير صحيحة."); return; } adminToken = token; try { sessionStorage.setItem(ADMIN_TOKEN_SESSION_KEY, token); } catch (e) {} try { localStorage.setItem(ADMIN_TOKEN_KEY, token); } catch (e) {} showAlert("success", ""); await refreshAuthStatus(); loadTickerSpeedSetting().catch(() =>{}); await loadRequests(); loadSourceTreeRows().catch(() =>{}); loadRequestsStats().catch(() =>{}); if (window.AlzidanAdminViews && typeof window.AlzidanAdminViews.loadViewsStats === "function") window.AlzidanAdminViews.loadViewsStats().catch(() =>{}); pollPendingRequestsForNotifications().catch(() =>{}); startPendingPolling(); }); } if (adminLogoutBtn) { adminLogoutBtn.addEventListener("click", async () =>{ hideAlert(); adminToken = ""; try { sessionStorage.removeItem(ADMIN_TOKEN_SESSION_KEY); } catch (e) {} try { localStorage.removeItem(ADMIN_TOKEN_KEY); } catch (e) {} await refreshAuthStatus(); await loadRequests(); stopPendingPolling(); }); } if (adminRefreshBtn) { adminRefreshBtn.addEventListener("click", async () =>{ hideAlert(); await refreshAuthStatus(); await loadRequests(); loadSourceTreeRows().catch(() =>{}); pollPendingRequestsForNotifications().catch(() =>{}); }); } if (adminEnableNotifsBtn) { updateNotifsButtonText(); adminEnableNotifsBtn.addEventListener("click", async () =>{ const ok = await ensureBrowserNotificationsEnabled(); updateNotifsButtonText(); if (!ok) return; pollPendingRequestsForNotifications().catch(() =>{}); }); } if (adminForgotBtn) { adminForgotBtn.addEventListener("click", () =>{ showAlert("error", "إذا نسيت كلمة المرور: تواصل مع مدير النظام لإعادة تعيينها."); }); } if (filterKind) filterKind.addEventListener("change", () =>{ requestsCurrentPage = 1; loadRequests(); }); if (filterStatus) filterStatus.addEventListener("change", () =>{ requestsCurrentPage = 1; loadRequests(); }); if (requestSearchInput) requestSearchInput.addEventListener("input", () =>{ requestsCurrentPage = 1; renderRequestsPage(); }); if (requestsPageSizeSelect) requestsPageSizeSelect.addEventListener("change", () =>{ requestsCurrentPage = 1; renderRequestsPage(); }); if (requestsPrevPageBtn) requestsPrevPageBtn.addEventListener("click", () =>{ requestsCurrentPage -= 1; renderRequestsPage(); }); if (requestsNextPageBtn) requestsNextPageBtn.addEventListener("click", () =>{ requestsCurrentPage += 1; renderRequestsPage(); }); if (sourceTreeBranch) sourceTreeBranch.addEventListener("change", () =>loadSourceTreeRows().catch(() =>{})); if (sourceTreeLoad) sourceTreeLoad.addEventListener("click", () =>loadSourceTreeRows().catch(() =>{})); if (sourceTreeNew) sourceTreeNew.addEventListener("click", () =>resetSourceTreeForm()); if (sourceTreeAddExtraChild) sourceTreeAddExtraChild.addEventListener("click", () =>addSourceTreeExtraChildField("")); if (sourceTreeForm) sourceTreeForm.addEventListener("submit", saveSourceTreeRow); if (sourceTreeDelete) sourceTreeDelete.addEventListener("click", () =>deleteSourceTreeSubtree().catch(() =>{})); if (eventsSourceLoad) eventsSourceLoad.addEventListener("click", () =>loadEventsSourceRows().catch(() =>{})); if (bannerGeneralForm) bannerGeneralForm.addEventListener("submit", publishBannerGeneralNews); if (bannerGeneralClear) bannerGeneralClear.addEventListener("click", clearBannerGeneralForm); if (bannerMessagesLoad) bannerMessagesLoad.addEventListener("click", () =>loadBannerMessagesRows().catch(() =>{})); if (bannerMessagesNew) bannerMessagesNew.addEventListener("click", resetBannerMessagesForm); if (bannerMessagesForm) bannerMessagesForm.addEventListener("submit", saveBannerMessageRow); if (bannerMessagesDelete) bannerMessagesDelete.addEventListener("click", () =>deleteBannerMessageRow().catch(() =>{})); if (adminTickerSpeedSave) adminTickerSpeedSave.addEventListener("click", () =>saveTickerSpeedSetting().catch(() =>{})); if (eventsSourceForm) eventsSourceForm.addEventListener("submit", saveEventsSourceRow); if (eventsSourceDelete) eventsSourceDelete.addEventListener("click", () =>deleteEventsSourceRow().catch(() =>{})); async function buildWaFileText() { if (!waFileTextEl) return ""; const file = waFileEl && waFileEl.files && waFileEl.files[0] ? waFileEl.files[0] : null; if (!file) { setWaFileStatus("اختر ملفاً أولاً."); return ""; } setWaFileStatus("جاري تجهيز النص..."); const raw = await readFileAsText(file); if (!raw) { waFileTextEl.value = ""; setWaFileStatus("تعذر قراءة الملف أو الملف فارغ."); return ""; } const msg = buildWhatsAppTextFromFile(file, raw); waFileTextEl.value = msg; setWaFileStatus("تم تجهيز النص."); return msg; } (async function init() { try { adminToken = String(sessionStorage.getItem(ADMIN_TOKEN_SESSION_KEY) || localStorage.getItem(ADMIN_TOKEN_KEY) || "").trim(); } catch (e) { adminToken = ""; } try { localStorage.removeItem(ADMIN_TOKEN_KEY); } catch (e) {} initEmailSettings(); if (adminUsername && !String(adminUsername.value || "").trim()) { adminUsername.value = "alshryhy"; } await refreshAuthStatus(); await loadRequests(); updateNotifsButtonText(); if (adminToken) { loadRequestsStats().catch(() =>{}); if (window.AlzidanAdminViews && typeof window.AlzidanAdminViews.loadViewsStats === "function") window.AlzidanAdminViews.loadViewsStats().catch(() =>{}); loadSourceTreeRows().catch(() =>{}); if (delegateAuditDetails && delegateAuditDetails.open) { loadDelegateAudit().catch(() =>{}); } pollPendingRequestsForNotifications().catch(() =>{}); if (adminEmailNotifAudit && adminEmailNotifAudit.checked) { seedAuditEmailKey().catch(() =>{}); } startPendingPolling(); } })(); if (refreshRequestsStatsBtn) refreshRequestsStatsBtn.addEventListener("click", () =>loadRequestsStats().catch(() =>{}));  if (refreshDelegateAuditBtn) refreshDelegateAuditBtn.addEventListener("click", () =>loadDelegateAudit().catch(() =>{})); if (delegatePermsTreeBtn) delegatePermsTreeBtn.addEventListener("click", () =>applyDelegatePermissions("tree").catch(() =>{})); if (delegatePermsEventsBtn) delegatePermsEventsBtn.addEventListener("click", () =>applyDelegatePermissions("events").catch(() =>{})); if (delegatePermsBothBtn) delegatePermsBothBtn.addEventListener("click", () =>applyDelegatePermissions("both").catch(() =>{})); if (delegatePermsDisableBtn) delegatePermsDisableBtn.addEventListener("click", () =>applyDelegatePermissions("disable").catch(() =>{})); if (delegateDeleteBtn) delegateDeleteBtn.addEventListener("click", () =>deleteDelegatePermanently().catch(() =>{})); if (delegateAuditDetails) { delegateAuditDetails.addEventListener("toggle", () =>{ if (delegateAuditDetails.open && getAdminToken()) { loadDelegateAudit().catch(() =>{}); } }); } if (treeImportDownloadBtn) { treeImportDownloadBtn.addEventListener("click", () =>{ hideAlert(); downloadTextFile("alzidan-tree-template.csv", treeCsvTemplateText(), "text/csv;charset=utf-8"); }); } if (treeImportTemplateEl) { try { treeImportTemplateEl.value = String(treeCardTemplateText() || ""); } catch (e) {} } if (treeImportCopyBtn) { treeImportCopyBtn.addEventListener("click", async () =>{ hideAlert(); const text = String(treeCardTemplateText() || ""); const ok = await copyText(text); setTreeImportStatus(ok ? "تم نسخ نموذج البطاقة." : "تعذر نسخ النموذج."); }); } if (treeImportWhatsappBtn) { treeImportWhatsappBtn.addEventListener("click", () =>{ hideAlert(); const text = String(treeCardTemplateText() || ""); const opened = openWhatsAppWithText(text); setTreeImportStatus(opened ? "تم فتح واتساب." : "تعذر فتح واتساب."); }); } if (treeImportFileEl) { treeImportFileEl.addEventListener("change", () =>{ setTreeImportStatus(""); }); } if (treeImportRunBtn) { treeImportRunBtn.addEventListener("click", () =>{ hideAlert(); const file = treeImportFileEl && treeImportFileEl.files && treeImportFileEl.files[0] ? treeImportFileEl.files[0] : null; runTreeImportFromFile(file).catch(() =>{ showAlert("error", "تعذر الاستيراد."); setTreeImportStatus(""); }); }); } if (waFileEl) { waFileEl.addEventListener("change", () =>{ setWaFileStatus(""); if (waFileTextEl) waFileTextEl.value = ""; }); } if (waFileBuildBtn) { waFileBuildBtn.addEventListener("click", () =>{ hideAlert(); buildWaFileText().catch(() =>setWaFileStatus("تعذر تجهيز النص.")); }); } if (waFileCopyBtn) { waFileCopyBtn.addEventListener("click", async () =>{ hideAlert(); let text = waFileTextEl ? String(waFileTextEl.value || "") : ""; if (!text) text = await buildWaFileText(); if (!text) return; const ok = await copyText(text); setWaFileStatus(ok ? "تم نسخ النص." : "تعذر نسخ النص."); }); } if (waFileOpenBtn) { waFileOpenBtn.addEventListener("click", async () =>{ hideAlert(); let text = waFileTextEl ? String(waFileTextEl.value || "") : ""; if (!text) text = await buildWaFileText(); if (!text) return; const opened = openWhatsAppWithText(text); setWaFileStatus(opened ? "تم فتح واتساب." : "تعذر فتح واتساب."); }); } if (batchTreeBuild) { batchTreeBuild.addEventListener("click", () =>{ hideAlert(); runBatchTreeBuild(); }); } if (batchTreeCopy) { batchTreeCopy.addEventListener("click", async () =>{ hideAlert(); let sql = batchTreeSql ? String(batchTreeSql.value || "") : ""; if (!sql) sql = (runBatchTreeBuild().sql || ""); if (!sql) { setBatchTreeStatus("لا يوجد أمر صيانة للنسخ."); return; } const ok = await copyText(sql); setBatchTreeStatus(ok ? "تم نسخ أمر الصيانة." : "تعذر نسخ أمر الصيانة."); }); } if (batchTreeDownload) { batchTreeDownload.addEventListener("click", () =>{ hideAlert(); let sql = batchTreeSql ? String(batchTreeSql.value || "") : ""; if (!sql) sql = (runBatchTreeBuild().sql || ""); if (!sql) { setBatchTreeStatus("لا يوجد أمر صيانة للتحميل."); return; } downloadTextFile("alzidan-batch-tree-import.txt", sql, "text/sql;charset=utf-8"); setBatchTreeStatus("تم تحميل ملف الصيانة."); }); } document.addEventListener("visibilitychange", () =>{ if (document.visibilityState === "visible") { if (getAdminToken()) { pollPendingRequestsForNotifications().catch(() =>{}); startPendingPolling(); } } else { stopPendingPolling(); } }); })(); window.addEventListener("load", () =>{ try { if (typeof loadTickerSpeedSetting === "function") loadTickerSpeedSetting().catch(() =>{}); } catch (e) { console.warn("تعذر تحميل إعداد سرعة الشريط:", e); } });
